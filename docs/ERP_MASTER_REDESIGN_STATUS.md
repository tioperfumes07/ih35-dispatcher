# IH35 ERP — Master redesign checklist vs codebase

**Consolidated report:** For a single document with **changelog**, **recommendations**, and **verification**, see **[`ERP_MASTER_REDESIGN_FINAL_REPORT.md`](./ERP_MASTER_REDESIGN_FINAL_REPORT.md)**. **Post-checklist / deferred work** (e.g. open bills pager + selection model): **[`ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md`](./ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md)**. **Suggested QA when redesign is “complete” / before release:** **[`ERP_MASTER_REDESIGN_POST_RELEASE_CHECKLIST.md`](./ERP_MASTER_REDESIGN_POST_RELEASE_CHECKLIST.md)**.

This file maps the **consolidated master redesign** (Rules 0–24) to **this repository** (`public/*.html`, `public/css/*`, `server.js`, no `/src` React tree). It is the durable checklist the assistant referenced when uploaded lists lived only in chat.

**Implementation guardrails (not “authorization gates”):** Ship checklist work in **focused diffs** (UI, CSS, client JS, read-only `GET` additions). **Prefer** not mixing large **save/post/sync** semantic changes with cosmetic passes—when persistence must change, document it in the PR. **Do not** remove **application authentication**, roles, API tokens, or user-visible security controls. **Do not** remove form fields or **parts-map SVG** assets. **Architecture deferrals** (e.g. open bills pager + selection model) stay in [`ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md`](./ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md) until explicitly picked up. **Loads & dispatch board** (`dispatch.html` and **dispatch-only** styling): **lowest priority** — treat as **end-of-checklist** work; routine autonomous passes **skip dispatch-only** changes unless explicitly clearing a backlog item. **Hourly Word-openable progress report:** run **`npm run report:erp`** (writes [`docs/reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf`](./reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf)); schedule with **cron** per [`docs/reports/README.md`](./reports/README.md).

**Architecture note:** The spec assumes `/src/styles/design-tokens.css` and `/src/components/StandardExpenseLines`. This app is **vanilla HTML + inline scripts + Express**. Equivalent locations are `public/css/` and markup inside `public/maintenance.html` (and siblings).

**Owner read:** Prefer the **scheduled RTF report** ([`docs/reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf`](./reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf) via **`npm run report:erp`**) for percentage + changelog + policy instead of chat-by-chat summaries. Routine passes still run **`npm run smoke`** when the environment allows.

---

## Master checklist progress (revised)

**Overall completion: ~66.3%** toward the **documented intent** of Rules **0–24** in this repo (not pixel-perfect spec parity). Figures are for **planning and continuity**; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ **0.6628** → **66.28%**; rounded headline **~66.3%**.)

**Method:** Treat each rule **0–24** as **one equal unit** (4% of the bar each). Assign a **fraction complete** per rule from the status text below (Done ≈ 1.0, strong Partial ≈ 0.45–0.75, Blocked/Future without alternate path ≈ 0–0.2, Skipped-by-design ≈ 0.85–1.0). **Overall = average of those 25 fractions × 100%.**

| Rule | Theme | Fraction (this revision) | Notes |
|------|--------|-------------------------|--------|
| 0 | Design tokens | 0.99 | **`erp-master-spec-2026.css`** **≤900px** **`body.hub-page`** **`.board-nav-inner`** + **`.board-nav-trigger`** padding trim (**changelog 130**); **`index.html`** hub **≤900px** **`header` / `.grid` / `a.card` / `footer`** padding + gap tighten (**changelog 129**); **`banking.html`** **≤900px** inline **`.wrap` / `.ph` / `.pb`** padding trim (**changelog 128**); **`settings.html`** **`.panel`** border prefers **`--color-app-frame-border`** with **`--app-frame-border`** fallback (**changelog 127**); **`fuel.html`** + **`settings.html`** page **`<style>`** (and **settings** inline **`select`** borders) use **`var(--color-border, var(--line))`** where borders previously used bare **`--line`** (**changelog 126**); **`banking.html`** page **`<style>`** **`.panel` / `.ph` / `.metric` / `button` / `textarea` / `th|td`** borders use **`var(--color-border, var(--line))`** (**changelog 125**); **`maint-accounting-ui-2026.css`** + **`erp-master-spec-2026.css`** **row / group** **`border-bottom`** rules (**WO unit list**, **upload recent table**, **QB cost-line `td`**, **`.erp-sb-group*`**, **`.mr-rep-nav-group`**) use **`var(--color-border, var(--line))`** instead of **`--color-bg-header`** as a divider surrogate (**changelog 124**); **`board-nav.css`** **`.board-nav-dd a`** row dividers use **`var(--color-border, var(--line))`** instead of **`--color-bg-header`** as a border surrogate (**changelog 123**); **`app-theme.css`** **maintenance** **`.topbar`**, **`.sidebar-expand-fab`**, **default** **`.btn` / `button`**, **`input|select|textarea`** borders → **`var(--color-border, var(--line|line-strong))`** (**changelog 122**); **`app-theme.css`** global **`:focus-visible`** outline prefers **`--color-border-focus`** with **`--accent`** fallback (**changelog 121**); **`board-nav.css`** **`.board-nav-dd`** border prefers **`--color-border`** before legacy **`--line`** (**changelog 120**); **`design-tokens.css`** **`--color-app-frame-border`** (navy frame, bridges legacy **`--app-frame-border`**) + **`app-theme.css`** **`.qb-doc-shell`**, **`.panel-head`**, **`.maint-section-card`**, **WO** **`.maint-wo-banner` / `.maint-subcard`**, **`#section-maintenance .panel-head`**, **`#section-maintenance .unit-box`**, **`.maint-form-stack .panel.maint-cost-panel`**, **`.maint-board-nav__btn.active`** / **`.maint-repair-chip--active`** surfaces (**changelog 119**); **`app-theme.css`** **banking** / **fuel** **`.panel`** frame borders + **fuel** **`.fleet-*`** **`border-color`** (**changelog 119**); **`app-theme.css`** **maintenance** **WO** **modal/cost/save** (**`.maint-workorder-fullmodal__head`**, **`.maint-cost-table__totalrow`**, **`.maint-cost-lines-head|footer`**, **`.maint-cost-panel-card`**, **`.maint-save-toolbar`**, **`.maint-panel-head-center`**, **`.btn--ghost`**, **`.record-card:hover`**) → **`var(--color-border, var(--line-strong))`** (**changelog 118**); **`app-theme.css`** **maintenance** **`.subtab`**, **`.wo-line`**, **shop** **`.shop-col|row`**, **`.asset-cat-tile`**, **`.shop-wo-fallback`**, **WO aside** **`.maint-aside-*`**, **`.maint-asset-header-card`**, **`.maint-asset-status-panel`**, **`.maint-cost-table`**, **`.acct-fuel-import-table`** borders → **`var(--color-border, var(--line|line-strong))`** (**changelog 117**); **`app-theme.css`** **maintenance** **`.unit-box`**, **`.table-wrap*`**, generic **`table th|td`**, **`.st-settlement-wrap`** borders **`var(--color-border, var(--line|line-strong))`** (**changelog 116**); **`app-theme.css`** **maintenance** **`.chip` / `.list` / `.list-row`** borders + **`.chip.active` / `.list-row.active`** **`--pill-blue-*`** + **`--btn-info-border`** (**changelog 115**); **`app-theme.css`** **maintenance** **`.status-*`** list pills use **`--pill-*`** with **`--green-soft` / `--accent-soft`** fallbacks (**changelog 114**); **`app-theme.css`** **dispatch (TMS)** **`.topbar` hover** / **secondary:hover** / **`.banner` / `.banner.err`** / **`table.data th`** use **`var(--color-bg-hover|pill-blue-bg|warning-border-soft|btn-danger-border)`** (**changelog 113**); **`erp-master-redesign.css`** **dispatch** hovers / **secondary** / **data** **`th`** / **modal** **`h2`** / **sidebar** / **`.erp-btn-primary`** use **`var(--color-bg-header|pill-blue-bg|btn-info-border|color-bg-active|color-bg-card)`** (**changelog 112**); **`erp-master-redesign.css`** **`:root`** **`--qb-*`** bridges to **`--color-*` / `--btn-*` / `--pill-*`** ( **`--qb-amber`** stays literal — spec **warning** token is brown, not amber ) (**changelog 111**); **`app-theme.css`** **WO** tire/part **SVG** dots + **`.qb-btn-primary`** + **`.cost-total-*`** + **settlement** **`td`** borders + **dashed** map/attach (**changelog 110**); **`app-theme.css`** **catalog** / **qb-picker** / **hero+panel** + **`.qb-doc-actions`** (**changelog 109**); **`app-theme.css`** **shop** / **board nav** / **KPI** / **`.nav-badge--danger`** (**changelog 108**); **`app-theme.css`** **`.qb-split*`** / **`.qb-in`** save-bar + form control borders (**changelog 107**); **`app-theme.css`** **`.sec-alert-*`** + **`.catalog-admin-table`** + **print** body + **`.acct-fuel-import-scroll`** (**changelog 106**); **`app-theme.css`** **WO** banner/subcard/save toolbar + **repair/cost line** chrome + **`.qb-split__menu*`** + semantic save hints (**changelog 105**); **`app-theme.css`** **maintenance** **`.qb-doc-*`** / **`.qb-lines-*`** + **nav dropdown** + **`.maint-modal-bg`** + **WO** asset/cost chrome (**changelog 104**); **`app-theme.css`** **maintenance** **`.erp-drawer*`** borders + **safety** / **tracking** / **shop** / **iframe** / **WO modal** / **fuel import** table chrome (**changelog 103**); **`design-tokens.css`** **`--shadow-menu-panel`** + **`erp-master-spec-2026.css`** busy-spinner **`color-mix`** (**changelog 102**); **`design-tokens.css`** **`--shadow-drawer`** + **`app-theme.css`** **`.erp-drawer`** **`box-shadow`** (**changelog 101**); **`app-theme.css`** **`.hub-page`** / **hub cards** + **`.banking-page button.primary`** + **`.erp-drawer__backdrop`** token stacks (**changelog 100**); **`app-theme.css`** legacy **maintenance** **`.nav-btn:hover|active`** **`color-mix`** over **`--sidebar-text` / `--nav-hover`** (**changelog 99**); **`app-theme.css`** legacy **maintenance** sidebar/list/table/**settlement** + **`.erp-drawer*`** (**changelog 98**); **`app-theme.css`** catalog + **qb-picker** + **maintenance** form control surfaces (**changelog 97**); **`maint-accounting-ui-2026.css`** maintenance WO/accounting/upload/cost-line chrome token sweep (**changelog 96**); **`erp-master-redesign.css`** maintenance + **fuel / banking / settings** hovers/surfaces + **`--qb-sidebar-active-bg`** bridge (**changelog 95**); **Global** **`.erp-pager__btn` / `__size-sel`**, **`.erp-toast*`**, **`.erp-new-menu__sep*`**, **`.erp-sb-sep*`**, **`.bp-driver-pay-chip`**, primary busy-spinner notch (**changelog 94**); **`erp-master-spec-2026.css`** AP/fuel **`.qb-doc-*`** footers + close buttons + **pay bills** table/log wraps + **reports hub** tiles + **`.erp-qb-cost-global*`** use **`var(--color-*|pill-blue-*)`** (**changelog 93**); **`erp-master-spec-2026.css`** **`.mr-pill--*`** + **module/reports** collapsible sidebars + **WO parts map** + **`#erpTrackingMap`** + **AP doc** topbar/backlink use **`var(--pill-*|color-*)`** (**changelog 92**); **`board-nav.css`** bridges dropdown/hover to **`var(--color-*|pill-*|shadow-dropdown)`**; **QBO sync alert bar** + **`#authBanner.erp-auth-banner`** use **`pill-*`** / **`color-*`** fallbacks in CSS (**maintenance** + **banking**); **Accounting** `#maintIntegrationStrip` + **posting preflight** + **Lists & catalogs** subtitle use **`var(--color-border|bg-card|text-body|text-label|semantic-warning)`**; **Settlement** TMS box + rollback + **Fuel expense** Relay panel + **expense history** / **AP doc** QBO banners + **rollback** scroller + **fleet roster** use **`var(--color-border|bg-card|bg-header|text-body)`**; **`renderReportsAll`** / **`renderRepMaintSpendByUnitTable`** empty states use **`var(--color-text-label|semantic-error)`**; **`fuel.html`** page `<style>` + roadmap / write-banner / relay info + map-adjacent surfaces use **`var(--color-*|pill-*|btn-info-*|shadow-dropdown)`** (**changelog 53**); **`banking.html`** panel headers + default buttons use **`var(--color-bg-header|bg-card)`**; **`settings.html`** primary buttons use **`var(--color-bg-card)`** for label fill (**changelog 54**); **`index.html`** hub: body glow **`var(--color-nav-bg)`**, card hover **`var(--shadow-dropdown)`**, workspace **`pill-*`** / **`btn-info-*`** tag chips (**changelog 55**); **`design-tokens.css`** **`--color-hub-*`** + **`index.html`** loads tokens before hub `<style>` (**changelog 68**); **`erp-master-spec-2026.css`** hub **`.erp-connection-strip`** neutral state uses **`--color-hub-*`** (**changelog 69**); **`body.hub-page .erp-help-tip__btn`** (border / fill / hover / **`focus-visible`**) uses **`--color-hub-*`** (**changelog 73**); **`board-nav.css`** **`body.hub-page`** strip + **dropdown** menus use **`--color-hub-*`** + **`--font-spec-sans`** (**changelogs 70–71**); **`dispatch.html`** inline **`maint-banner`** tiers, **delivered** chip, **stop** / **miles** panels, **autocomplete** list, **embed** separator, **`.btn-primary`** text use design-token fallbacks (**changelog 56**); **`maintenance.html`** **`#retiredAssetsWrap`** border uses **`var(--color-border, …)`** (**changelog 58**); **Reports → Team & security** injected posture / team / audit / CMMS panels use **`border:1px solid var(--color-border, var(--line))`** (**changelog 60**); **WO** **QuickBooks — posting header** dashed divider + **shop queue PDF** link border use **`var(--color-border, var(--line))`** (**changelog 61**); **Reports → Maintenance detailed** JS-rendered cost-line + record cards + **Lists → Vendors** cache scrollboxes use **`var(--color-border, var(--line))`** (**changelog 62**); **`.erp-pager`** / **`erp-pager__btn`** / **`erp-pager__size-sel`** borders use **`var(--color-border, var(--line, …))`** (**changelog 63**); **`.erp-pager`** surface + **`erp-pager__btn` / `erp-pager__size-sel`** **`:focus-visible`** (**changelog 84**); **`board-nav`** **`:focus-visible`** rings (**changelog 72**); **Upload center** active tab color / underline use **`var(--color-border-focus)`** in **`maint-accounting-ui-2026.css`** + **`erp-master-spec-2026.css`** (**changelog 75**); **Upload center** drop targets (border, surface, hover, labels, icon) use **`var(--color-*|pill-blue-bg|color-border-focus)`** (**changelog 76**); **`erp-master-redesign.css`** **`#qboSyncAlertBar`** visible chrome uses **`var(--pill-red-*|btn-danger-border|color-text-primary)`** (**changelog 76**); **`app-theme.css`** **`.qbo-sync-meta`** adds **`--color-text-label`** fallback; **`.qbo-sync-actions`** links / buttons **`:focus-visible`** ring (**changelog 76**); **Upload** **`.mr-upload-drop`** **`tabindex="0"`** / **`role="button"`** / **Enter / Space** + **`:focus-visible`** ring (**changelog 77**); **`erp-master-spec-2026.css`** **`.erp-help-tip*`** + **`body.erp-maintenance .mr-filter-chipwrap`** / **`.mr-filter-drop`** / **`.mr-filter-search`** use **`var(--color-*|pill-blue-*|shadow-dropdown)`**; chip **`<summary>`** **`:focus-visible`** (**changelog 78**); **`mr-filter-search`** / **`mr-filter-inline-select`** **`:focus-visible`** (**changelog 79**); **`maint-accounting-ui`** **`.mr-upload-help`** body + **`summary`** tokens + **`summary` `focus-visible`** (**changelog 79**); **`mr-filter-bar .qb-in` / `.in`** **`focus-visible`** + **`.mr-violation-legend*`** pill tokens (**changelog 80**); **Upload → Connections** **`.mr-upload-conn-card*`** / **`.mr-upload-pill-*`** tokens (**changelog 81**); **`fuel` / `banking` / `settings`** **`mr-filter-bar`** **`:focus-visible`** on **`.qb-in` / `.in` / `select.in` / `.btn` / `button`** (**changelog 82**); **`fuel` / `banking` / `settings` / `dispatch`** **`board-nav-wrap`** uses **`--font-spec-sans`**; **`.board-nav-trigger`** / **`.board-nav-strip-toggle`** **500** weight (**changelog 83**); **Reports** `.erp-reports-roadmap` callout uses **`var(--pill-yellow-*|color-semantic-warning)`** (**changelog 85**); **`body.erp-maintenance .modal`** border uses **`var(--color-border, var(--qb-line))`** (**changelog 86**); **`--color-modal-backdrop`** + **`.modal-bg`** use that token (**changelog 87**); **Dispatch** **`.modal-bg` / `.modal`** use the same backdrop + border tokens (**changelog 88**); **KPI “ok”** card border + **WO cost table** header row surface use **`var(--color-*)`** (**changelog 88**); **`.erp-connection-strip--ok` / `--warn`** tier borders use **`--color-success-border-soft` / `--color-warning-border-soft`**; **`.erp-dedicated-form-modal__backdrop`** uses **`--color-modal-backdrop`** (**changelog 89**); **maintenance** prior passes; full **`app-theme`** migration still deferred |
| 1 | Responsive | 0.555 | **`erp-master-spec-2026.css`** hub **workspace strip** fits narrow viewports with the **index.html** hub grid pass (**changelog 130** + **129**); **`index.html`** company hub **≤900px** chrome (**changelog 129**); **`banking.html`** **≤900px** **`.wrap`**, **`.ph`**, **`.pb`** padding tighten (**changelog 128**); **`fuel.html`** **≤900px** topbar + **`.layout`** padding/gap tighten (**changelog 127**); **`settings.html`** **≤900px** **`body`** + **`.panel`** padding tighten (**changelog 127**); **`app-theme.css`** **fuel** **`.fleet-columns` / `.fleet-section-title` / `.fleet-item`** **`border-color`** → **`var(--color-border, var(--line))`** (**changelog 119**); Shell patterns; **Reports** toolbar + main column gap tighten at **≤900px** (**`erp-master-spec-2026.css`**); **≤900px** main column + top bar horizontal padding tighten (**changelog 85**); **≤900px** module sidebar head / inner / footer horizontal padding tighten (**changelog 87**); **≤900px** **dispatch** board wrap + QBO strip margins; **banking** **`.wrap`** + **settings** **`body`** horizontal padding; **fuel** QBO strip margins (**changelog 88**); **≤900px** **company hub** **`.erp-connection-strip`** horizontal padding tightens (**changelog 89**); **Upload center** tab padding + panel + drop zone tighten at **≤900px** (**`maint-accounting-ui-2026.css`** — **changelog 74**); full viewport audit deferred |
| 2 | App shell | 0.664 | **`erp-master-spec`** **≤900px** **`body.hub-page .board-nav-*`** padding pairs with **hub** **`index.html`** layout (**changelog 130**); **`index.html`** hub **≤900px** layout padding (**changelog 129**); **`fuel.html`** / **`settings.html`** **≤900px** shell padding + **settings** **`.panel`** **`--color-app-frame-border`** stack (**changelog 127**); **`fuel.html`** / **`settings.html`** satellite chrome borders align with **`--color-border`** stacks like **banking** (**changelog 126**); **`board-nav.css`** **workspace dropdown** link row borders tokenized (**changelog 123**); **`app-theme.css`** **`:focus-visible`** ring aligns with **`design-tokens`** focus color when linked (**changelog 121**); **`board-nav.css`** **`.board-nav-dd`** shell border stack (**changelog 120**); **`app-theme.css`** **dispatch (TMS)** legacy **`.topbar` / `.banner` / `table.data`** (**changelog 113**); **`erp-master-redesign.css`** **dispatch** **`.topbar` / `.tabs` / `#dispatchApp`** surfaces: raw grays/blues replaced with **`--color-*` / `--pill-*` / `--btn-info-border`** stacks (**changelog 112**); **`erp-master-redesign.css`** **`:root`** **`--qb-page|panel|line|text|icon-nav-*`** etc. read **`design-tokens`** when linked (**changelog 111**); **`app-theme.css`** **maintenance** **`.nav-dropdown-menu`** / **`.nav-dd-item:hover`** use **`--sidebar-border`** + **`var(--shadow-dropdown)`** + **`color-mix`** on **`--sidebar-text`** (**changelog 104**); **`app-theme.css`** **`.hub-page`** landing gradient + body text + **workspace cards** use **`--color-hub-*`** + **`shadow-dropdown`** on hover (**changelog 100**); **`index.html`** hub **`.tag-tracking`** chip uses **`var(--pill-blue-bg)`** (**changelog 97**); **`erp-master-redesign.css`** sidebar/footer + topbar hovers + icon rail label tokens on **maintenance** + **fuel / banking / settings** chrome (**changelog 95**); **Maintenance topbar** compact workspace lead + **`erp-help-tip`** (**`.topbar-hint-wrap`**); ≤**720px** hides the short lead, keeps **`?`** (**changelog 86**); **`erp-master`** + **Lists** (**▤**) icon opens **`#catalog`**; dispatch + **settings** + hub **`board-nav`** strip (Accounting dropdown adds dedicated **#accounting-*** expense / pay links — **changelog 64**); **`index.html`** company hub header **`erp-help-tip`** for cards + strip + QBO line (**changelog 55**); **`index.html`** hub workspace **`a.card:focus-visible`** ring (**changelog 73**); **`board-nav.css`** hub strip + **dark dropdowns** match **`--color-hub-*`** (**changelogs 70–71**); **`board-nav`** **`:focus-visible`** (**changelog 72**); **`erp-master-spec`** satellite + **dispatch** **`board-nav`** **`--font-spec-sans`** + **500** triggers (**changelog 83**); **`system-smoke.mjs`** GET **`/index.html`** (**changelog 67**) + **`/tracking.html`** (**changelog 85**); spec dimensions/copy deferred |
| 3 | Collapsible sidebar | 1.0 | **`erp-master-spec-2026.css`** **`.erp-sb-group*`** / **`.erp-sb-group__head`** + **reports** **`.mr-rep-nav-group`** **`border-bottom`** use **`var(--color-border, var(--line))`** instead of **`--color-bg-header`** (**changelog 124**); pattern shipped (`ih35_sb_*`) |
| 4 | + New menu | 0.48 | **`erp-master-spec-2026.css`** **`#erpApp .erp-new-menu`** **`box-shadow`** uses **`var(--shadow-menu-panel)`** (**changelog 102**); **+ New** section separators (**`.erp-new-menu__sep*`**) use **`var(--color-bg-header|text-placeholder)`** (**changelog 94**); **+ New** menu item links use **`var(--pill-blue-text)`** (**changelog 92**); **+ New** column title row uses **`var(--color-text-label|color-border)`** (**changelog 91**); **+ New** three-column menu: items **A–Z** within each column; **Expenses & bills** adds **Standard** / **Maintenance & fuel** / **Drivers** separators + **Maintenance expense**, **Maintenance bill**, **Fuel expense**, **Fuel bill**, **Vendor / driver bill**, **Driver bill pay** (**changelog 64**); spec width/matrix / full Rule **5** shell parity deferred |
| 5–9 | Modal shells | 0.47 each (avg) | **`app-theme.css`** **`.erp-drawer__backdrop`** uses **`var(--color-modal-backdrop)`** (**changelog 100**); **`app-theme.css`** **`.erp-drawer*`** shell uses **`var(--color-bg-card)`** (**changelog 98**); QB-style modals; **`#erpDedicatedFormModal`** overlay teleports existing **AP / fuel manual** shells (Rule **5**-adjacent dedicated entry — **changelog 64**); parity not verified rule-by-rule (table row is shorthand — **average** still uses **five** × **0.47** for rules **5–9** in the ÷25 sum) |
| 10 | StandardExpenseLines | 0.15 | No React tree; documented blocked / future |
| 11 | Pay bills | 0.56 | **`app-theme.css`** **`.qb-doc-actions`** border token (**changelog 109**); **`app-theme.css`** **`.qb-doc-shell`** / **`.qb-doc-head`** / **lines** / **attach** surfaces (**changelog 104**); **Driver bill pay** **`.bp-driver-pay-chip`** uses **`var(--pill-blue-*|btn-info-border)`** (**changelog 94**); **`#acct-bill-pay`** **`qb-doc-shell--bill-pay`** + **`.mr-bp-table-wrap`** / **`.mr-bp-log-wrap`** borders/surfaces use **`var(--color-border|bg-card)`** (**changelog 93**); flows exist; **Driver bill pay** hash **`#accounting-driver-bill-pay`** + panel title + **Driver payees only** chip + **client-side** open-bills filter vs **`/api/maintenance/vendor-catalog`** driver **`qboVendorId`** set (**changelog 64**); open-bills pager + selection model still deferred |
| 12 | Maintenance layout | 0.702 | **`maint-accounting-ui-2026.css`** **WO unit picker rows**, **upload recent table cells**, **QB cost-line table `td`** dividers → **`var(--color-border, var(--line))`** (**changelog 124**); **`app-theme.css`** **shell** **`.topbar`** rule + **`.sidebar-expand-fab`** + **generic** **`.btn` / `button` / form fields** use **`--color-border`** stacks (**changelog 122**); **`app-theme.css`** **panel-head** / **QB doc shell** / **section cards** / **WO banner+subcard** / **maintenance unit+cost panel** frame borders use **`--color-app-frame-border`**; **shop board nav active** + **repair chip active** use **`--color-nav-bg`** fill (**changelog 119**); **`app-theme.css`** **WO full modal head** + **cost lines head/footer** + **cost panel** + **save toolbar** + **panel head center** + **ghost btn** + **record hover** border stacks (**changelog 118**); **`app-theme.css`** **subtabs** + **WO lines** + **shop** + **catalog tiles** + **WO aside** + **asset header/status** + **cost table** border stacks (**changelog 117**); **`app-theme.css`** **`.unit-box` / `.table-wrap` / `table` / `.st-settlement-wrap`** border stacks (**changelog 116**); **`app-theme.css`** **`.chip*`** / **`.list*`** home/dashboard chrome (**`--color-border`**, **`--pill-blue-*`**, **`--btn-info-border`**) (**changelog 115**); **`app-theme.css`** **`.status-current|soon|past|posted|error`** → **`--pill-*`** + legacy semantic soft colors (**changelog 114**); **`app-theme.css`** **tire** + **parts-map** dots + **`.qb-attach-box`** dashed + **`.cost-total-*`** rings (**changelog 110**); **`app-theme.css`** **Lists & catalogs** add-card + search + **qb-picker** shadow + **home** **hero/panel/metric/record** shells (**changelog 109**); **`app-theme.css`** **shop queue** + **shop board nav** + **KPI strip** cards + **expense pills** (**changelog 108**); **`app-theme.css`** **`.catalog-admin-table`** borders/header (**changelog 106**); **`app-theme.css`** **WO** **banner** / **subcard** / **save toolbar** + **repair chips** + **cost line** blocks + **panel head center** (**changelog 105**); **`app-theme.css`** **`.shop-col-head`** + **WO** **KPI** / **asset header** / **status** / **cost table** header (**changelog 104**); **`app-theme.css`** **tracking** / **iframe** / **tire** / **parts-map** + **WO full modal** / **`.maint-modal`** surfaces (**changelog 103**); **`app-theme.css`** **`.panel-head`** gradient + **chips** / **lists** / **unit-box** / **`.table-wrap*`** (**changelog 98**); **`app-theme.css`** **Lists & catalogs** add-card / search / **qb-picker** + **asset category** tiles + **iframe** surfaces (**changelog 97**); **`maint-accounting-ui-2026.css`** **WO** action strip, unit list, two-column shell, **`.maint-qb-cost-details*`** / **segmented control** (**changelog 96**); **embedded cost shell** + **list-row** hover + **ops hero** strip surfaces use **`var(--color-bg-header|bg-hover)`** in **`erp-master-redesign.css`** (**changelog 95**); **WO** **`.erp-qb-cost-global*`** (default new cost line row) uses **`var(--color-text-label|text-body)`** (**changelog 93**); **parts map** + **tracking map** + **AP doc** shell surfaces tokenized in **`erp-master-spec-2026.css`** (**changelog 92**); **Lists & catalogs** sub-tab intros (**Rule 22**) + **Service types (DB)** filter/pager (**Rules 15/23**); **Accounting → QuickBooks rollback** title **`erp-help-tip`** + compact body copy; **Home** **System / Sync Summary** + **Maintenance → Units** panel titles **`erp-help-tip`** (**changelog 58**); **Maintenance Table** + **Unit History** + **Tracking → Map** (**Units** + **Live map**) titles **`erp-help-tip`** (**changelog 59**); **Tracking → Map** **Rename vehicle in Samsara** row: dense token note → **`erp-help-tip`** + short muted lead (**changelog 74**); **Rename driver in Samsara** same **`erp-help-tip`** + muted lead (**changelog 75**); **Imports & uploads** title **`erp-help-tip`** + **Shop** **Maintenance reports** / **Integrity alerts** / **Tracking** **Yard & idle** + **Summary** title tips (**changelog 60**); **Operational status** sheet + **QuickBooks — posting header** on WO form **`erp-help-tip`** (**changelog 61**); **Maintenance** top **Workspace snapshot** KPI strip **`erp-help-tip`** (**changelog 62**); **Tracking** top **Fleet mix snapshot** KPI row + **`erp-help-tip`** (**changelog 63**); **Accounting** sidebar **Specialized** expense/bill links + dedicated modal host (**changelog 64**); **Accounting → Maintenance expenses** AP doc **category / item** line tables + JS (**changelog 65**); **`openSection` / `openAccountingTab` / `+ New` doc-click** guards + **`applyNavHash`** after **`loadAll`** failure (**changelog 84**); full spec QA deferred |
| 13 | Accounting board | 0.588 | **`banking.html`** inline panel + metric + table chrome borders align with **`--color-border`** stacks (**changelog 125**); **`maint-accounting-ui-2026.css`** **`.maint-qb-lines-table tbody td`** row rules use **`var(--color-border, var(--line))`** (**changelog 124**); **`app-theme.css`** **maintenance** **`.qb-doc-shell`** outer border → **`--color-app-frame-border`** stack (**changelog 119**); **`app-theme.css`** **`.acct-fuel-import-table`** cell rules **`var(--color-border, var(--line))`** (**changelog 117**); **`app-theme.css`** **`.qb-btn-primary`** + **`.qb-attach-box`** dashed (**changelog 110**); **`app-theme.css`** **`.qb-in`** border tokens (**changelog 107**); **`app-theme.css`** **`.acct-fuel-import-scroll`** chrome (**changelog 106**); **`app-theme.css`** **`.maint-cost-coordination*`** + **`.qb-split__menu*`** (save/post chrome) (**changelog 105**); **`app-theme.css`** **`.qb-doc-*`** / **`.qb-lines-*`** (AP-style doc chrome on maintenance) (**changelog 104**); **`app-theme.css`** **`.acct-fuel-import-table thead th`** sticky header uses **`var(--color-nav-bg|sidebar-bg|sidebar-text)`** (**changelog 103**); **`maint-accounting-ui-2026.css`** **`.acct-dash-*`** KPIs/cards/buttons + scoped **`.erp-toast*`** + **`.acct-conn-*`** dots (**changelog 96**); **Maintenance expenses** AP + **fuel expense** doc footers + close controls use **`var(--color-*|pill-blue-*)`** (**changelog 93**); KPIs + dash cards; **`#acctBoardStrip`** **Accounting board** title row **`erp-help-tip`** (**changelog 62**); card subcopy uses **token** text colors; **`#authBanner`** login strip: **`erp-auth-banner`** token chrome on **maintenance** + **banking** (**`erp-master-spec-2026.css`**); **QuickBooks Live Master** title **`erp-help-tip`** + **Vendors** / **Items** / **Accounts** column title tips (**changelog 60**) + **AP** / **expense history** inline QBO banners use **token** borders/backgrounds (**changelog 47**); **Saved Maintenance Expense Transactions** + **Recent bill payments** + **Expense history log** panel title **`erp-help-tip`** (**changelog 60**); **QBO rollback** **ERP import batches** + **Fuel / Relay import batches** sub-head **`erp-help-tip`** (**changelog 61**); **Maintenance expenses** cost lines use split **category / item** grids aligned with WO cost breakdown (**changelog 65**); dark-panel cleanup partial |
| 14 | Upload center | 0.631 | **`maint-accounting-ui-2026.css`** **`.mr-upload-recent-table th|td`** **`border-bottom`** → **`var(--color-border, var(--line))`** (**changelog 124**); upload tabbar, **`.mr-upload-panel-card`** (single merged rule), pick line, progress, recent table, import buttons (**changelog 96**); tabs + flows; **?** tips on every upload subpanel intro (Bank, Comdata, Fuel, AP, Maint, Other, Connections); **Upload center** main panel title + **`erp-help-tip`** on one row (**changelog 61**); sub-tab row **A–Z** by label; **≤900px** upload tab + card + drop padding (**changelog 74**); **drop zone** colors use design-token fallbacks (**changelog 76**); **drop targets** keyboard (**`tabindex` + `role="button"` + `onkeydown`**) + **`:focus-visible`** (**changelog 77**); **`details.mr-upload-help`** **`summary`** token + **`focus-visible`** (**changelog 79**); **`.mr-upload-panel-title`** / **`.mr-upload-panel-desc`** / **`.mr-upload-section-h`** text tokens (**changelog 80**); **Connections** **`.mr-upload-conn-card*`** + **`.mr-upload-pill-*`** tokens (**changelog 81**); full file QA deferred |
| 15 | Filter bar | 0.68 | **`.mr-qbo-banner*`** (inline QBO issue strip) uses **`var(--pill-red-*|color-text-*|color-border|…)`** on typography + actions (**changelog 91**); several tables + **Security alerts** + **Lists & catalogs → Service types (DB)** + **Reports → Maintenance detailed report** + **Reports → Settlement** trip lookup + **Accounting → Fuel expenses** (ledger + **manual fuel / DEF** header grid) + **Expense history** (grid + date presets in **`mr-filter-bar__right`**) + **Pay bills** composer + **Recent bill payments** log filters + **Fuel & route** satellite **Diesel purchase** + **Bank & matching** steps **1** (history window) + **3** (batch id + suggestions) + **Settings → Employees** search row (**`body.fuel-board`**, **`body.banking-page`**, **`body.settings-page`** in **`erp-master-spec-2026.css`**) (**changelogs 48–54**); **`mr-filter-chipwrap > summary`** tokens + **`focus-visible`** + flyout **`var(--shadow-dropdown)`** (**changelog 78**); **`mr-filter-search`** / **`mr-filter-inline-select`** **`:focus-visible`** (**changelog 79**); **`mr-filter-bar`** **`.qb-in` / `.in`** **`focus-visible`** (**changelog 80**); **`mr-filter-bar .btn` / `button`** **`focus-visible`** on **maintenance** (**changelog 81**) + same on **`fuel` / `banking` / `settings`** (**changelog 82**); not universal |
| 16 | Safety / HOS | 0.58 | **`app-theme.css`** **`.sec-alert-high|warn|info`** tier borders (**changelog 106**); **`app-theme.css`** **`.safety-table-wrap`** border/surface tokens (**changelog 103**); Tables + **Rule 23** pagers on Safety + idle snapshot; **Active / in service** tab **Active drivers** + **In service now** column title **`erp-help-tip`** (**changelog 61**); **HOS clocks** tab: muted lead under legend + **`?`** (**changelog 77**); **`.mr-violation-legend`** text + dot swatches use **`var(--color-text-label|pill-red-*|pill-yellow-*)`** (**changelog 80**) |
| 17 | Reports | 0.67 | **`app-theme.css`** **settlement** **`td`** row dividers (**changelog 110**); **Settlement** **`.st-settlement*`** table chrome (header gradient + zebra/hover + borders) uses **`var(--color-nav-bg|bg-card|bg-hover|bg-header|border|sidebar-bg)`** in **`app-theme.css`** (**changelog 98**); **Reports hub** category grid (**`.erp-reports-hub*`** cards/tiles/active/hover) use **`var(--color-*|pill-blue-*|border-focus)`** (**changelog 93**); hub + settlement; **Reports** toolbar title + **`erp-help-tip`** (sidebar vs hub vs search) (**changelog 62**); **Executive overview** panel title + **`erp-help-tip`**; **TMS**, **QBO**, **sync**, **IFTA**, **Maintenance spend by unit**, **Team & security**, **Maint detailed** tab titles + **`erp-help-tip`**; **Team & security** body panels (**Configuration posture**, **ERP user activity**, **Security audit log**, **CMMS benchmark**) title **`erp-help-tip`** when rendered (**changelog 60**); **Settlement** trip cost rollup load # + actions use **`mr-filter-bar`** (**changelog 51**); **Settlement** **Trip cost rollup** + **Loads with recorded costs (index)** panel title **`erp-help-tip`** (**changelog 60**); **Settlement** TMS trip box uses **`var(--color-border|bg-card)`**; **Driver pay settlements** title **`erp-help-tip`** + short body lines; **QBO / sync / IFTA** bodies use shorter intro copy (**Rule 22**); **Executive overview** timestamp line + **TMS** summary / empty / error copy use token-muted (**Rule 0**); **Maint spend by unit** empty state two-line pattern; **Maint spend by unit** + **Maint detailed** bodies paginated (**Rule 23**); “all on one page” partial |
| 18 | QBO GET aliases | 0.90 | Skipped — existing catalog/status routes |
| 19 | Toasts | 1.0 | **`erp-master-spec`** **`.erp-toast*`** surface + message + tier dots use design-token fallbacks (**changelog 94**); **`showToast`** + **`erpNotify`** pattern |
| 20 | Button loading | 1.0 | **`app-theme.css`** **`.qb-split*`** focus rings + caret divider + **neutral** split surface (**changelog 107**); **`app-theme.css`** **`.maint-save-toolbar--qb .qb-split`** uses **`var(--shadow-sm)`** (**changelog 105**); **`erp-master-spec-2026.css`** **`.erp-btn--busy::after`** notch/ring uses **`color-mix`** on text/card tokens (**changelog 102**); `erpWithBusy` pattern; **Maintenance expenses** **Save** / **Post to QuickBooks** / **Save and close** wired (**changelog 64**) |
| 21 | QBO error banner | 0.54 | **`#qboSyncAlertBar`** **`position: sticky; top: 0`** so sync issues stay visible while scrolling the ERP main column; tier colors use **token fallbacks**; **`.qbo-sync-actions`** **`a` / `button`** **`:focus-visible`** (**changelog 76**); maintenance visible strip aligns **`erp-master-redesign`** with **`var(--pill-red-*|color-text-primary)`** (**changelog 76**) |
| 22 | “?” tips | 0.99 | **Maintenance topbar** workspace navigation: one-line lead + **`erp-help-tip`** (**changelog 86**); **Maintenance → work order** **Field help** drawer (**?**): short service lead + **`erp-help-tip`** for QBO cost lines / invoice lock (**changelog 85**); **Lists & catalogs** — every sub-tab (**Service**, **QBO**, **Vendors**, **Operational**, **Fleet**) has short intro + **?**; **Reports** hub toolbar + **`erp-help-tip`** (**changelog 62**); **Reports → Executive overview** title **`erp-help-tip`**; **Accounting → QuickBooks Live Master** + **rollback** + **Reports → Settlement → Driver pay settlements** panel titles **`erp-help-tip`**; **Accounting board** strip title **`erp-help-tip`** (**changelog 62**); **QBO activity**, **ERP ↔ QBO sync**, and **IFTA** bodies: dense paragraphs shortened + **`var(--color-text-label)`** on secondary lines; **TMS** + **Maint spend by unit** empty states split headline + muted steps; **Live Master** vendor intro folded into **`?`** + short lines; + **Team** dynamic intro + **Team** posture / roster / audit / CMMS panel title **`erp-help-tip`** (**changelog 60**) + prior report/upload/shop; **dispatch** sidebar **Tips** line + **?**; **`fuel.html`** **Selected Unit** panel title **`erp-help-tip`** (**changelog 53**); **`settings.html`** **Employees** title **`erp-help-tip`** (**changelog 54**); **`settings.html`** **Users** + **`banking.html`** **Import bank CSV** **`erp-help-tip`** (**changelog 57**); **`index.html`** company hub header **`erp-help-tip`** (**changelog 55**); **`index.html`** footer mileage note + **`erp-help-tip`** for OSRM / PCMiler / env (**changelog 67**); hub **`erp-help-tip__btn`** chrome matches dark shell (**changelog 73**); **`maintenance.html`** **Home** **System Summary** + **Sync Summary** + **Units** column title **`erp-help-tip`** (**changelog 58**); **Maintenance Table** + **Unit History** + **Tracking → Map** (**Units** + **Live map**) titles **`erp-help-tip`** (**changelog 59**); **Tracking → Map** **Rename vehicle in Samsara** **`erp-help-tip`** for ids / write token / 403 (**changelog 74**); **Rename driver in Samsara** **`erp-help-tip`** (**changelog 75**); **Imports & uploads** + **Shop** exports / integrity + **Tracking** idle / summary + **Accounting** saved AP / bill payment log / expense history titles + **Settlement** rollup / index + **Live Master** **Vendors** / **Items** / **Accounts** column tips (**changelog 60**); **Upload center** shell title row (**changelog 61**); **WO** **Operational status** + **QBO posting header** + **rollback** batch sub-heads + **Safety** **Active drivers** / **In service now** column tips (**changelog 61**); **Maintenance** **Workspace snapshot** strip (**changelog 62**); **Tracking** **Fleet mix snapshot** KPI row (**changelog 63**); **Safety → HOS clocks** muted intro (**changelog 77**); **inline `?`** **`.erp-help-tip*`** token chrome + **`focus-visible`** (**changelog 78**); many `mini-note` blocks remain |
| 23 | Pagination | 0.88 | **`.erp-pager__btn` / `__size-sel`** default/hover surfaces use **`var(--color-bg-card|bg-header)`** (**changelog 94**); broad **`erpPagerRender`** coverage; **Lists & catalogs → Service types (DB)** (**`serviceCatalogAdminPager`**); **Reports** spend-by-unit + maint detailed (prior); **`.erp-pager`** token surface + **`erp-pager__btn` / `erp-pager__size-sel`** **`:focus-visible`** (**changelog 84**); not every long table |
| 24 | Connection strip | 0.66 | **`erpMountConnectionStrip`** on **maintenance** + **dispatch** main columns (**`changelog 65`**, **`changelog 66`**) plus **banking**, **settings**, **fuel**, **hub**; **`system-smoke.mjs`** asserts **`erpConnectionStrip`** on **hub + ERP satellites** (**changelog 67**); **hub** neutral strip chrome matches **`--color-hub-*`** before tier classes (**changelog 69**); **dispatch** strip hidden in **`?embed=1`**; **`.erp-connection-strip--ok` / `--warn`** borders tokenized (**changelog 89**); universal Samsara+QBO banner deferred |

**Rolling average (above table):** sum of fractions ÷ 25 ≈ **0.6628** (**66.28%**; headline **~66.3%**). When reporting updates, **revise fractions** (not the formula) as work lands, then re-average.

**Intentionally out of this % (until product agrees):** items in [`ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md`](./ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md) (e.g. open bills pager + selection model).

---

## System inspection (high level)

| Area | Finding |
|------|---------|
| **APIs** | QuickBooks and Samsara routes live in `server.js` (`/api/qbo/*`, maintenance/fleet/board APIs). Token refresh and error handling are embedded in those flows — **not re-audited line-by-line** in this pass. |
| **Forms** | Primary ERP forms are in `public/maintenance.html` (maintenance, accounting, fuel tabs, uploads, etc.). Satellite pages: `banking.html`, `settings.html`, `fuel.html`, `dispatch.html`. |
| **Routes** | Static HTML under `public/`; no client router. |
| **Smoke test** | `node scripts/system-smoke.mjs` requires a **running server** and network to `localhost`; it failed in CI sandbox with “fetch failed” (expected when server off). |

---

## Rules — status

Legend: **Done** (meets intent in this repo), **Partial**, **Skipped** (already satisfied or out of scope), **Blocked** (would violate protection block or needs product decision), **Future** (large build).

### Rule 0 — Design system tokens

- **Partial (this pass):** Added `public/css/design-tokens.css` with spec-aligned **additional** `:root` variables (`--color-*`, **`--color-semantic-*`**, `--btn-*`, `--pill-*`, spacing, dimensions). Linked **before** `app-theme.css` on maintenance, dispatch, fuel, banking, settings. **Satellite page shells** use **`background: var(--color-bg-page, var(--bg))`** on **banking**, **settings**, and **fuel**; **company hub** uses dedicated **`--color-hub-*`** tokens + hub `<style>` after tokens (**changelog 68**). **Banking**, **settings**, **dispatch**, and **maintenance** inline / `<style>` blocks increasingly use **`var(--color-*|pill-*|btn-danger-*|focus-ring|semantic-*)`** (see changelog **33**). **`fuel.html`** inline layout CSS maps panels, badges, autocomplete, write-secret banner, and relay pointer box to **`var(--color-*|pill-*|btn-info-*|shadow-dropdown)`** (**changelog 53**). **`banking.html`** panel header gradient + default button fills; **`settings.html`** primary button text on accent uses **`var(--color-bg-card)`** (**changelog 54**). **`index.html`** hub inline CSS uses **`var(--color-nav-bg|shadow-dropdown|pill-*|btn-info-*|font-spec-sans)`** plus **`--color-hub-*`** for surfaces and text (**changelogs 55, 68**). **`dispatch.html`** load-board inline CSS: maintenance banners, delivered chip, stop / miles panels, autocomplete dropdown, embed separator, primary button label (**changelog 56**). **`maintenance.html`** **`#retiredAssetsWrap`** top border uses **`var(--color-border, …)`** (**changelog 58**). **Reports → Team & security** posture / team / audit / CMMS panels use **`border:1px solid var(--color-border, var(--line))`** (**changelog 60**). **WO** **QuickBooks — posting header** dashed divider + **shop queue PDF** link use **`var(--color-border, var(--line))`** (**changelog 61**). **Reports → Maintenance detailed** cards / lines + **Lists → Vendors** cache lists use **`var(--color-border, var(--line))`** in inline styles (**changelog 62**). **`erp-master-spec-2026.css`** **`.erp-pager`** shell + **Prev/Next** + page-size controls use **`border: 1px solid var(--color-border, var(--line, …))`** (**changelog 63**). **`#authBanner.erp-auth-banner`** on **maintenance** and **banking** uses the same token-backed warning strip in **`erp-master-spec-2026.css`** (changelog **43** + **46**). **`renderReportsAll`** overview timestamp, **TMS** loaded summary, and **`renderRepMaintSpendByUnitTable`** empty secondary line use **`var(--color-text-label)`**; **TMS** errors use **`var(--color-semantic-error)`** (changelog **45**). **Accounting** `#maintIntegrationStrip` + **`#maintPostingPreflight`** + **Lists & catalogs** panel subtitle use design-token fallbacks (**changelog 48**). **Company hub** QBO connection strip neutral state uses **`--color-hub-*`** (**changelog 69**). **Upload → Connections** **`.mr-upload-conn-card*`** / **`.mr-upload-pill-*`** use **`var(--color-*|pill-*-*)`** in **`maint-accounting-ui-2026.css`** (**changelog 81**). **`fuel` / `banking` / `settings`** **`mr-filter-bar`** **`:focus-visible`** rings match maintenance (**changelog 82**). **`fuel` / `banking` / `settings` / `dispatch`** **`board-nav`** strip uses **`--font-spec-sans`** + **500**-weight controls (**changelog 83**). **Reports** `.erp-reports-roadmap` callout uses **`var(--pill-yellow-*|color-semantic-warning)`** (**changelog 85**). **`body.erp-maintenance .modal`** border uses **`var(--color-border, var(--qb-line))`** (**changelog 86**). **`--color-modal-backdrop`** + **`.modal-bg`** overlay (**changelog 87**). **Dispatch** **`.modal-bg` / `.modal`** use the same tokens (**changelog 88**); **`.mr-kpi-card--ok`** + **WO cost table** header row use **`var(--color-border|bg-header)`** (**changelog 88**). **QBO strip** ok/warn tier borders + **dedicated expense modal** backdrop use new/linked tokens (**changelog 89**). **Maintenance** workspace KPI strip + toast dismiss control use **`var(--color-text-*|pill-red-bg|btn-danger-border)`** (**changelog 90**). **`.mr-qbo-banner*`** + **+ New** column titles use design-token fallbacks (**changelog 91**). **`app-theme.css`** **WO** **`.tire-dot` / `.maint-part-dot`** + **`.qb-btn-primary`** (**`--btn-primary-save-*`** + **`--color-bg-card`**) + **`.cost-total-*`** (**`--color-success-border-soft`**, **`--color-semantic-*`** + **`color-mix`** glows) + **settlement** **`td`** borders + **dashed** **`.qb-attach-box` / `.maint-line-part-map-host`** (**changelog 110**). **`erp-master-redesign.css`** **`:root`** **`--qb-page|panel|line|…`** bridge to **`design-tokens`** (**`--qb-amber`** stays literal) (**changelog 111**); **`body.dispatch-page`** rules in the same file (**`.topbar`**, **`.tabs`**, **`.data`**, **`#dispatchApp`**) replace leftover hex hovers/secondaries with **`--color-bg-header`**, **`--pill-blue-bg`**, **`--btn-info-border`**, **`--color-bg-active`**, **`--color-bg-card`** (**changelog 112**). **`app-theme.css`** **dispatch (TMS)** **`.topbar`**, **`.banner`**, **`table.data th`** align with **`--color-bg-hover`**, **`--pill-blue-bg`**, **`--color-warning-border-soft`**, **`--btn-danger-border`** (**changelog 113**). **Dashboard list** **`.status-*`** chips read **`--pill-*`** when tokens load (**changelog 114**). **`.chip` / `.list`** borders and **selected** chip/row use **`--color-border`** + **`--pill-blue-*`** (**changelog 115**). **`.unit-box`**, **`.table-wrap`**, **maintenance `table`**, **`.st-settlement-wrap`** borders use **`--color-border`** stacks (**changelog 116**). **Subtabs**, **shop**, **WO lines**, **catalog tiles**, **WO field-help aside**, **asset header/status**, **cost table**, **fuel import grid** use the same border stacks (**changelog 117**). **WO full-screen modal header**, **inline cost lines** head/footer, **cost panel card**, **save toolbar**, **centered panel head**, **ghost buttons**, and **record card hover** use **`--color-border`** over **`--line-strong`** (**changelog 118**). **`--color-app-frame-border`** in **`design-tokens.css`** bridges **maintenance**/**banking**/**fuel** navy panel frames without misusing **`--color-border`** (**changelog 119**). **`board-nav`** light dropdown (**`.board-nav-dd`**) border stack corrected (**changelog 120**). Global **`:focus-visible`** ring prefers **`--color-border-focus`** (**changelog 121**). **Topbar**, **sidebar expand FAB**, **default chrome buttons**, and **plain form controls** use **`--color-border`** stacks (**changelog 122**). **`board-nav`** workspace dropdown link dividers use **`--color-border`** instead of **`--color-bg-header`** as a border surrogate (**changelog 123**). **`maint-accounting-ui-2026.css`** + **`erp-master-spec-2026.css`** maintenance **list / table / sidebar group** **`border-bottom`** rules align the same way (**changelog 124**). **`banking.html`** satellite inline borders prefer **`--color-border`** before legacy **`--line`** (**changelog 125**). **`fuel.html`** / **`settings.html`** match the same border stack on panels, fleet chrome, topbar, form controls, autocomplete shell, and **settings** admin **`select`** rows (**changelog 126**). **`settings.html`** **`.panel`** reads **`--color-app-frame-border`**; **fuel** + **settings** add **≤900px** padding trims (**changelog 127**). **`banking.html`** matches **fuel/settings** narrow **`.wrap`** + panel header/body padding (**changelog 128**). **`index.html`** hub grid + cards + footer tighten at **≤900px** (**changelog 129**). **`erp-master-spec-2026.css`** trims **hub** **`.board-nav-inner`** gutter + **`.board-nav-trigger`** hit area at **≤900px** (**changelog 130**).
- **Not done:** Full migration of all components from legacy `--bg`, `--panel`, etc. to **`--color-*`** (would be a large visual sweep).
- **Spec path:** Use `public/css/design-tokens.css`, not `/src/styles/`.

### Rule 1 — Responsive layout

- **Partial:** `erp-master-spec-2026.css` + `app-theme.css` already enforce `min-width:0`, scroll columns, table wrappers for maintenance shell. **Reports** hub: at **≤900px**, toolbar padding and **`erp-reports-main`** vertical gap tighten slightly when the browse column stacks (**changelog 45**). At **≤900px**, maintenance **`.erp-main-body`** + **`.erp-topbar.topbar`** horizontal padding and gap trim slightly for tablets (**changelog 85**); module sidebar head / inner / footer padding trims slightly (**changelog 87**). **Dispatch** board shell, **banking** / **settings** page chrome, and **fuel** QBO strip margins tighten slightly on tablets (**changelog 88**); **company hub** QBO strip side padding tightens on tablets (**changelog 89**). **Fuel** fleet column chrome uses **`var(--color-border, var(--line))`** for shared dividers (**changelog 119**). Dispatch/fuel/banking use simpler layouts.
- **Future:** Strict “no horizontal scrollbar at 1280/1440/1920” audit across every section tab.

### Rule 2 — App shell

- **Partial:** Maintenance uses `erp-master` shell (icon nav, sidebars, top bar patterns in HTML/CSS). **`erp-master-redesign.css`** **`:root`** **`--qb-*`** maps into **`design-tokens`** (`--color-*`, **`--btn-primary-save-bg`**, etc.) when that sheet is linked (**changelog 111**); **dispatch** rows in the same file lose standalone grays/blues in favor of those tokens (**changelog 112**); **`app-theme.css`** still carries a slim **dispatch (TMS)** strip — **`.topbar` / `.banner` / `table.data`** now use **`design-tokens`** fallbacks too (**changelog 113**). **Lists & catalogs** is a first-class **Lists** (**▤**) target in icon nav (and **`/maintenance.html#catalog`** + **`#catalog-*`** sub-tabs); dispatch nav includes the same deep link. **Settings** uses the shared **`board-nav.js`** workspace strip (**`board-nav.css`**) like banking, fuel, dispatch, maintenance, and **company hub**. **`board-nav.css`** **`.board-nav-dd`** border prefers **`--color-border`** (**changelog 120**). **`app-theme.css`** **`:focus-visible`** uses **`--color-border-focus`** when tokens are linked (**changelog 121**); **maintenance** **`.topbar`**, **sidebar expand FAB**, **default buttons**, and **plain form fields** use **`--color-border`** stacks (**changelog 122**). **`board-nav`** dropdown link row dividers use **`--color-border`** (**changelog 123**). **`index.html`** hub header adds **`erp-help-tip`** for workspace cards + strip + QBO status (**changelog 55**); footer mileage / routing note uses the same **`?`** pattern (**changelog 67**); **company hub** **`board-nav`** strip + dropdowns use **`--color-hub-*`** (**changelogs 70–71**). **`erp-master-spec-2026.css`** aligns **fuel / banking / settings / dispatch** strip typography with **`--font-spec-sans`** + medium-weight triggers (**changelog 83**). **`system-smoke.mjs`** includes **`GET /index.html`** shell checks (**changelog 67**), **`GET /tracking.html`** redirect target (**changelog 85**), and asserts **`topbar-hint-wrap`** on **`/maintenance.html`** (**changelog 86**). Labels and module order still differ from the spec’s canonical list in places (e.g. “Fuel Plan” naming).
- **Future:** Align copy, dimensions (68px / 216px / 48px), and tab set exactly to spec.

### Rule 3 — Collapsible sidebar

- **Done (pattern):** Maintenance uses `erpSbGroup` / `erpToggleSbGroup` with `localStorage` keys `ih35_sb_*`. Not every sidebar in the spec has been re-enumerated item-by-item in this audit.

### Rule 4 — “+ New” menu

- **Partial:** Maintenance has a **+ New** menu; within each column (**Expenses & bills**, **Maintenance**, **Other**) menu rows are ordered **alphabetically** by label within **Standard** groups (**changelog 64** adds **Standard** / **Maintenance & fuel** / **Drivers** separators + specialized expense/bill shortcuts). Width/columns/custom fleet rows may not match the full 680px / three-column / custom-type matrix in the spec.
- **Blocked:** Implementing **new** modal types that change save endpoints would violate “no new backend unless instructed”; **UI-only** presets that reuse existing save paths ship as **dedicated modal entry** + hash routes (**changelog 64**) without new POST APIs.

### Rules 5–9 — Modal shells (Expense, Bill, WO, etc.)

- **Partial:** QuickBooks-style modals exist (`erp-qb-dialog`, maintenance work order / accounting flows). **`#erpDedicatedFormModal`** (**`erp-master-spec-2026.css`**) provides a dimmed overlay + scroll host that **teleports** the existing **Maintenance expenses** document shell and **Fuel manual** shell for focused entry (**changelog 64**). Full parity with spec (duplicate Rule **5** chrome, unsaved confirm on every edge) is **not verified rule-by-rule**.

### Rule 10 — StandardExpenseLines component

- **Blocked / Future:** There is **no** `/src/components/StandardExpenseLines`. Cost lines are implemented in **maintenance HTML/JS** (`maint-cost-line`, etc.). Extracting a shared component would be a large refactor; **not started** to avoid breaking saves.

### Rule 11 — Pay bills

- **Partial:** Pay bills / QBO flows exist in maintenance + `server.js` (`/api/qbo/open-bills`, `bill-payment`, etc.). **Driver bill pay** — **`#accounting-driver-bill-pay`**, panel retitle + **Driver payees only** chip, **client-side** filter on loaded open bills vs **`/api/maintenance/vendor-catalog`** driver **`qboVendorId`** set (**changelog 64**); open-bills **pager + selection** model still deferred per [`ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md`](./ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md).

### Rule 12 — Maintenance page layout

- **Partial / evolving:** Layout has been iterated (action bar, two-column patterns). **Lists & catalogs** sub-tabs use compact copy + **`erp-help-tip`** on each intro (Rule 22); **Service types (DB)** admin grid uses name filter + pager (**Rules 15/23**). **Accounting → QuickBooks rollback & import safety** uses the same title **`erp-help-tip`** + short intro lines (**changelog 46**). **Home** **System Summary** + **Sync Summary** and **Maintenance → Units** panel titles include **`erp-help-tip`** (**changelog 58**). **Maintenance Table**, **Unit History**, and **Tracking → Map** (**Units** + **Live map**) panel titles include **`erp-help-tip`** (**changelog 59**). **Imports & uploads**, **Shop** **Maintenance reports** / **Integrity alerts**, **Tracking** **Yard & idle** + **Summary** titles include **`erp-help-tip`** (**changelog 60**). **WO** **Operational status** flyout + **QuickBooks — posting header** include **`erp-help-tip`** (**changelog 61**). **Maintenance** top **Workspace snapshot** KPI strip includes **`erp-help-tip`** (**changelog 62**). **Tracking** top **Fleet mix snapshot** row (above the four-metric strip) includes **`erp-help-tip`** (**changelog 63**). **Accounting → Maintenance expenses** uses split **category / item** line tables consistent with WO cost breakdown (**changelog 65**). **Dashboard-style list** **`.status-*`** chips in **`app-theme.css`** prefer **`--pill-*`** with legacy **`--*-soft` / `--accent`** fallbacks (**changelog 114**); **`.chip` / `.list`** use **`--color-border`** and **active** states use **`--pill-blue-*`** (**changelog 115**); **`.unit-box` / `.table-wrap` / settlement wrap** share the same border stack (**changelog 116**); **subtabs / shop / WO / catalog / aside / cost / fuel import** tables match (**changelog 117**); **WO modal / cost stack / save bar** match (**changelog 118**). **Navy frame** (**`.qb-doc-shell`**, **`.panel-head`**, **WO** **`.maint-*`**, **maintenance** **unit+cost** panels) + **active** **shop**/**repair** fills read **`--color-app-frame-border`** / **`--color-nav-bg`** (**changelog 119**). **Topbar** / **sidebar expand** / **default buttons** / **basic inputs** match **`--color-border`** (**changelog 122**). Spec’s “remove views/QBO panels” list may not be 100% applied — needs visual QA against spec.

### Rule 13 — Accounting board

- **Partial:** KPI-style cards and QBO chrome exist in maintenance accounting sections; **`.qb-doc-shell`** outer border uses **`--color-app-frame-border`** when tokens load (**changelog 119**); **`#acctBoardStrip`** adds **Accounting board** title row **`erp-help-tip`** (**changelog 62**); accounting **dash card** subcopy uses **`var(--color-text-body)`** / link color **`var(--color-border-focus)`** where updated. **`#authBanner`** (401 “login required”) uses class **`erp-auth-banner`** with **token-backed** warning strip styling in **`erp-master-spec-2026.css`** on **maintenance** and **banking** (replaces banking’s prior inline red strip). **QuickBooks Live Master** uses title **`erp-help-tip`** and shorter vendor intro (**changelog 47**); **Vendors** / **Items** / **Accounts** list column titles add **`erp-help-tip`** (**changelog 60**); **Saved Maintenance Expense Transactions**, **Recent bill payments**, and **Expense history log** panel titles add **`erp-help-tip`** (**changelog 60**); **QuickBooks rollback** **ERP import batches** + **Fuel / Relay import batches** sub-head **`erp-help-tip`** (**changelog 61**); **AP** / **expense history** QBO strips use **token** borders/backgrounds. **Maintenance expenses** document lines: **Category** and **Item** grids + aligned save/transfer/reset paths (**changelog 65**). “Remove dark panel / instruction paragraphs” — partially addressed over time; confirm with UX pass.

### Rule 14 — Upload center

- **Partial:** Tabs and import flows exist; **Upload center** main panel title + **`erp-help-tip`** on one row (**changelog 61**); **?** + one-line intro on **every** upload subpanel (**Bank**, **Comdata**, **Fuel**, **AP**, **Maint history**, **Other**, **Connections**). Sub-tab buttons are ordered **A–Z** by label (**Bank CSV** … **Other**). **Connections** status cards + pills use **`var(--color-*|pill-*-*)`** (**changelog 81**). “Every tab fully functional” should be verified with manual tests and real files.

### Rule 15 — Samsara-style filter bar

- **Partial:** Filter patterns exist on several tables (see spec comments in `erp-master-spec-2026.css`). **Maintenance → Security alerts** uses **`mr-filter-bar`** + **`mr-filter-bar__grow`** / **`__right`** for search, window, severity, and **Refresh alerts**. **Lists & catalogs → Service types (DB)** adds **`mr-filter-bar`** + live name filter for the Postgres catalog admin table. **Reports → Maintenance detailed report** wraps unit/date/type/search controls in **`mr-filter-bar`** + **`mr-filter-bar__grow`**. **Accounting → Fuel expenses** ledger filters + **manual fuel / DEF** header grid, **Expense history** date/source/search row + preset buttons (**`mr-filter-bar`** + **`__grow`** / **`__right`**), **Reports → Settlement** trip **Load / invoice #** + **Run lookup** / **Download CSV** row, **Pay bills** vendor/payment composer + **Recent bill payments** log filters, **`fuel.html`** **Diesel purchase**, **`banking.html`** snapshot + suggestions rows, and **`settings.html`** **Employees** search + **Refresh** use the same **`mr-filter-bar`** shell (**changelogs 48–54**; **`body.fuel-board`**, **`body.banking-page`**, **`body.settings-page`** in **`erp-master-spec-2026.css`**). **`mr-filter-bar .btn` / `button`** **`:focus-visible`** uses **`var(--color-border-focus)`** on **maintenance** (**changelog 81**) and on **`fuel` / `banking` / `settings`** filter rows (**changelog 82**). Not every table has full chip + pager parity.

### Rule 16 — Safety / HOS

- **Partial:** Tables and pills exist in maintenance for safety/tracking; **Active / in service** tab **Active drivers** + **In service now** column titles include **`erp-help-tip`** (**changelog 61**); column sets vs spec — verify.

### Rule 17 — Reports

- **Partial:** Reports hub shell in maintenance; **Reports** toolbar title includes **`erp-help-tip`** (sidebar vs hub tiles vs search — **changelog 62**); **Executive overview** panel title includes **`erp-help-tip`** (Refresh vs **Sync QuickBooks lists + activity**); **TMS**, **QBO**, **sync**, **IFTA**, **Maintenance spend by unit**, **Team activity & security**, **Maintenance detailed report** tab titles include **`erp-help-tip`** (IFTA / maint detailed body copy shortened + tip). **`loadTeamSecurityPanel`** renders **Configuration posture**, **ERP user activity**, **Security audit log**, and **CMMS benchmark** panels with title-row **`erp-help-tip`** (**changelog 60**). **Settlement** **Trip cost rollup** + **Loads with recorded costs (index)** panel titles include **`erp-help-tip`** (**changelog 60**). **Settlement** trip cost rollup uses **`mr-filter-bar`** for load # + lookup actions (**changelog 51**). **Settlement** trip TMS context box uses **`var(--color-border|bg-card)`**; **Driver pay settlements** panel title **`erp-help-tip`** + compact **`mini-note`** lines. **Executive overview** generated/cache line and **TMS** summary use muted token text; **TMS** not-configured / error and **Spend by unit** empty states use two-line headline + secondary (**Rule 22**). **Spend by unit** rollup table and **Maintenance detailed** cards use **Rule 23** pagers after **Refresh reports**. “All reports on one page / remove links from other sidebars” — **not fully enforced** in this pass.

### Rule 18 — QBO GET endpoints

- **Skipped (different shape):** Catalog data is served via existing routes such as **`GET /api/qbo/catalog`**, **`GET /api/qbo/master`**, **`GET /api/qbo/status`** and cached in `erp.qboCache` — not the exact `/api/qbo/accounts` … list from the spec. Adding parallel read-only GETs would be **new API surface** (allowed only if product wants duplication); **not added** here to avoid confusion with existing clients.

### Rule 19 — Toasts

- **Done (this pass):** Global **`.erp-toast-host` / `.erp-toast*`** styles in `erp-master-spec-2026.css`. **`window.showToast(message, type)`** and **`window.erpNotify(message, type?)`** in `erp-ui.js` (`success` | `error` | `warning` | `info`; `erpNotify` infers type when omitted and replaces legacy **`alert()`** across maintenance, fuel, banking, settings, dispatch). Host div on banking, settings, dispatch, fuel, maintenance, and company **`index.html`** (hub loads **`erp-ui.js`** deferred for future use).

### Rule 20 — Button loading

- **Done (pattern):** **FIX 10** — `erpWithBusy`, `erp-btn--busy`, spinners in `erp-ui.js` + CSS. Applied on multiple async actions (maintenance save split, banking, settings, fuel, **dispatch** refresh / catalog / save / uploads / auto miles / QBO doc sync from modal, **row** Create invoice & Sync attachments, quick-add truck/trailer; status `<select>` uses `disabled` + `aria-busy` while patching).

### Rule 21 — QBO error banner

- **Partial:** QBO alert / sync messaging exists in maintenance (**`#qboSyncAlertBar`**). Banner is **`position: sticky`** at the top of the scrollable main column so users keep context while scrolling; tier styling uses **design-token fallbacks** in **`app-theme.css`**. Accounting nav badge behavior — compare to spec.

### Rule 22 — Instruction cleanup → “?” tips

- **Done (pattern):** Global **`.erp-help-tip*`** styles; **`erpHelpTipToggle`** + click/Escape close in **`erp-ui.js`**. Banking includes a sample tip + **step 2** CSV **`erp-help-tip`** (**changelog 57**). Dispatch board uses compact copy + tips. **Maintenance:** **accident** WO **Cost breakdown** + **tire** WO “same invoice / multiple positions” + **shop → Parts** queue intro each use a short line + **`erp-help-tip`** panel; **Home** **System Summary** + **Sync Summary** + **Maintenance → Units** panel titles + **?** (**changelog 58**); **Maintenance Table** + **Unit History** + **Tracking → Map** (**Units** + **Live map**) titles + **?** (**changelog 59**); **Imports & uploads** + **Shop** exports / integrity + **Tracking** idle / summary + **Accounting** saved AP / bill payments / expense history + **Settlement** rollup / index + **Live Master** three-column titles + **?** (**changelog 60**); **`loadTeamSecurityPanel`** posture / roster / audit / CMMS panel titles + **?** (**changelog 60**); **Upload center** main title row + **?** (**changelog 61**); **WO** **Operational status** + **QuickBooks — posting header** + **?** (**changelog 61**); **Accounting → QBO rollback** batch sub-head + **?** (**changelog 61**); **Safety → Active / in service** column titles + **?** (**changelog 61**); **Maintenance** **Workspace snapshot** KPI strip + **?** (**changelog 62**); **Tracking** **Fleet mix snapshot** row + **?** (**changelog 63**); **reports** hub toolbar + **Accounting board** strip + **?** (**changelog 62**); **reports → Executive overview** panel title + **?** (Refresh vs QBO sync); **reports → Settlement / P&L** intro + trip rollup panel use the same pattern; **reports → Settlement → Driver pay settlements** title + **?**; **Accounting → QuickBooks rollback & import safety** title + **?**; **Fleet → Vehicles by shop / location context** title + **?**; **reports** tabs **TMS**, **QBO**, **sync**, **IFTA**, **maintenance spend**, **detailed**, **team** + **Lists & catalogs** (panel title + **all five** sub-tab intros) + **?**; upload subpanel intros (prior passes). **`fuel.html`** **Selected Unit** panel title + **?** explains satellite diesel ledger vs **Maintenance → Fuel expenses** (**changelog 53**). **`settings.html`** **Employees** + **Users** titles + **?** (**changelogs 54, 57**). **`index.html`** company hub header + **?** (**changelog 55**); **footer** mileage / OSRM / PCMiler + **?** (**changelog 67**); **Maintenance → work order** **Field help** drawer: service one-liner + **`erp-help-tip`** for QBO cost lines / invoice lock (**changelog 85**); **Maintenance topbar** compact lead + **`erp-help-tip`** for workspace navigation (**changelog 86**). Many other long **`mini-note`** blocks remain for future passes.

### Rule 23 — Pagination

- **Partial:** **`erpPagerRender`** / **`erpPagerSliceRange`** for tables that were wired (**banking** suggestions, **settings** employees, **maintenance shop board** internal / external / roadside / parts queue tables with per-tab page state; **maintenance accounting → Fuel expense** grid with **`fuelExpensePager`**; **maintenance accounting → Expense history** with **`expHistPager`**; **maintenance accounting → Saved Maintenance Expense Transactions** card list (**`apTxnListPager`**); **accounting → Pay bills → Recent bill payments** log (**`bpLogPager`**, **`__bpBillPaymentLogAllRows`** + client filter); **reports → Settlement / P&L** load index + per-load line items (**`settlementIndexPager`**, **`settlementLinesPager`**); **reports → Maintenance spend by unit** rollup table (**`repMaintByUnitPager`**, **`#repMaintByUnitPagerHost`**); **reports → Maintenance detailed report** filtered record cards (**`repMaintDetailPager`**, **`#repMaintDetailPagerHost`**); **Lists & catalogs → Service types (DB)** (**`serviceCatalogAdminPager`**, **`#serviceCatalogAdminPagerHost`**, client-side name filter); **maintenance → Maintenance Table** (**`maintDashboardTablePager`**); **maintenance → Safety / HOS** tab: **HOS clocks** (**`safetyHosPager`**), **Active** / **In service** panels (**`safetyActivePager`**, **`safetyInServicePager`**), **Assignments** (**`safetyAssignPager`**); **tracking → Map → Units** cards (**`trackingListPager`**); **tracking → All tracked assets** + **Drivers (HOS)** tables (**`trackingAssetsTablePager`**, **`trackingDriversPager`**); **tracking → Idle snapshot** vehicles table (**`idleSnapshotPager`**, rows cached on **`idleSnapshotPager._rows`** for page changes without refetch). Not literally every table >10 rows.

### Rule 24 — Connection verification on load

- **Partial:** Maintenance loads QBO status / sync alerts in the **sidebar** and embed bars, and hydrates the same **read-only QuickBooks one-liner** as satellites via **`#erpConnectionStrip`** + **`erpMountConnectionStrip('erpConnectionStrip')`** on boot (**changelog 65**). **Dispatch** loads the same strip above the board topbar (**changelog 66**); strip is **hidden** when **`?embed=1`** so the embed status bar is the only QBO line. **Company hub** neutral strip uses **`--color-hub-*`** until QBO tier classes apply (**changelog 69**). **`erpMountConnectionStrip(hostId)`** in **`erp-ui.js`** + **`.erp-connection-strip*`** in **`erp-master-spec-2026.css`** also run on **banking**, **settings**, **fuel**, and **company hub** (`index.html`) after load. **`.erp-connection-strip--ok` / `--warn`** tier borders use **`--color-success-border-soft` / `--color-warning-border-soft`** (**changelog 89**). Universal yellow **Samsara + QBO** banner on every surface — still **not** fully unified with maintenance’s richer sidebar text.

---

## Changes made in the pass that produced this document

1. **`public/css/design-tokens.css`** — Rule 0 token file (public path; spec’s `/src/styles/` noted as inapplicable); semantic status line colors (**`--color-semantic-*`**); bulk **`maintenance.html`** inline **`var(--*)`** for errors, links, QBO banners, import failures (see changelog **29**).
2. **Linked `design-tokens.css`** in `maintenance.html`, `dispatch.html`, `fuel.html`, `banking.html`, `settings.html`.
3. **`public/css/erp-master-spec-2026.css`** — Global Rule 19 toast styles.
4. **`public/js/erp-ui.js`** — `window.showToast(message, type)`.
5. **`#erpToastHost`** on banking, settings, dispatch, fuel (maintenance already had it).
6. **`showErpToast`** in maintenance delegates to **`window.showToast`** when present (single toast implementation).
7. **Banking / settings / fuel** async flows call **`showToast`** on success and error (and banking uses **warning/info** where appropriate).
8. **`dispatch.html`** — Rule 22 compact intro + help tips; token-backed main column tweaks in **`erp-master-spec-2026.css`**; **`erpWithBusy`** on refresh, QBO catalog sync (toolbar), save load, doc upload, rate-con PDF extract, auto miles, docs-modal QBO sync, **board row** QBO buttons (invoice + attachments), and **quick-add** truck/trailer; status changes disable the row `<select>` while saving; **`showToast`** on those outcomes plus QBO invoice / attachment sync, **`patchStatus`**, and **openEditLoad** load failures; **`showMsg`** escapes text; manual refresh passes **`loadTab(true)`** so failures surface as toasts while the timer refresh stays quiet.
9. **`erp-ui.js` — `erpNotify(message, type?)`:** toast-first replacement for legacy **`alert()`** across **`maintenance.html`**, **`fuel.html`**, **`banking.html`**, **`settings.html`**, **`dispatch.html`** (with heuristic type when `type` omitted). **`index.html`** — shared **`design-tokens`**, **`erp-master-spec-2026`** (toasts), **`#erpToastHost`**, **`erp-ui.js`** for consistency if hub gains scripts later.
10. **Rule 24 + Rule 0 (continuation):** **`erpMountConnectionStrip`** + **`#erpConnectionStrip`** on **banking**, **settings**, **fuel**, **index**; token-backed **`--color-bg-page`** body background on banking/settings/fuel. **Rule 22:** maintenance **accident** WO inline help → **`erp-help-tip`**.
11. **Rule 22 (tire WO):** **Tire** record first-tire / same-invoice copy → compact line + **`erp-help-tip`** panel in **`maintenance.html`**.
12. **Documentation:** **[`ERP_MASTER_REDESIGN_FINAL_REPORT.md`](./ERP_MASTER_REDESIGN_FINAL_REPORT.md)** — consolidated changelog, rule matrix, recommendations, verification, risks.
13. **Rule 23 + 22 (shop board):** **`maintenance.html`** — shop queue tables (**internal**, **external**, **roadside**, **parts**) use **`erpPagerRender`** with **`shopQueuePager`** state (default 15 rows/page; filters reset page). **Parts** tab intro uses compact copy + **`erp-help-tip`**.
14. **Rule 23 (fuel expense grid):** **`maintenance.html`** — **`#fuelExpPagerHost`** + **`fuelExpensePager`** slice the fuel expense table; **`postFuelExpenseToQbo`** reads ERP row + draft when the row is not on the current page so **Record filtered to QuickBooks** still posts the full filtered set.
15. **Rule 23 (expense history):** **`maintenance.html`** — **`#expHistPagerHost`** + **`expHistPager`** paginate the combined expense history table; **Export filtered CSV** still uses the full filtered row set.
16. **Rule 23 (settlement report):** **`maintenance.html`** — **`#settlementIndexPagerHost`** / **`settlementIndexPager`** for **Loads with recorded costs**; **`#settlementLinesPagerHost`** / **`settlementLinesPager`** for trip lookup line items (new load # resets page). **Download CSV** still exports the full load from the API.
17. **Rule 23 (saved WO + AP cards):** **`maintenance.html`** — **`#apListPagerHost`** + **`apTxnListPager`** paginate the combined work-order and AP **record-card** list (default 10 cards/page); CSV exports still use server / full **`erp`** datasets.
18. **Rule 22 (settlement report copy):** **`maintenance.html`** — **Reports → Settlement / P&L** page intro and trip rollup panel: long paragraphs replaced with short **`mini-note`** lines plus **`erp-help-tip`** panels.
19. **Rule 23 (maintenance fleet table):** **`maintenance.html`** — **`#maintTablePagerHost`** + **`maintDashboardTablePager`** paginate the **Maintenance Table** (filter key = fleet category + service filter string).
20. **Rule 23 (tracking unit cards):** **`maintenance.html`** — **`#trackingListPagerHost`** + **`trackingListPager`** paginate **Tracking → Map → Units** cards (filter key = search text).
21. **Rule 23 (tracking assets + drivers + bill pay log):** **`maintenance.html`** — **`#trackingAssetsPagerHost`** / **`#trackingDriversPagerHost`**; **Pay bills** payment log uses **`window.__bpBillPaymentLogAllRows`** so clearing the log search shows all loaded payments again, with **`#bpLogPagerHost`** + **`bpLogPager`** slicing the filtered list.
22. **Rule 23 (safety + idle snapshot):** **`maintenance.html`** — **Safety / HOS** tab: **`#safetyHosPagerHost`** + **`safetyHosPager`**; **`#safetyActivePagerHost`** / **`#safetyInServicePagerHost`** + **`safetyActivePager`** / **`safetyInServicePager`** (duty filter resets page); **`#safetyAssignPagerHost`** + **`safetyAssignPager`** (assign filter + search reset page). **Tracking → Idle:** **`#idleSnapshotPagerHost`** + **`idleSnapshotPager`** with **`renderIdleSnapshotTableBody`** paging **`idleSnapshotPager._rows`** after **`loadIdleSnapshot`** succeeds.
23. **Rule 22 (upload Connections):** **`maintenance.html`** — **Upload center → Connections**: title row **`erp-help-tip`** + shortened **`mr-upload-panel-desc`** so long guidance sits behind **?**.
24. **Rule 22 + 0 (upload subpanels):** **`maintenance.html`** — **Bank CSV**, **Comdata / Relay**, **Fuel / DEF**, **Maintenance AP**, **Maintenance history**, **Other**: same title **`erp-help-tip`** + one-line **`mr-upload-panel-desc`**; existing **`<details>`** help kept. **Connections** Samsara meta line uses **`var(--color-text-label, …)`**.
25. **Rules 22 + 15 + 17 + 0:** **`maintenance.html`** — **Fleet → Vehicles by shop / location context**: long intro → title **`erp-help-tip`** + short line. **Security alerts**: **`mr-filter-bar`** around search / window / severity + **Refresh**. **Reports → TMS load pipeline**: panel title **`erp-help-tip`**. Accounting nav QBO summary box: **`var(--color-text-body|label)`** instead of hard-coded grays.
26. **Rules 17 + 22 + 0 + 13:** **`maintenance.html`** — **Reports → QuickBooks** + **ERP vs QBO sync** + **IFTA** panel titles **`erp-help-tip`** (IFTA: three paragraphs → one line + tip). **Lists & catalogs** title **`erp-help-tip`**. Accounting board dash card copy + **View all errors** link: **`var(--color-text-body)`** / **`var(--color-border-focus)`**. Maintenance **edit record hint** + **QBO issues** aside: token text colors.
27. **Rules 17 + 22 + 0:** **`maintenance.html`** — **Reports → Team & security**, **Maintenance spend by unit**, **Maintenance detailed report**: panel title **`erp-help-tip`**; maint detailed intro → one line + **?**. **Connections** integration strip **QuickBooks: not configured** uses **`var(--color-text-label)`**; shop queue **Remove** uses same token.
28. **Rules 0 + 22:** **`design-tokens.css`** — **`--color-semantic-success|warning|warn-accent|error`**. **`maintenance.html`** — **`paintMaintConnectionStrip`** uses semantic tokens; **`loadTeamSecurityPanel`** intro → short line + **`erp-help-tip`**; team/audit errors + idle alert column colors use **`var(--color-semantic-*)`**.
29. **Rule 0 + docs:** **`maintenance.html`** — bulk **`var(--color-semantic-error|…)`**, **`var(--color-border-focus)`**, **`var(--pill-red-text|green-text)`** on inline error/link/import/QBO-dot/banner strings; **`paintQboStatusBanner`** / **`paintApTxnQboBanner`** tier text uses tokens. **`ERP_MASTER_REDESIGN_STATUS.md`** — “protection block” reframed as **implementation guardrails** (auth stays); “human review” → optional product notes.
30. **Rules 0 + 22:** **`fuel.html`** — **Home** + maintenance fuel link use **`var(--color-border-focus)`**. **`maintenance.html`** — **Lists & catalogs → QuickBooks items & accounts**: compact line + **`erp-help-tip`**.
31. **Rules 22 + 0:** **`maintenance.html`** — **Lists & catalogs** sub-tabs **Service types**, **Vendors & drivers**, **Operational status**, **Fleet & Samsara**: same short line + **`erp-help-tip`** pattern as QBO tab; **Fleet** Samsara write card top border uses **`var(--color-border)`**.
32. **Rule 0:** **`dispatch.html`** — load document list **`border-bottom`** uses **`var(--color-border)`**; **QBO posting alert** brief text colors use **`var(--color-semantic-error|warn-accent)`**; board intro **`mini-note`** uses **`var(--color-text-label)`**.
33. **Rule 0:** **`banking.html`** — metrics + table header + **`#authBanner`** + suggest-card inline styles use **`var(--color-bg-header|border|card|pill-red|btn-danger-*)`**. **`settings.html`** — **`input:focus`** + **`.err`** use **`var(--color-border-focus|focus-ring|semantic-error)`**. **`dispatch.html`** — modal/stop/embed/chrome backgrounds and miles table borders use **`var(--color-bg-header|bg-hover|bg-card|color-border)`**.
34. **Rules 23 + 22 + hub polish:** **`maintenance.html`** — **Reports → Maintenance detailed report**: **`#repMaintDetailPagerHost`** + **`repMaintDetailPager`** paginate filtered record cards (default 10/page; filters reset page). **`index.html`** — **Safety** / **Tracking** hub card tags use **`.tag-safety`** / **`.tag-tracking`** (no inline colors). **`dispatch.html`** — sidebar **Tips**: one-line summary + **`erp-help-tip`** behind **?** (**`erpHelpTipToggle`**).
35. **Rule 23:** **`maintenance.html`** — **Reports → Maintenance spend by unit**: **`#repMaintByUnitPagerHost`** + **`repMaintByUnitPager`** + **`renderRepMaintSpendByUnitTable`** paginate the rollup table after **Refresh reports** (default 15/page; full refresh resets page). **CSV export** unchanged (server route).
36. **Rules 0 + 12 + 15 + 23 (lists, not reports):** **`public/css/board-nav.css`** — company hub strip dropdown borders/shadow/hover use **`var(--color-border|shadow-dropdown|pill-blue-*|color-bg-card)`** with legacy fallbacks. **`maintenance.html`** — **Lists & catalogs → Service types (DB)**: **`mr-filter-bar`** + **`#serviceCatalogAdminSearch`**, **`#serviceCatalogAdminPagerHost`** + **`serviceCatalogAdminPager`**, **`renderServiceCatalogAdmin`** (filter slices rows then **`erpPagerRender`**); **`toggleCatalogActiveFromBtn`** calls **`loadServiceCatalogAdminOnly`** after **`loadAll`** so the grid refreshes.
37. **Discoverability (Rule 2 / shell):** **`maintenance.html`** — new **Lists** (**▤**) icon in **`#erpIconNav`** (between **Accounting** and **Reports**) opens **`#catalog`**; duplicate hidden ghost **`catalog`** nav control removed. Sidebar block title **`Lists, imports & admin`** → **`Lists & catalogs`** plus one-line pointer to the **Lists** icon. **`dispatch.html`** — same **Lists** link to **`/maintenance.html#catalog`** for parity.
38. **Naming + A–Z sub-tabs (Rule 2 / polish):** **`maintenance.html`** — remaining copy and **`erpSbGroup`** titles use **Lists & catalogs** (replacing **Lists & admin** / **Catalogs →** phrasing where user-facing); catalog sidebar browse group titled **Sub-tabs** with links ordered A–Z. **Safety**, **Tracking**, **Shop board**, **Lists & catalogs** main subtabs, **Reports** top row (**Overview** first, then **Accounting** / **Maintenance** / **Operations**), dropdown items (**Accounting**, **Maintenance**, **Operations**), and hidden **accounting** tab stubs reordered **alphabetically** while preserving default **active** tabs (HOS, map, internal shop, service types, overview, maintenance expenses, fuel upload).
39. **Lists deep links + hub strip (Rule 2):** **`maintenance.html`** — **`applyNavHash`** handles **`#catalog`**, **`#catalog-service`**, **`#catalog-qbo`**, **`#catalog-vendors`**, **`#catalog-operational`**, **`#catalog-fleet`** so the matching **Lists & catalogs** sub-tab opens (not only the section). **`board-nav.js`** — new **Lists & catalogs** workspace item (dropdown links A–Z + **Upload center**) for **`index`**, **`dispatch`**, **`fuel`**, **`banking`**, and **`maintenance`** strips. **`index.html`** — hub card + strip copy mention **Lists & catalogs**.
40. **Settings workspace strip (Rule 2):** **`settings.html`** — link **`board-nav.css`**, add **`#boardNavMount`** + **`board-nav.js`** (defer) so **Settings & users** matches banking/fuel/dispatch hub navigation to ERP workspaces (**Lists & catalogs**, reports, etc.).
41. **+ New menu A–Z (Rule 4):** **`maintenance.html`** — **`#erpNewMenu`** column items reordered **alphabetically** by label within **Expenses & bills**, **Maintenance**, and **Other** (handlers unchanged).
42. **Reports detailed filters + QBO banner (Rules 15 / 21 / 0):** **`maintenance.html`** — **Reports → Maintenance detailed report**: filter row wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** (same pattern as other filter strips). **`app-theme.css`** — **`.qbo-sync-alert-bar`** **`sticky`** top chrome + **`var(--pill-*|btn-danger-border|color-border|color-text-primary)`** fallbacks for default / critical / degraded tiers.
43. **Reports overview tip + auth banner tokens (Rules 17 / 22 / 0 / 13):** **`maintenance.html`** — **Reports → Executive overview**: **`panel-title`** row adds **`erp-help-tip`** (Refresh vs QBO sync); body keeps a short **`mini-note`** before **`loadReportsData`** replaces it. **`#authBanner`**: class **`erp-auth-banner`**, remove inline colors. **`erp-master-spec-2026.css`** — **`body.erp-maintenance #authBanner.erp-auth-banner`** uses **`var(--pill-yellow-bg|color-border|color-semantic-warning)`** fallbacks.
44. **Reports QBO / sync / IFTA body copy (Rules 22 + 17 + 14):** **`maintenance.html`** — **`renderReportsAll`**: **QuickBooks activity** empty state split into headline + muted steps; loaded-state window line shortened + token color; **ERP ↔ QBO sync** intro/footer **`mini-note`**s shortened and cross-reference panel **`?`**; **IFTA** roadmap body reduced to one line pointing at title **`?`**. Rule **14** notes: upload sub-tab row **A–Z** by label (verified in layout).
45. **Reports overview + TMS + spend-by-unit empty (Rules 0 + 22 + 17 + 1):** **`maintenance.html`** — **`renderReportsAll`**: **Executive overview** generated/cache line uses **`var(--color-text-label)`**; **TMS loads** not-configured / error / OK summary lines use headline + token-muted or **`var(--color-semantic-error)`** on the error text. **`renderRepMaintSpendByUnitTable`**: empty rollup uses two-line pattern (headline + **Refresh reports** hint). **`erp-master-spec-2026.css`** — at **≤900px**, **Reports** toolbar padding and **`erp-reports-main`** gap tighten slightly (**Rule 1**).
46. **Accounting rollback + settlement driver pay + banking auth strip (Rules 22 + 0 + 12 + 17 + 13):** **`maintenance.html`** — **Accounting → QuickBooks rollback & import safety**: panel title **`erp-help-tip`**; long intro paragraph → two short lines + token-muted secondary; sub-head borders use **`var(--color-border)`**. **Reports → Settlement / P&L**: **`#settlementTmsBox`** border/background **`var(--color-border|bg-card)`**; **Driver pay settlements** title **`erp-help-tip`** + shortened body. **`banking.html`** — **`#authBanner`** adds **`erp-auth-banner`**, removes inline red banner CSS. **`erp-master-spec-2026.css`** — same **`erp-auth-banner`** token strip for **`body.banking-page`** (warning palette; **`margin-bottom: 16px`** on banking).
47. **Accounting QBO Live Master + token chrome (Rules 22 + 0 + 13):** **`maintenance.html`** — **Accounting → QuickBooks Live Master**: panel title **`erp-help-tip`** (vendors vs items vs accounts vs payroll); vendor form long **`mini-note`** → short line + **`var(--color-text-label)`** second line; section dividers **`var(--color-border)`**. **Fuel & DEF entry** Relay import panel: **`var(--color-border|bg-card)`**. **Expense history** QBO verify strip + **AP expense** doc QBO banner: token border/background/text. **Rollback** bulk-revert checkbox scroller + **Maintenance → unit** fleet roster block: **`var(--color-border)`**.
48. **Accounting connections strip + fuel ledger filter bar (Rules 0 + 15):** **`maintenance.html`** — **`#maintIntegrationStrip`**: border/background/text use **`var(--color-border|bg-card|text-body)`** with legacy fallbacks; **`#maintPostingPreflight`** default color **`var(--color-semantic-warning)`**. **Accounting → Fuel expenses**: ledger **`wo-line-grid`** wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`**. **Lists & catalogs** panel subtitle uses **`var(--color-text-label)`**.
49. **Pay bills filter chrome + post-release QA doc (Rule 15 + docs):** **`maintenance.html`** — **Accounting → Pay bills**: payment composer **`wo-line-grid`** (vendor, pay-from, bank, date, check #, memo) and **Recent bill payments** log **`wo-line-grid`** each wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** with **`aria-label`**. **`docs/ERP_MASTER_REDESIGN_POST_RELEASE_CHECKLIST.md`** — expanded suggested check list for release / “redesign complete” sign-off (environment, auth, maintenance hotspots, satellites, deferred follow-ups); linked from **`ERP_MASTER_REDESIGN_STATUS.md`** header and **`ERP_MASTER_REDESIGN_FINAL_REPORT.md`** §7.
50. **Expense history filter bar (Rule 15):** **`maintenance.html`** — **Accounting → Expense history log**: date/source/search **`wo-line-grid`** plus **Last 7/30/90 days** / **All dates** buttons wrapped in **`mr-filter-bar`** with **`mr-filter-bar__grow`** + **`mr-filter-bar__right`**.
51. **Settlement trip lookup filter bar (Rules 15 + 17):** **`maintenance.html`** — **Reports → Settlement / P&L → Trip cost rollup**: load # + **Run lookup** / **Download CSV** **`wo-line-grid`** wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** with **`aria-label="Settlement trip lookup"`**.
52. **Manual fuel header filter bar (Rule 15):** **`maintenance.html`** — **Accounting → Fuel expenses → Manual fuel / DEF expense** document header **`wo-line-grid`** (vendor, expense #, date, unit, class, payment, bank, location) wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** with **`aria-label="Manual fuel expense header"`**.
53. **Fuel satellite tokens + filter bar + tip (Rules 0 + 15 + 22):** **`fuel.html`** — page `<style>` uses design-token fallbacks for panels, fleet rows, badges, roadmap note, write-secret banner, relay info callout, autocomplete list; topbar workspace links use **`pill-green-text`** / **`pill-purple-text`**; **Selected Unit** **`h2`** adds **`erp-help-tip`** (satellite ledger vs **Maintenance → Fuel expenses**); **Diesel purchase** inputs + **Save purchase** wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** / **`__actions`** with **`aria-label="Diesel purchase entry"`**. **`erp-master-spec-2026.css`** — **`mr-filter-bar`** / **`__grow`** / **`__actions`** + shared bottom border apply to **`body.fuel-board`** as well as **`body.erp-maintenance`**; filter bar border uses **`var(--color-border)`**.
54. **Banking + settings satellites (Rules 0 + 15 + 22):** **`banking.html`** — **.ph** gradient and default **`button`** backgrounds use **`var(--color-bg-header|bg-card)`**; **step 1** (days of history + **Load snapshot**) and **step 3** (import batch id + **Run suggestions**) wrapped in **`mr-filter-bar`** + **`mr-filter-bar__grow`** / **`__actions`** with **`aria-label`**. **`settings.html`** — **Employees** **`h2`** adds **`erp-help-tip`** (internal directory vs **Lists & catalogs** QBO vendors); **Search** + **Refresh** wrapped in **`mr-filter-bar`** (**`aria-label="Employee directory filter"`**); scoped input margin + primary **`color: var(--color-bg-card)`**. **`erp-master-spec-2026.css`** — **`mr-filter-bar`** shell extended to **`body.banking-page`** and **`body.settings-page`** (with fuel).
55. **Company hub polish (Rules 0 + 2 + 22):** **`index.html`** — radial page glow uses **`var(--color-nav-bg)`**; **`font-family`** prefers **`var(--font-spec-sans)`**; card hover shadow **`var(--shadow-dropdown)`**; workspace **`card-tag`** colors use **`pill-*`** / **`btn-info-*`** fallbacks; **Bank** card uses **`tag-banking`** (not **`tag-reports`**); footer **`strong`** uses **`var(--color-text-placeholder)`**; **header** adds **`erp-help-tip`** (cards, workspace strip, QBO connection line).
56. **Dispatch board token pass (Rule 0):** **`dispatch.html`** — **`maint-banner`** critical / high / medium use **`pill-*`**, **`btn-danger-*`**, **`color-semantic-*`** fallbacks; **Delivered** status chip **`--dispatch-delivered*`** maps to **`pill-purple-*`**; **stop-block** / **stop-miles-summary** backgrounds **`var(--color-bg-header|bg-card)`**; **`.btn-primary`** label **`var(--color-bg-card)`**; **`.ac-list`** / **`.ac-item`** use **`var(--color-bg-card|border|shadow-dropdown)`**; **embed** status bar separator **`var(--color-text-placeholder)`**.
57. **Settings Users + banking CSV tips (Rule 22):** **`settings.html`** — **Users** **`h2`** adds **`erp-help-tip`** (ERP sign-in / JWT vs **QuickBooks** users; points to **Employees** tab). **`banking.html`** — **step 2 · Import bank CSV**: expected-columns line + **`erp-help-tip`** for **import batch id** → **step 3** and **YYYY-MM-DD** date parsing.
58. **Maintenance home + Units tips (Rules 22 + 0 + 12):** **`maintenance.html`** — **Home** dashboard **System Summary** + **Sync Summary** panel titles **`erp-help-tip`** (what fills cards vs QBO snapshot / **Accounting** + **Reports** cross-links). **Maintenance → Units** list column title **`erp-help-tip`** (selection vs **Maintenance Table** filters + collapse **▼**). **`#retiredAssetsWrap`** top border uses **`var(--color-border, #e5e9f0)`**.
59. **Fleet table + map panel tips (Rules 22 + 12):** **`maintenance.html`** — **Maintenance Table** + **Unit History** panel titles **`erp-help-tip`** (grid vs **Units** selection, filters, pager; history vs **New work order**). **Tracking → Map**: **Units** + **Live map (Samsara GPS)** titles **`erp-help-tip`** (map list separate from **Maintenance → Units**; Samsara GPS + detail box).
60. **Shop, tracking, accounting, settlement, team tips (Rules 22 + 12 + 13 + 17 + 0):** **`maintenance.html`** — **Imports & uploads** title **`erp-help-tip`** (body duplicate **`?`** removed). **Shop**: **Maintenance reports** + **Integrity · fuel · parts alerts** title **`erp-help-tip`**. **Tracking**: **Yard & idle (T120–T178)** + **Summary** title **`erp-help-tip`**. **Accounting**: **Saved Maintenance Expense Transactions** + **Recent bill payments (this app)** + **Expense history log** title **`erp-help-tip`**; **QuickBooks Live Master** **Vendors** / **Items** / **Accounts** column title **`erp-help-tip`**. **Reports → Settlement**: **Trip cost rollup** + **Loads with recorded costs (index)** title **`erp-help-tip`**. **`loadTeamSecurityPanel`**: **Configuration posture**, **ERP user activity**, **Security audit log**, **CMMS benchmark** panel title **`erp-help-tip`** + panel borders **`var(--color-border, var(--line))`**.
61. **Upload shell + WO + rollback + Safety columns (Rules 22 + 14 + 12 + 13 + 16 + 0):** **`maintenance.html`** — **Upload center** panel title + **`erp-help-tip`** on one flex row. **WO** **Operational status** flyout + **QuickBooks — posting header** **`erp-help-tip`**; posting block dashed **`border-top`** + **shop PDF** link **`border`** use **`var(--color-border, var(--line))`**. **Accounting → QBO rollback**: **ERP import batches** + **Fuel / Relay import batches** sub-head **`erp-help-tip`**. **Safety → Active / in service**: **Active drivers** + **In service now** column title **`erp-help-tip`**.
62. **Reports + accounting + maintenance KPI chrome (Rules 22 + 17 + 13 + 12 + 0):** **`maintenance.html`** — **Reports** hub toolbar **Reports** + **`erp-help-tip`** (sidebar vs tile hub vs search). **`#acctBoardStrip`** **Accounting board** title row + **`erp-help-tip`** (KPIs vs cards vs **Connections**). **Maintenance** top **Workspace snapshot** KPI strip + **`erp-help-tip`**. **Reports → Maintenance detailed** JS card / cost-line borders + **Lists → Vendors** paired cache scrollboxes use **`var(--color-border, var(--line))`**.
63. **Tracking KPI tip + pager token borders (Rules 22 + 12 + 0):** **`maintenance.html`** — **Vehicle tracking** section: **Fleet mix snapshot** label row + **`erp-help-tip`** above the four-metric strip (Samsara mix vs **Assets** / **Map** / **Shop**). **`erp-master-spec-2026.css`** — **`.erp-pager`**, **`.erp-pager__btn`**, **`.erp-pager__size-sel`** borders use **`var(--color-border, var(--line, …))`** (shared pagers on maintenance, banking, settings, fuel, dispatch).
64. **Dedicated accounting / fuel forms + driver bill pay (Rules 4 + 5 + 11 + 12 + 20 + 2):** **`maintenance.html`** — **`#erpDedicatedFormModal`** overlay teleports **`#erpApDocShellPanel`** (maintenance expense/bill, fuel bill, vendor/driver bill presets; **`apTxnType`** locked; titles + wrench/pump accents; **`saveApTransaction({ postToQbo })`** chains existing **`postWorkOrderToQbo`**); teleports **`#erpFuelManualDocShellPanel`** for **Fuel expense**; **+ New** and **Accounting** sidebar **Specialized** groups + hash routes **`#accounting-maintenance-expense-new`**, **`#accounting-fuel-expense-new`**, etc.; **`#accounting-driver-bill-pay`** filters open bills client-side to driver-linked QBO vendor ids (**`vendor-catalog`**); **`board-nav.js`** Accounting dropdown adds matching deep links.
65. **AP expense doc category/item tables + Rule 24 on ERP (Rules 12 + 13 + 24 + 0):** **`maintenance.html`** — **Maintenance expenses** document: **Category** and **Item** line sections (`#apWoCatHost` / `#apWoItemHost`, templates **`woLineCategoryTpl`** / **`woLineItemTpl`**); JS **`collectApFormWoLines`**, **`addWoCategoryLine` / `addWoItemLine`**, **`clearWoLines`**, **`resetWoForm`**, **`saveApTransaction`**, **`openAccountingTab`** bootstrap, maintenance→accounting transfer, PDF apply, and init aligned with the new DOM (no **`.wo-mode`**). **`maint-accounting-ui-2026.css`** — **`.maint-qb-td-hidden`**, **`.ap-wo-lines-root`**. **`erp-master-spec-2026.css`** — maintenance main-surface margin for **`.erp-connection-strip`**. **`erp-ui.js`** — strip mount documented for **maintenance**. **`#erpConnectionStrip`** in main column + **`erpMountConnectionStrip`** on load (**Rule 24** parity with banking/settings/fuel/hub).
66. **Rule 24 — dispatch board (`dispatch.html` + `erp-master-spec-2026.css` + `erp-ui.js`):** **`#erpConnectionStrip`** as first row inside **`.erp-main-body.dispatch-board-body`**, **`erpMountConnectionStrip`** from **`init()`** when not **`dispatch-embed`**; inline CSS hides strip in **embed** mode; scoped strip margins under **`body.dispatch-page #dispatchApp`**.
67. **Hub smoke + footer tip (Rules 2 + 22 + 24 + verification):** **`scripts/system-smoke.mjs`** — GET **`/index.html`** requires **`hub-page`**, **`erpConnectionStrip`**, **`/js/erp-ui.js`**; **`index.html`** — footer mileage block: short visible line + **`erp-help-tip`** (OSRM / PCMiler / **`PCMILER_API_KEY`** detail in panel); **`.hub-footer-*`** layout in page `<style>`.
68. **Rule 0 — dark hub tokens (`design-tokens.css` + `index.html`):** **`--color-hub-bg-deep`**, **`--color-hub-card`**, **`--color-hub-border`**, **`--color-hub-text`**, **`--color-hub-text-muted`**, **`--color-hub-accent`**, **`--color-hub-footer-strong`** in **`public/css/design-tokens.css`**; company hub inline CSS uses them (drops duplicate **`:root`** hex aliases); **`design-tokens.css`** link moved **before** hub `<style>` so **`var(--font-spec-sans)`** / **`pill-*`** resolve consistently.
69. **Hub QBO strip neutral state (Rules 0 + 24):** **`erp-master-spec-2026.css`** — **`body.hub-page .erp-connection-strip`** without **`--ok` / `--warn` / `--muted`** uses **`--color-hub-card`**, **`--color-hub-border`**, **`--color-hub-text-muted`** so “checking…” matches the dark hub; tier classes unchanged.
70. **Hub workspace strip (`board-nav.css`, Rules 0 + 2):** **`body.hub-page`** overrides for **`.board-nav-wrap`**, strip toggle, item borders, trigger hover/open, and **`.board-nav-hint`** use **`--color-hub-bg-deep`**, **`--color-hub-card`**, **`--color-hub-border`**, **`--color-hub-text`**, **`--color-hub-text-muted`**, **`--font-spec-sans`**.
71. **Hub workspace dropdowns (`board-nav.css`, Rules 0 + 2):** **`body.hub-page .board-nav-dd`** + link rows use **`--color-hub-card`**, **`--color-hub-text`**, **`--color-hub-border`**; row hover uses **`--color-hub-bg-deep`** + **`--color-hub-accent`**; stronger shadow for depth on the dark page.
72. **Workspace strip keyboard focus (`board-nav.css`, Rule 0):** **`:focus-visible`** rings on **strip toggle**, **nav triggers**, and **dropdown links** use **`var(--color-border-focus)`**; **`body.hub-page`** uses **`--color-hub-accent`** for outline color.
73. **Company hub cards + help “?” on dark shell (`index.html` + `erp-master-spec-2026.css`, Rules 0 + 2 + 22):** **`a.card:focus-visible`** outline uses **`--color-hub-accent`**; **`body.hub-page .erp-help-tip__btn`** border / background / hover / **`focus-visible`** use **`--color-hub-*`** so inline **`?`** controls match the hub instead of light-theme chrome.
74. **Upload center narrow viewport + Tracking map Samsara rename tip (`maint-accounting-ui-2026.css` + `maintenance.html`, Rules 1 + 14 + 22 + 12):** **`@media (max-width: 900px)`** tightens **Upload center** tab padding, panel padding, and drop-zone padding; **Tracking → Map** **Rename vehicle in Samsara** replaces long inline **`mini-note`** with **`erp-help-tip`** (ids, **`SAMSARA_WRITE_API_TOKEN`**, scope / 403) plus one muted lead line.
75. **Tracking map Samsara driver rename tip + upload tab focus token (`maintenance.html` + CSS, Rules 22 + 12 + 0):** **Rename driver in Samsara** matches vehicle row (**`erp-help-tip`** + short lead); **Upload center** active sub-tab text / underline use **`var(--color-border-focus)`** in **`maint-accounting-ui-2026.css`** and **`erp-master-spec-2026.css`**.
76. **QBO sync bar + upload drop token polish (`app-theme.css` + `erp-master-redesign.css` + `maint-accounting-ui-2026.css`, Rules 21 + 0 + 14):** **`.qbo-sync-alert-bar`** — **`.qbo-sync-meta`** uses **`var(--muted, var(--color-text-label, …))`**; **`.qbo-sync-actions`** **`a` / `button`** **`:focus-visible`** outline **`var(--color-border-focus)`**; **`body.erp-maintenance #qboSyncAlertBar`** uses **`var(--pill-red-bg|pill-red-text|btn-danger-border|color-text-primary)`**; **Upload center** **`.mr-upload-drop`** / **`__primary`** / **`__icon`** use **`var(--color-border|bg-header|border-focus|pill-blue-bg|text-placeholder)`**.
77. **Upload drop keyboard + Safety HOS lead (`maintenance.html` + `maint-accounting-ui-2026.css`, Rules 14 + 0 + 16 + 22):** All **Upload center** **`.mr-upload-drop`** targets add **`tabindex="0"`**, **`role="button"`**, **`aria-label`**, **Enter / Space** **`onkeydown`**, and **`:focus-visible`** outline in CSS; **Safety → HOS clocks** adds a token-muted one-line lead above the filter bar (points at chips, **Show**, and **`?`**).
78. **Maintenance filter chips + global `?` help tokens (`erp-master-spec-2026.css`, Rules 0 + 15 + 22):** **`body.erp-maintenance .mr-filter-chipwrap > summary`** / **`[open]`** / **`.mr-filter-drop`** / **`.mr-filter-drop__hint`** / **`.mr-filter-search`** use **`var(--color-*|pill-blue-*|shadow-dropdown)`**; chip **`<summary>`** **`:focus-visible`** ring **`var(--color-border-focus)`**; **`.erp-help-tip__btn`** / **`.erp-help-tip__panel`** borders, surfaces, **`focus-visible`**, and panel shadow use design-token fallbacks (hub **`body.hub-page`** overrides unchanged).
79. **Filter bar search/select + upload `details` focus (`erp-master-spec-2026.css` + `maint-accounting-ui-2026.css`, Rules 15 + 0 + 14):** **`body.erp-maintenance .mr-filter-search`** and **`.mr-filter-inline-select`** **`:focus-visible`** rings **`var(--color-border-focus)`**; **Upload center** **`.mr-upload-help`** copy color + **`summary`** link color use **`var(--color-text-label|color-border-focus)`**; **`summary:focus-visible`** ring for expandable upload hints.
80. **Upload panel type + filter bar field focus + HOS legend tokens (`maint-accounting-ui-2026.css` + `erp-master-spec-2026.css`, Rules 14 + 0 + 15 + 16):** **`.mr-upload-panel-title`** / **`.mr-upload-panel-desc`** / **`.mr-upload-section-h`** use **`var(--color-text-primary|text-label)`**; **`body.erp-maintenance .mr-filter-bar .qb-in` / `.in`** **`:focus-visible`**; **`.mr-violation-legend`** muted text + pink/yellow dots use **`var(--color-text-label|pill-red-*|pill-yellow-*)`**.
81. **Filter bar button focus + Upload Connections tokens (`erp-master-spec-2026.css` + `maint-accounting-ui-2026.css`, Rules 15 + 0 + 14):** **`body.erp-maintenance .mr-filter-bar .btn` / `button`** **`:focus-visible`** outline **`var(--color-border-focus)`** (parity with inputs / **`qb-in`**); **Upload → Connections** **`.mr-upload-conn-card`** / **`__title`** and **`.mr-upload-pill--yellow|gray|red`** use **`var(--color-border|bg-card|text-primary|pill-*-*)`**.
82. **Satellite `mr-filter-bar` keyboard focus (`erp-master-spec-2026.css`, Rules 15 + 0):** **`body.fuel-board`**, **`body.banking-page`**, and **`body.settings-page`** — same **`:focus-visible`** outline as maintenance for **`.qb-in` / `.in` / `select.in` / `.btn` / `button`** inside **`.mr-filter-bar`**.
83. **Satellite + dispatch workspace strip typography (`erp-master-spec-2026.css`, Rules 0 + 2):** **`body.fuel-board`**, **`body.banking-page`**, **`body.settings-page`**, **`body.dispatch-page`** — **`.board-nav-wrap`** **`font-family: var(--font-spec-sans, …)`**; **`.board-nav-trigger`** and **`.board-nav-strip-toggle`** **`font-weight: 500`** (hub **`body.hub-page`** + **maintenance** strip unchanged).
84. **Maintenance boot resilience + pager keyboard polish (`maintenance.html` + `erp-master-spec-2026.css`, Rules 12 + 0 + 23):** **`openSection`** / **`openAccountingTab`** bail safely if a **`#section-*` / accounting tab** node is missing (avoids a **`null`** **`.classList`** access from stopping the rest of the page script); **`erpDocClickCloseNew`** guards **`ev.target.closest`**; **`loadAll`** **`catch`** calls **`applyNavHash()`** so hash / default section still applies when the first **`Promise.all`** refresh fails (e.g. server off); **`.erp-pager`** background **`var(--color-bg-header)`** + **`erp-pager__btn` / `erp-pager__size-sel`** **`:focus-visible`** ring **`var(--color-border-focus)`**.
85. **Reports roadmap tokens + narrow shell + WO field help + tracking smoke (`erp-master-spec-2026.css` + `maintenance.html` + `system-smoke.mjs`, Rules 0 + 1 + 22 + 2):** **`.erp-reports-roadmap`** border / background / text use **`var(--pill-yellow-*|color-semantic-warning)`**; at **≤900px**, **`.erp-main-body`** + **`.erp-topbar.topbar`** padding/gap tighten; **Maintenance** work-order **Field help** drawer: service one-liner + **`erp-help-tip`** for QBO lines / lock; smoke **`GET /tracking.html`** asserts redirect target **`maintenance.html#tracking`**.
86. **Topbar workspace tip + modal border + smoke (`maintenance.html` + `erp-master-spec-2026.css` + `erp-master-redesign.css` + `system-smoke.mjs`, Rules 2 + 0 + 22):** **`.topbar-hint-wrap`** — short lead + **`erp-help-tip`** for rail / search / **+ New**; **≤720px** hides **`.topbar-hint--lead`** only; **`.modal`** border token; smoke **`/maintenance.html`** also requires **`topbar-hint-wrap`**.
87. **Modal backdrop token + narrow sidebar padding (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 1):** **`--color-modal-backdrop`** on **`:root`**; maintenance **`.modal-bg`** uses **`var(--color-modal-backdrop, …)`**; at **≤900px**, **`.erp-module-sidebar__head` / `__inner` / `__footer`** horizontal padding tightens slightly.
88. **Dispatch modal parity + satellite tablet padding + cost header token (`erp-master-spec-2026.css`, Rules 0 + 1):** **Dispatch** **`.modal-bg`** / **`.modal`** match maintenance token pattern; **`.mr-kpi-card--ok`** border + **`.maint-cost-table thead th`** background use **`var(--color-*)`**; **≤900px** tighter horizontal padding / margins on **dispatch** board wrap + strip, **banking** **`.wrap`**, **settings** **`body`**, **fuel** connection strip.
89. **Connection strip tier borders + dedicated modal backdrop + hub tablet strip (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 1 + 24):** **`--color-success-border-soft`** / **`--color-warning-border-soft`**; **`.erp-connection-strip--ok` / `--warn`** border colors; **`.erp-dedicated-form-modal__backdrop`** uses **`--color-modal-backdrop`**; **≤900px** **company hub** **`.erp-connection-strip`** horizontal padding **24px → 16px**.
90. **Hourly Word-openable report + dispatch depriority + maintenance KPI/toast tokens (`scripts/generate-erp-progress-report.mjs` + `docs/reports/*` + `package.json` + `ERP_MASTER_REDESIGN_STATUS.md` + `erp-master-spec-2026.css`):** **`npm run report:erp`** → **`docs/reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf`** (open in Word; cron per **`docs/reports/README.md`**); guardrails: **dispatch** lowest priority; **Workspace snapshot** **`.mr-kpi-card*`** typography + **bad** tier + **`.erp-toast__x`** use design-token fallbacks (**no dispatch-only** changes in this item).
91. **QBO inline banner + +New column titles (`erp-master-spec-2026.css`, Rules 0 + 15 + 4):** **`.mr-qbo-banner*`** icon/title/meta/list/severity/buttons + **degraded** tier + **`body.erp-maintenance .erp-new-menu__col-title`** use **`var(--pill-red-*|color-text-*|color-border|color-bg-card|color-border-focus)`** fallbacks.
92. **Maintenance pills + sidebars + map/AP chrome (`erp-master-spec-2026.css`, Rules 0 + 4 + 12):** **`.mr-pill--*`** use **`var(--pill-*-*)`**; **+ New** **`erp-new-menu__item`** uses **`var(--pill-blue-text)`**; **`.erp-module-sidebar`** / **`.erp-reports-sidebar`** group chrome + **WO parts map** + **`#erpTrackingMap`** + **Accounting AP** **`qb-doc-shell--ap-spec`** topbar/backlink/hover use **`var(--color-*|pill-blue-*)`** fallbacks.
93. **AP / fuel doc chrome + pay bills wraps + reports hub + WO cost row (`erp-master-spec-2026.css`, Rules 0 + 11 + 13 + 17 + 12):** **`#acct-transactions`** close/footer; **`#acct-fuel-expense`** shell/topbar/back/close/footer; **`#acct-bill-pay`** shell + **`.mr-bp-table-wrap`** / **`.mr-bp-log-wrap`**; **`.erp-reports-hub*`** category grid + tiles; **`.erp-qb-cost-global*`** label/body text use **`var(--color-*|pill-blue-*)`** fallbacks.
94. **Global pager + toasts + menu/sidebar separators + driver pay chip (`erp-master-spec-2026.css`, Rules 0 + 19 + 23 + 4 + 11):** **`.erp-pager__btn` / `__size-sel`** surfaces + hover; primary **`.erp-btn--busy`** spinner notch uses **`var(--color-bg-card)`**; **`.erp-toast*`** card border/surface + message body + status dots use **`var(--color-*|btn-*|pill-red-text)`**; **`.erp-new-menu__sep*`** + **`.erp-sb-sep*`** lines/labels; **`.bp-driver-pay-chip`** uses **`var(--pill-blue-*|btn-info-border)`**.
95. **Maintenance + satellite shell hovers/surfaces (`erp-master-redesign.css`, Rules 0 + 2 + 12, no dispatch-only edits):** **`:root`** **`--qb-sidebar-active-bg`** → **`var(--pill-blue-bg)`**; **maintenance** icon rail active/hover text, sidebar collapse/footer, **+ New** item hover, topbar/subtab/board-nav hovers, QB dialog foot + secondary hover, hero/panel head, embedded cost shell, list rows, reports nav links, ops hero strip; **fuel / banking / settings** panel headers + table/hover + primary button label use **`var(--color-bg-card|bg-header|bg-hover)`**.
96. **Maint WO strip + accounting dash + upload + cost tables (`maint-accounting-ui-2026.css`, Rules 0 + 12 + 13 + 14):** **`.maint-action-strip*`**, **`.maint-fleet-pill*`**, **`.maint-qbo-*`**, **WO two-column** units/form/summary, **`.acct-dash-*`**, **`.mr-upload-*`** (deduped **`.mr-upload-panel-card`**), **`.maint-qb-cost-details*`** / **lines table** / **totals bar** / **`.erp-qb-seg*`**, scoped **`.erp-toast*`** under **maintenance** — raw hex replaced with **`var(--color-*|pill-*|qb-*|btn-primary-save-hover)`** fallbacks.
97. **Lists/catalog picker + maintenance form chrome (`app-theme.css` + `index.html`, Rules 0 + 2 + 12):** **`.catalog-add-card__head`** gradient + **`.catalog-plus-btn`** label + **`.catalog-search-results`** / **`.catalog-search-hit*`** + **`.qb-picker-menu`** / **`.qb-picker-item`** surfaces; **maintenance** default **`.btn` / `button`**:hover surface + **input/select/textarea** **`:focus`** border + **`.asset-cat-tile*`** + **`.erp-iframe`**; company hub **`.tag-tracking`** uses **`var(--pill-blue-bg)`** for chip fill.
98. **Legacy maintenance shell + drawer + settlement table (`app-theme.css`, Rules 0 + 5 + 17 + 12):** **`.erp-drawer`** / **`.erp-drawer__close`** surfaces; **maintenance** **`.sidebar-collapse-btn*`** (token borders/surfaces + **`:hover`** **brightness**), **`.nav-btn`** / **`.nav-btn.active`**; **`.panel-head`** gradient; **`.chip`**, **`.list`**, **`.list-row:hover`**, **`.unit-box`**, **`.table-wrap*`** / **`th`**, **`.st-settlement*`** header + row striping + cell borders use **`var(--color-*|sidebar-*|nav-*|nav-hover)`** fallbacks.
99. **Legacy sidebar nav row surfaces (`app-theme.css`, Rule 0):** **`.nav-btn:hover`** / **`.nav-btn.active`** use **`color-mix`** over **`var(--sidebar-text)`** / **`var(--nav-hover)`** with **rgba** fallbacks for engines without **`color-mix`**.
100. **Hub shell + banking primary + drawer backdrop (`app-theme.css`, Rules 0 + 2 + 5–9):** **`.hub-page`** page background + text use **`--color-hub-*`** + **`color-mix`** radial (**rgba** / hex fallbacks); **`.hub-page .grid a.card`** surface + **`color-mix`** border on **`--color-hub-text`** + hover accent border + **`var(--shadow-dropdown)`** fallback stack; **`.banking-page button.primary`** label **`var(--color-bg-card)`**; **`.erp-drawer__backdrop`** uses **`var(--color-modal-backdrop)`** with **rgba** fallback.
101. **Drawer elevation token (`design-tokens.css` + `app-theme.css`, Rule 0):** **`--shadow-drawer`** documents the eastern **`box-shadow`** for **`.erp-drawer`**; drawer rule prefers **`var(--shadow-drawer)`** with the prior literal as fallback.
102. **+ New menu shadow + busy notch (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 4 + 20):** **`--shadow-menu-panel`** for **`body.erp-maintenance #erpApp .erp-new-menu`** **`box-shadow`**; **`button.erp-btn--busy::after`** (default + **primary** / **banking** / **maintenance** / **settings**) uses **`color-mix`** on **`--color-text-primary`** / **`--color-bg-card`** with **rgba** fallbacks.
103. **Maintenance legacy surfaces + drawer borders (`app-theme.css`, Rules 0 + 12 + 13 + 16):** **`.erp-drawer*`** borders use **`var(--color-border|line)`**; **Safety** **`.safety-table-wrap`**, **shop** **`.shop-col-add`**, **tracking** map / **cards** / **iframe** / **tire** / **parts-map** host surfaces; **fuel import** sticky **`thead`** use **`--color-nav-bg|sidebar-*`** + **`--color-bg-header|card|hover`** fallbacks; **WO full modal** header gradient + title/body + **`.maint-modal`** surface tokenized.
104. **Maintenance QB doc + shop + nav dropdown + WO chrome (`app-theme.css`, Rules 0 + 2 + 11 + 12 + 13):** **`.qb-doc-*`** / **`.qb-lines-*`** / **`.qb-attach-box`** / **`.qb-btn-del`** surfaces + borders + titles; **`.shop-col-head`**; **`.nav-dropdown-menu`** border/shadow + **`.nav-dd-item*`** / **`.nav-dd-sep`**; **`.maint-modal-bg`** **`--color-modal-backdrop`**; **WO** **KPI** / **asset header** / **status pill** / **cost table** header + **modal flow** title.
105. **WO form + save split flyout (`app-theme.css`, Rules 0 + 12 + 13 + 20):** **`.maint-field--readonly`**; **repair chips** + **cost line** header/acct/extras rails; **`.maint-wo-banner*`** gradient + eyebrow/unit text (**banner** hairline **`box-shadow`** unchanged); **`.maint-subcard*`** / **`.maint-save-toolbar*`** gradients + **`var(--shadow-sm)`** where applicable; **`.qb-split__menu*`** surfaces + **`var(--shadow-dropdown)`** + focus/hover tokens; **neutral split caret**; **`#maintSaveHint`** caution/error → **`--color-semantic-*`**; **`.maint-panel-head-center`** + **`.maint-cost-coordination`** surfaces.
106. **Security alerts + catalog + print + fuel scroll (`app-theme.css`, Rules 0 + 12 + 13 + 16):** **`.sec-alert-high|warn|info`** borders ordered so **`border-left`** stripe wins + **`pill-*` / `btn-danger-border` / `color-warning-border-soft` / `color-semantic-warning`**; **`.catalog-admin-table`** **`th`/`td`** borders + **`th`** **`--color-bg-header`**; **`@media print`** **`body.erp-maintenance`** page background **`var(--color-bg-card)`**; **`.acct-fuel-import-scroll`** border/surface tokens.
107. **Save split + QB inputs (`app-theme.css`, Rules 0 + 13 + 20):** **`.qb-split--primary|neutral`** shells (**`--btn-primary-save-bg`** border + **`--sidebar-muted`** + **`--color-bg-card`**); **neutral** **`.qb-split__main`** gradient/text/hover + **primary** label/caret **`--color-bg-card`**; **`:focus-visible`** rings **`--color-warning-border-soft`**; **`.qb-split__caret`** divider **`color-mix`** on **`--color-bg-card`** (with **rgba** fallback); **`.qb-in`** border **`--color-border|line`**.
108. **Shop + board nav + KPI strip (`app-theme.css`, Rules 0 + 12):** **`.maint-exp-pill`** / **`.shop-queue-card*`** surfaces + borders + **overdue** **`--color-semantic-warn-accent`** border; **`.nav-badge--danger`** label **`--color-bg-card`**; **`.maint-board-nav__btn:hover`** **`color-mix`** on **`--accent`** (**rgba** fallback); **`.maint-board-nav__btn.active`** **`--app-frame-border` / `--sidebar-text` / `--color-nav-bg|sidebar-bg`**; **`.maint-kpi-card`** border/surface tokens.
109. **Catalog + picker + dashboard cards (`app-theme.css`, Rules 0 + 12):** **`.catalog-add-card*`** / **`.catalog-search-results`** borders + card surface; **`.qb-picker-menu`** border + prefers **`var(--shadow-dropdown)`** over **`--shadow-md`** (literal fallback); **maintenance** **`.hero-card` / `.panel` / `.metric` / `.record-card`** **`--color-bg-card|border`**; **`.qb-doc-actions`** top rule **`--color-border|line`**.
110. **WO map dots + QB primary + cost totals + settlement rows (`app-theme.css`, Rules 0 + 12 + 13 + 17):** **`.tire-dot` / `.maint-part-dot`** fills/strokes/active use **`--line-strong`**, **`--muted`**, **`--accent`**, **`--color-nav-bg`**, **`--sidebar-bg`**; **`.qb-btn-primary`** uses **`--btn-primary-save-bg|hover`** + **`--color-bg-card`** label; **`.cost-total-ok|bad`** use **`--color-success-border-soft`** / **`--color-semantic-*`** with **`color-mix`** ring (rgba fallbacks); **`table.st-settlement td`** **`border-bottom`** **`var(--color-border, var(--line))`**; **`.qb-attach-box`** + **`.maint-line-part-map-host`** dashed borders **`var(--color-border, var(--line-strong))`**.
111. **QB shell `:root` token bridge (`erp-master-redesign.css`, Rules 0 + 2):** **`--qb-page|panel|line|line-input|text|text-strong|muted|placeholder`** → **`--color-bg-page|bg-card|border|border-input|text-body|text-primary|text-label|text-placeholder`**; **`--qb-green`** → **`--btn-primary-save-bg`**; **`--qb-blue`** → **`--color-border-focus`**; **`--qb-red`** → **`--color-semantic-error`**; **`--qb-icon-nav-bg|muted`** → **`--color-nav-bg|nav-inactive`**; **`--qb-amber`** unchanged (no spec-aligned amber token); **`--qb-focus-ring`** unchanged rgba.
112. **Dispatch sheet literals (`erp-master-redesign.css`, Rules 0 + 2):** **`body.dispatch-page`** legacy **`.topbar` / `.tabs` / `.data` / `.modal`** + **`#dispatchApp`** **topbar** / **sidebar** / **`.erp-btn-primary`** / **embed status bar** — remaining **`#f1f3f4`**, **`#fafbfc`**, **`#e8f0fe`**, **`#aec9f9`**, **`#d2e3fc`**, **`#fff`** on controls use **`var(--color-bg-header|pill-blue-bg|btn-info-border|color-bg-active|color-bg-card)`**; **`@media print`** literals unchanged.
113. **Dispatch TMS in `app-theme.css` (Rules 0 + 2):** **`body.dispatch-page .topbar button:hover`** → **`var(--color-bg-hover)`**; **`.topbar button.secondary:hover`** → **`var(--pill-blue-bg)`**; **`.banner`** / **`.banner.err`** borders → **`var(--color-warning-border-soft)`** / **`var(--btn-danger-border)`**; **`table.data th`** → **`var(--color-bg-hover)`**.
114. **Maintenance dashboard status pills (`app-theme.css`, Rules 0 + 12):** **`.status-current` / `.status-soon` / `.status-past` / `.status-posted` / `.status-error`** backgrounds and label colors prefer **`--pill-green-*`**, **`--pill-yellow-*`**, **`--pill-red-*`**, **`--pill-blue-*`** from **`design-tokens.css`**, with existing **`--green-soft`**, **`--yellow-soft`**, **`--red-soft`**, **`--accent-soft`**, **`--accent`** as second fallbacks.
115. **Home/dashboard chips + list shell (`app-theme.css`, Rules 0 + 12):** **`.chip`** / **`.list`** / **`.list-row`** borders use **`var(--color-border, var(--line))`**; **`.chip.active`** and **`.list-row.active`** surfaces use **`--pill-blue-bg`**, **`--btn-info-border`**, **`--pill-blue-text`** with **`--accent-soft` / `--accent-border` / `--accent`** fallbacks.
116. **Unit box + table shells (`app-theme.css`, Rules 0 + 12 + 17):** **`.unit-box`** border; **`.table-wrap`** shell + **`.table-wrap td|th`** row rules; **generic** **`body.erp-maintenance th|td`**; **`.st-settlement-wrap`** outer border — all use **`var(--color-border, var(--line|line-strong, …))`** instead of bare **`--line`**.
117. **Subtabs + shop + WO + catalog + aside + cost + fuel import (`app-theme.css`, Rules 0 + 12 + 13):** **`.subtab`**, **`.wo-line`**, **`.shop-col` / `.shop-row`**, **`.asset-cat-tile`**, **`.shop-wo-fallback`** (dashed), **`.maint-aside-kv` / `.maint-aside-issue`**, **`.maint-asset-header-card`**, **`.maint-asset-status-panel`** (dashed), **`.maint-cost-table`**, **`.acct-fuel-import-table`** — borders use **`var(--color-border, var(--line|line-strong))`**.
118. **WO modal + cost chrome + save bar (`app-theme.css`, Rules 0 + 12):** **`.maint-workorder-fullmodal__head`**, **`.maint-cost-table__totalrow td`**, **`.maint-cost-lines-head`**, **`.maint-cost-lines-footer`** (dashed), **`.maint-cost-panel-card`**, **`.maint-save-toolbar`**, **`.maint-panel-head-center`**, **`.btn--ghost`**, **`.record-card:hover`** — **`--line-strong`** borders now read **`var(--color-border, var(--line-strong, …))`**.
119. **App frame token + satellite panels (`design-tokens.css` + `app-theme.css`, Rules 0 + 1 + 12 + 13):** New **`--color-app-frame-border`**; **maintenance** navy frame chrome (**`.panel-head`**, **`.qb-doc-shell`**, **`.maint-section-card`**, **`.maint-wo-banner`**, **`.maint-subcard`**, **`#section-maintenance`** panel head + **unit/cost panel** borders) and **active** **shop nav** / **repair chip** fills use **`--color-nav-bg`** where appropriate; **banking** / **fuel** **`.panel`** borders + **fuel** **fleet** dividers tokenized.
120. **Board-nav dropdown border (`board-nav.css`, Rules 0 + 2):** **`.board-nav-dd`** uses **`var(--color-border, var(--line, #e2e6ee))`** so **`design-tokens`** wins over legacy **`--line`** (fixes inverted fallback order).
121. **Global focus ring (`app-theme.css`, Rules 0 + 2):** Root **`:focus-visible`** **`outline`** uses **`var(--color-border-focus, var(--accent))`** so pages that load **`design-tokens.css`** pick up the spec focus blue before the legacy accent.
122. **Maintenance shell chrome borders (`app-theme.css`, Rules 0 + 12):** **`.topbar`** divider, **`.sidebar-expand-fab`**, **default** **`.btn` / `button`**, and **`input` / `select` / `textarea`** use **`var(--color-border, var(--line|line-strong))`** instead of bare **`--line`** / **`--line-strong`**.
123. **Board-nav dropdown row dividers (`board-nav.css`, Rules 0 + 2):** **`.board-nav-dd a`** **`border-bottom`** uses **`var(--color-border, var(--line))`**; **`body.hub-page`** still overrides **`border-bottom-color`** with **`--color-hub-border`**.
124. **Maintenance divider borders vs header fill (`maint-accounting-ui-2026.css` + `erp-master-spec-2026.css`, Rules 0 + 3 + 12 + 13 + 14):** **`border-bottom: 1px solid var(--color-bg-header, …)`** on **WO unit picker rows**, **upload recent imports table cells**, **QB cost-line body cells**, **module sidebar** **`.erp-sb-group` / `.erp-sb-group__head`**, and **reports** **`.mr-rep-nav-group`** → **`var(--color-border, var(--line, #e2e6ee))`** so dividers follow the shared border token (not header background as a line color).
125. **Banking satellite inline borders (`banking.html`, Rules 0 + 13):** Page **`<style>`** **`.panel`**, **`.ph`**, **`.metric`**, **`button`**, **`textarea`**, and **`th|td`** rules that used bare **`var(--line)`** now use **`var(--color-border, var(--line))`** so **`design-tokens.css`** can steer borders consistently with other ERP surfaces.
126. **Fuel + settings satellite inline borders (`fuel.html` + `settings.html`, Rules 0 + 2):** **`fuel.html`** topbar, panels, fleet columns/items/badges, autocomplete list shell, and form controls: every **`solid var(--line)`** border → **`solid var(--color-border, var(--line))`**. **`settings.html`** **`<style>`** **`input` / `button` / `th|td`** plus inline **`select`** borders use the same stack.
127. **Fuel + settings narrow viewport + settings panel frame (`fuel.html` + `settings.html`, Rules 0 + 1):** **`@media (max-width: 900px)`** — **fuel** trims **`.topbar`** padding and **`.layout`** padding/gap; **settings** trims **`body`** horizontal/vertical padding and **`.panel`** inner padding/margin. **`settings.html`** **`.panel`** border uses **`var(--color-app-frame-border, var(--app-frame-border))`** (same stack as **`app-theme`** QB/maintenance frames).
128. **Banking satellite narrow viewport (`banking.html`, Rules 1 + 2):** **`@media (max-width: 900px)`** tightens **`.wrap`** padding (**18px → 14px** sides, **48px → 40px** bottom) and slightly reduces **`.ph`** / **`.pb`** vertical padding so the bank matching page matches **fuel** / **settings** tablet behavior.
129. **Company hub narrow viewport (`index.html`, Rules 0 + 1 + 2):** **`@media (max-width: 900px)`** reduces **`header`** horizontal padding, **`.grid`** gap/padding, **`a.card`** padding + **`min-height`**, and **`footer`** padding so the dark hub matches other ERP surfaces on tablets (cards already use **`--color-hub-border`** — no bare **`--line`** swap needed).
130. **Hub workspace strip tablet chrome (`erp-master-spec-2026.css`, Rules 0 + 1 + 2):** Inside the existing **≤900px** satellite block, **`body.hub-page .board-nav-inner`** horizontal padding **12px → 8px**; **`body.hub-page .board-nav-trigger`** padding **11px 16px → 10px 12px** so the sticky strip aligns with tightened **`.erp-connection-strip`** padding on the hub.

---

## Recommendation audit vs codebase (FINAL_REPORT §5–7)

| Source | Status |
|--------|--------|
| **[`ERP_MASTER_REDESIGN_FINAL_REPORT.md`](./ERP_MASTER_REDESIGN_FINAL_REPORT.md) §5 P0** — smoke test, sign-in / 401 paths, QBO strip with server | **Manual / environment** — not executable as static code; run when a server and credentials are available. |
| **§7 verification checklist** (checkbox list) | **Manual QA** — expanded list lives in **[`ERP_MASTER_REDESIGN_POST_RELEASE_CHECKLIST.md`](./ERP_MASTER_REDESIGN_POST_RELEASE_CHECKLIST.md)**; tick there when you run sign-off. |
| **[`ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md`](./ERP_MASTER_REDESIGN_DEFERRED_AFTER_CHECKLIST.md)** — open bills pager + selection model, driver pay nested table pagination, Safety → Driver files pager | **Intentionally not implemented** — requires architecture/product decisions per deferred doc. |
| **Deferred §2** — remaining upload / dense **`mini-note`** sweeps | **Partial** — upload subpanels already use title **`erp-help-tip`** + short **`mr-upload-panel-desc`**; further folding is optional. |
| **Deferred §3–4** — universal Samsara strip, Rule 4/10/18 platform items | **Out of scope** for checklist UI passes unless product picks them up. |

---

## Product / engineering notes (optional; not blockers for checklist work)

1. **Priorities:** Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).
2. **Rule 10 / React path:** Shared component + build step vs one canonical HTML/JS cost-line implementation.
3. **Rule 4 — + New menu:** Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).
4. **Rule 18:** Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.
5. **Regression:** Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.

---

## Rules that are **large** or **platform-shaped** (why “100%” is staged)

- **Scope:** Rules 5–12, 17, modals, and layout restructures touch **tens of thousands** of lines in `maintenance.html` alone.
- **Persistence:** Big-bang **save/post/sync** edits are **risky**—keep them scoped PRs, not mixed with UI-only sweeps.
- **No `/src` tree:** StandardExpenseLines as a separate framework component **does not apply** without a broader platform decision.
- **Verification:** Full QBO/Samsara click-through requires a **running environment** and credentials—not fully automated in CI.

---

## Reminder (uploaded lists)

If a newer checklist lives **outside** this repo, add it under `docs/` or paste unchecked items into a GitHub issue / this file’s appendix so implementation can be traced rule-by-rule.
