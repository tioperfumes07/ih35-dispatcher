{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T20:59:09.616Z\par \par --- Summary ---\par Overall completion: ~66.3% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6628 → 66.28%; rounded headline ~66.3%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6628 (66.28%; headline ~66.3%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 123. Board-nav dropdown row dividers (`board-nav.css`, Rules 0 + 2): `.board-nav-dd a` `border-bottom` uses `var(--color-border, var(--line))`; `body.hub-page` still overrides `border-bottom-color` with `--color-hub-border`.\par 124. Maintenance divider borders vs header fill (`maint-accounting-ui-2026.css` + `erp-master-spec-2026.css`, Rules 0 + 3 + 12 + 13 + 14): `border-bottom: 1px solid var(--color-bg-header, …)` on WO unit picker rows, upload recent imports table cells, QB cost-line body cells, module sidebar `.erp-sb-group` / `.erp-sb-group__head`, and reports `.mr-rep-nav-group` → `var(--color-border, var(--line, #e2e6ee))` so dividers follow the shared border token (not header background as a line color).\par 125. Banking satellite inline borders (`banking.html`, Rules 0 + 13): Page `<style>` `.panel`, `.ph`, `.metric`, `button`, `textarea`, and `th|td` rules that used bare `var(--line)` now use `var(--color-border, var(--line))` so `design-tokens.css` can steer borders consistently with other ERP surfaces.\par 126. Fuel + settings satellite inline borders (`fuel.html` + `settings.html`, Rules 0 + 2): `fuel.html` topbar, panels, fleet columns/items/badges, autocomplete list shell, and form controls: every `solid var(--line)` border → `solid var(--color-border, var(--line))`. `settings.html` `<style>` `input` / `button` / `th|td` plus inline `select` borders use the same stack.\par 127. Fuel + settings narrow viewport + settings panel frame (`fuel.html` + `settings.html`, Rules 0 + 1): `@media (max-width: 900px)` — fuel trims `.topbar` padding and `.layout` padding/gap; settings trims `body` horizontal/vertical padding and `.panel` inner padding/margin. `settings.html` `.panel` border uses `var(--color-app-frame-border, var(--app-frame-border))` (same stack as `app-theme` QB/maintenance frames).\par 128. Banking satellite narrow viewport (`banking.html`, Rules 1 + 2): `@media (max-width: 900px)` tightens `.wrap` padding (18px → 14px sides, 48px → 40px bottom) and slightly reduces `.ph` / `.pb` vertical padding so the bank matching page matches fuel / settings tablet behavior.\par 129. Company hub narrow viewport (`index.html`, Rules 0 + 1 + 2): `@media (max-width: 900px)` reduces `header` horizontal padding, `.grid` gap/padding, `a.card` padding + `min-height`, and `footer` padding so the dark hub matches other ERP surfaces on tablets (cards already use `--color-hub-border` — no bare `--line` swap needed).\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}