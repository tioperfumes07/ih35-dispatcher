{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T19:42:02.494Z\par \par --- Summary ---\par Overall completion: ~65.8% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6580 → 65.80%; rounded headline ~65.8%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6580 (65.80%; headline ~65.8%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 98. Legacy maintenance shell + drawer + settlement table (`app-theme.css`, Rules 0 + 5 + 17 + 12): `.erp-drawer` / `.erp-drawer__close` surfaces; maintenance `.sidebar-collapse-btn*` (token borders/surfaces + `:hover` brightness), `.nav-btn` / `.nav-btn.active`; `.panel-head` gradient; `.chip`, `.list`, `.list-row:hover`, `.unit-box`, `.table-wrap*` / `th`, `.st-settlement*` header + row striping + cell borders use `var(--color-*|sidebar-*|nav-*|nav-hover)` fallbacks.\par 99. Legacy sidebar nav row surfaces (`app-theme.css`, Rule 0): `.nav-btn:hover` / `.nav-btn.active` use `color-mix` over `var(--sidebar-text)` / `var(--nav-hover)` with rgba fallbacks for engines without `color-mix`.\par 100. Hub shell + banking primary + drawer backdrop (`app-theme.css`, Rules 0 + 2 + 5–9): `.hub-page` page background + text use `--color-hub-*` + `color-mix` radial (rgba / hex fallbacks); `.hub-page .grid a.card` surface + `color-mix` border on `--color-hub-text` + hover accent border + `var(--shadow-dropdown)` fallback stack; `.banking-page button.primary` label `var(--color-bg-card)`; `.erp-drawer__backdrop` uses `var(--color-modal-backdrop)` with rgba fallback.\par 101. Drawer elevation token (`design-tokens.css` + `app-theme.css`, Rule 0): `--shadow-drawer` documents the eastern `box-shadow` for `.erp-drawer`; drawer rule prefers `var(--shadow-drawer)` with the prior literal as fallback.\par 102. + New menu shadow + busy notch (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 4 + 20): `--shadow-menu-panel` for `body.erp-maintenance #erpApp .erp-new-menu` `box-shadow`; `button.erp-btn--busy::after` (default + primary / banking / maintenance / settings) uses `color-mix` on `--color-text-primary` / `--color-bg-card` with rgba fallbacks.\par 103. Maintenance legacy surfaces + drawer borders (`app-theme.css`, Rules 0 + 12 + 13 + 16): `.erp-drawer*` borders use `var(--color-border|line)`; Safety `.safety-table-wrap`, shop `.shop-col-add`, tracking map / cards / iframe / tire / parts-map host surfaces; fuel import sticky `thead` use `--color-nav-bg|sidebar-*` + `--color-bg-header|card|hover` fallbacks; WO full modal header gradient + title/body + `.maint-modal` surface tokenized.\par 104. Maintenance QB doc + shop + nav dropdown + WO chrome (`app-theme.css`, Rules 0 + 2 + 11 + 12 + 13): `.qb-doc-*` / `.qb-lines-*` / `.qb-attach-box` / `.qb-btn-del` surfaces + borders + titles; `.shop-col-head`; `.nav-dropdown-menu` border/shadow + `.nav-dd-item*` / `.nav-dd-sep`; `.maint-modal-bg` `--color-modal-backdrop`; WO KPI / asset header / status pill / cost table header + modal flow title.\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}