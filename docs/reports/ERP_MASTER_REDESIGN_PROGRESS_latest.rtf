{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T21:41:49.432Z\par \par --- Summary ---\par Overall completion: ~66.3% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6629 → 66.29%; rounded headline ~66.3%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6629 (66.29%; headline ~66.3%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 134. Settings satellite panel + buttons (`settings.html`, Rules 0 + 2): Page `<style>` `.panel` gradient end `var(--panel)` → `var(--color-bg-card, var(--panel))`; non-primary `button` `background` uses the same stack (matches banking / fuel / `app-theme` maintenance chrome).\par 135. Fuel + banking satellite panel shells (`fuel.html`, `banking.html`, Rules 0 + 2): Page `<style>` `.panel` `background: var(--panel)` → `var(--color-bg-card, var(--panel))` so inline shells agree with `app-theme` `.banking-page` / `body.fuel-board` panel treatment and `settings`.\par 136. Maintenance shop inline surfaces (`maintenance.html`, Rules 0 + 12): Shop queue PDF `<a style="…">` `background: var(--panel)` → `var(--color-bg-card, var(--panel))`; JS template `return \\`<div style="…background:var(--panel)…>\\`` uses the same stack so dynamic cards match `app-theme` maintenance chrome.\par 137. Maintenance WO tire extra + maint detailed line chips (`maintenance.html`, Rules 0 + 12): `#tireExtraLineTpl` `.tire-extra-row` `border` / `background` use `var(--color-border, #e5e7eb)` + `var(--color-bg-header, #fafafa)`; Reports → Maintenance detailed `lineHtml` nested row `background:#fbfdff` → `var(--color-bg-hover, #fbfdff)` so nested cost lines read `design-tokens` when linked.\par 138. Maintenance JS inline hex → tokens (`maintenance.html`, Rules 0 + 12): Home “recent visits by repair location type” bucket headers `color:#334155` → `var(--color-text-body, #334155)`; Shop → Integrity “Driver / damage expense signals” title `#0f172a` → `var(--color-text-primary, #0f172a)`; shop queue overdue status `<select>` `border-color:#f59e0b` → `var(--color-semantic-warn-accent, #f59e0b)`.\par 139. Dispatch load-board modal + buttons (`dispatch.html`, Rules 0 + 2): Page `<style>` `.modal` and default `.btn` `background: var(--panel)` → `var(--color-bg-card, var(--panel))`, matching satellite `.panel` treatment and clearing the last bare `--panel` fills in `public/*.html`.\par 140. Dispatch load-board border stacks (`dispatch.html`, Rules 0 + 2): Same page `<style>` — `1px solid var(--line)` → `var(--color-border, var(--line))` on `.modal`, `.modal h2`, `.stop-block`, `.btn`, `.ac-list`, embed status bar, `select.sm` / `input.sm`, `.stop-miles-summary`, etc.; `.btn:hover` / `.btn-ghost` `border-color` use `var(--color-border, var(--line-strong|line))` instead of bare `--line*` (parity with changelog 126 satellites).\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}