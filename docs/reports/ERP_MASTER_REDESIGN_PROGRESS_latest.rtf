{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T19:28:50.560Z\par \par --- Summary ---\par Overall completion: ~65.8% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6576 → 65.76%; rounded headline ~65.8%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6576 (65.76%; headline ~65.8%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 93. AP / fuel doc chrome + pay bills wraps + reports hub + WO cost row (`erp-master-spec-2026.css`, Rules 0 + 11 + 13 + 17 + 12): `#acct-transactions` close/footer; `#acct-fuel-expense` shell/topbar/back/close/footer; `#acct-bill-pay` shell + `.mr-bp-table-wrap` / `.mr-bp-log-wrap`; `.erp-reports-hub*` category grid + tiles; `.erp-qb-cost-global*` label/body text use `var(--color-*|pill-blue-*)` fallbacks.\par 94. Global pager + toasts + menu/sidebar separators + driver pay chip (`erp-master-spec-2026.css`, Rules 0 + 19 + 23 + 4 + 11): `.erp-pager__btn` / `__size-sel` surfaces + hover; primary `.erp-btn--busy` spinner notch uses `var(--color-bg-card)`; `.erp-toast*` card border/surface + message body + status dots use `var(--color-*|btn-*|pill-red-text)`; `.erp-new-menu__sep*` + `.erp-sb-sep*` lines/labels; `.bp-driver-pay-chip` uses `var(--pill-blue-*|btn-info-border)`.\par 95. Maintenance + satellite shell hovers/surfaces (`erp-master-redesign.css`, Rules 0 + 2 + 12, no dispatch-only edits): `:root` `--qb-sidebar-active-bg` → `var(--pill-blue-bg)`; maintenance icon rail active/hover text, sidebar collapse/footer, + New item hover, topbar/subtab/board-nav hovers, QB dialog foot + secondary hover, hero/panel head, embedded cost shell, list rows, reports nav links, ops hero strip; fuel / banking / settings panel headers + table/hover + primary button label use `var(--color-bg-card|bg-header|bg-hover)`.\par 96. Maint WO strip + accounting dash + upload + cost tables (`maint-accounting-ui-2026.css`, Rules 0 + 12 + 13 + 14): `.maint-action-strip*`, `.maint-fleet-pill*`, `.maint-qbo-*`, WO two-column units/form/summary, `.acct-dash-*`, `.mr-upload-*` (deduped `.mr-upload-panel-card`), `.maint-qb-cost-details*` / lines table / totals bar / `.erp-qb-seg*`, scoped `.erp-toast*` under maintenance — raw hex replaced with `var(--color-*|pill-*|qb-*|btn-primary-save-hover)` fallbacks.\par 97. Lists/catalog picker + maintenance form chrome (`app-theme.css` + `index.html`, Rules 0 + 2 + 12): `.catalog-add-card__head` gradient + `.catalog-plus-btn` label + `.catalog-search-results` / `.catalog-search-hit*` + `.qb-picker-menu` / `.qb-picker-item` surfaces; maintenance default `.btn` / `button`:hover surface + input/select/textarea `:focus` border + `.asset-cat-tile*` + `.erp-iframe`; company hub `.tag-tracking` uses `var(--pill-blue-bg)` for chip fill.\par 98. Legacy maintenance shell + drawer + settlement table (`app-theme.css`, Rules 0 + 5 + 17 + 12): `.erp-drawer` / `.erp-drawer__close` surfaces; maintenance `.sidebar-collapse-btn*` (token borders/surfaces + `:hover` brightness), `.nav-btn` / `.nav-btn.active`; `.panel-head` gradient; `.chip`, `.list`, `.list-row:hover`, `.unit-box`, `.table-wrap*` / `th`, `.st-settlement*` header + row striping + cell borders use `var(--color-*|sidebar-*|nav-*|nav-hover)` fallbacks.\par 99. Legacy sidebar nav row surfaces (`app-theme.css`, Rule 0): `.nav-btn:hover` / `.nav-btn.active` use `color-mix` over `var(--sidebar-text)` / `var(--nav-hover)` with rgba fallbacks for engines without `color-mix`.\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}