{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T20:23:38.130Z\par \par --- Summary ---\par Overall completion: ~66.2% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.662 → 66.2%; rounded headline ~66.2%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.662 (66.2%; headline ~66.2%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 112. Dispatch sheet literals (`erp-master-redesign.css`, Rules 0 + 2): `body.dispatch-page` legacy `.topbar` / `.tabs` / `.data` / `.modal` + `#dispatchApp` topbar / sidebar / `.erp-btn-primary` / embed status bar — remaining `#f1f3f4`, `#fafbfc`, `#e8f0fe`, `#aec9f9`, `#d2e3fc`, `#fff` on controls use `var(--color-bg-header|pill-blue-bg|btn-info-border|color-bg-active|color-bg-card)`; `@media print` literals unchanged.\par 113. Dispatch TMS in `app-theme.css` (Rules 0 + 2): `body.dispatch-page .topbar button:hover` → `var(--color-bg-hover)`; `.topbar button.secondary:hover` → `var(--pill-blue-bg)`; `.banner` / `.banner.err` borders → `var(--color-warning-border-soft)` / `var(--btn-danger-border)`; `table.data th` → `var(--color-bg-hover)`.\par 114. Maintenance dashboard status pills (`app-theme.css`, Rules 0 + 12): `.status-current` / `.status-soon` / `.status-past` / `.status-posted` / `.status-error` backgrounds and label colors prefer `--pill-green-*`, `--pill-yellow-*`, `--pill-red-*`, `--pill-blue-*` from `design-tokens.css`, with existing `--green-soft`, `--yellow-soft`, `--red-soft`, `--accent-soft`, `--accent` as second fallbacks.\par 115. Home/dashboard chips + list shell (`app-theme.css`, Rules 0 + 12): `.chip` / `.list` / `.list-row` borders use `var(--color-border, var(--line))`; `.chip.active` and `.list-row.active` surfaces use `--pill-blue-bg`, `--btn-info-border`, `--pill-blue-text` with `--accent-soft` / `--accent-border` / `--accent` fallbacks.\par 116. Unit box + table shells (`app-theme.css`, Rules 0 + 12 + 17): `.unit-box` border; `.table-wrap` shell + `.table-wrap td|th` row rules; generic `body.erp-maintenance th|td`; `.st-settlement-wrap` outer border — all use `var(--color-border, var(--line|line-strong, …))` instead of bare `--line`.\par 117. Subtabs + shop + WO + catalog + aside + cost + fuel import (`app-theme.css`, Rules 0 + 12 + 13): `.subtab`, `.wo-line`, `.shop-col` / `.shop-row`, `.asset-cat-tile`, `.shop-wo-fallback` (dashed), `.maint-aside-kv` / `.maint-aside-issue`, `.maint-asset-header-card`, `.maint-asset-status-panel` (dashed), `.maint-cost-table`, `.acct-fuel-import-table` — borders use `var(--color-border, var(--line|line-strong))`.\par 118. WO modal + cost chrome + save bar (`app-theme.css`, Rules 0 + 12): `.maint-workorder-fullmodal__head`, `.maint-cost-table__totalrow td`, `.maint-cost-lines-head`, `.maint-cost-lines-footer` (dashed), `.maint-cost-panel-card`, `.maint-save-toolbar`, `.maint-panel-head-center`, `.btn--ghost`, `.record-card:hover` — `--line-strong` borders now read `var(--color-border, var(--line-strong, …))`.\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}