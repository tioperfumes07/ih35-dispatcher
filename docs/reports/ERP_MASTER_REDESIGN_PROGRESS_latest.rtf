{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-20T02:49:08.696Z\par \par --- Summary ---\par Overall completion: ~66.3% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6634 → 66.34%; rounded headline ~66.3%.) Changelogs 141–468 (468 — `erp-master-redesign.css` dispatch `.modal h2` + `maint-accounting-ui-2026.css` `.mr-upload-recent-table` `th`/`td` — `min-width: 0`, `overflow-wrap: anywhere` (changelog 468); 467 — `erp-master-spec-2026.css` `.erp-toast` / `.erp-toast__msg` — `min`/`max-width` vs `100vw` + `overflow-wrap: anywhere` (narrow screens) (changelog 467); 466 — `app-theme.css` dispatch `table.data` `th`/`td` — `overflow-wrap: anywhere` (load board cells) (changelog 466); 465 — `erp-master-redesign.css` banking / settings `table` `th`/`td` — `overflow-wrap: anywhere` (long memo / vendor / email cells) (changelog 465); 464 — `erp-master-redesign.css` dispatch `.tabs.dispatch-board-tabs` `button` + hub `.hub-footer-tip` — `min-width: 0`, `overflow-wrap: anywhere` (changelog 464); 463 — `erp-master-redesign.css` dispatch `#msg` + `.dispatch-board-intro` + `erp-master-spec-2026.css` `.dispatch-board-intro--compact` — `min-width: 0`, `overflow-wrap: anywhere` (changelog 463); 462 — `maint-accounting-ui-2026.css` `.maint-action-strip__note` + panel heads (catalog / reports / uploads) + `mr-bp-panel-head__title` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports) (changelog 462); 461 — `maint-accounting-ui-2026.css` `.acct-dash-kpi_*` / `.acct-dash-card__head` + reports `.erp-reports-hub__tile-title`/`__tile-meta`/`__label` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports) (changelog 461); 460 — `maint-accounting-ui-2026.css` `.maint-qb-cost-details__title` — `flex`: `1 1 auto`, `min-width: 0`, `overflow-wrap: anywhere` (AP / WO `<details>` line headers) (changelog 460); 459 — `erp-master-redesign.css` dispatch sidebar (`.erp-module-sidebar__brand-title` / `.erp-sb-heading` / `.erp-sb-link` / `.dispatch-sidebar-footer__line`) + `.maint-banner` + fuel `.roadmap-note` / `.fuel-erp-write-banner` + hub `.hub-footer-lead` — `min-width: 0`, `overflow-wrap: anywhere` (changelog 459); 458 — `erp-master-redesign.css` maintenance `.erp-topbar .mini-note` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports; lead tip line) (changelog 458); 457 — `erp-master-redesign.css` `body.settings-page` `p` + fuel `.fleet-summary` / `.fleet-section-title` / `.fleet-title` / `.fleet-sub` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports) (changelog 457); 456 — `erp-master-redesign.css` fuel `.panel h2` + hub `a.card h2`/`p` + banking `p.lead` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports) (changelog 456); 455 — `erp-master-spec-2026.css` `.erp-new-menu__col-title` + `board-nav.css` `.board-nav-hint` + `erp-master-redesign.css` `html.dispatch-embed` `.dispatch-embed-status-bar` + maintenance `.erp-topbar .topbar-hint` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports) (changelog 455); 454 — `app-theme.css` `.qbo-sync-alert-bar` `.qbo-sync-head` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports: long QBO sync headline wraps) (changelog 454); 453 — `erp-master-spec-2026.css` `.erp-connection-strip` `overflow-wrap: anywhere` + `erp-master-redesign.css` `body.hub-page` `header p` `min-width: 0` / `overflow-wrap: anywhere` (Rule 24 strip + hub intro) (changelog 453); 452 — `erp-master-redesign.css` `body.erp-maintenance` `.erp-topbar .title` + `body.dispatch-page` `.topbar h1` — `min-width: 0`, `overflow-wrap: anywhere` (maintenance + legacy dispatch topbar titles) (changelog 452); 451 — `erp-master-redesign.css` `body.fuel-board` `.topbar` `#lastUpdated` — `min-width: 0`, `flex: 1 1 12rem`, `overflow-wrap: anywhere` + `.topbar` `min-width: 0` (status line wraps on narrow viewports) (changelog 451); 450 — `erp-master-redesign.css` `body.dispatch-page` `#dispatchApp` `.erp-topbar .title` + `body.hub-page` `header h1` — `min-width: 0`, `overflow-wrap: anywhere` (narrow viewports: long titles wrap) (changelog 450); 449 — `erp-master-spec-2026.css` `#erpDedicatedFormModal` bar `title` `min-width: 0` + `total-wrap` `flex-shrink: 0` (narrow shells: title ellipsizes before crowding amount/actions) (changelog 449); 448 — `erp-master-redesign.css` `body.banking-page` `h1` + `body.settings-page` `h1` — `min-width: 0`, `overflow-wrap: anywhere` for long titles on narrow viewports (changelog 448); 447 — `erp-master-spec-2026.css` `body.fuel-board` `.erp-connection-strip` — `margin: 0 auto`, `width` / `max-width: min(1100px, 100%)` (replace horizontal margins that could widen past the viewport; drop ≤900px margin override) (changelog 447); 446 — `erp-master-redesign.css` `body.banking-page` `.row` — `min-width: 0` so filter/control rows shrink inside `.wrap` on narrow viewports (changelog 446); 445 — `erp-master-redesign.css` `body.fuel-board` `.roadmap-note` + `.fuel-erp-write-banner` — `margin: 0 auto`, `width: min(1100px, 100%)`, safe-area horizontal `padding` (replace `margin: 0 12px` that could widen past the viewport) (changelog 445); 444 — `erp-master-redesign.css` `body.hub-page` `.hub-footer-lead` — `min-width: 0` (was `min(100%, 240px)`) so footer copy + `erp-help-tip` flex row can shrink without horizontal overflow (changelog 444); 443 — `erp-master-redesign.css` `body.fuel-board` `.topbar h1` `min-width: 0` + ≤720px `flex: 1 1 100%`; `board-nav.css` `.board-nav-wrap` `min-width: 0`; `erp-master-redesign.css` `body.hub-page` `header` / `footer` (changelog 443); 442 — `erp-master-redesign.css` `body.hub-page .grid` `min-width: 0` + `board-nav.css` `.board-nav-hint` `min-width: 0` (hub card grid + workspace strip hint flex shrink) (changelog 442); 441 — `erp-master-spec-2026.css` `.erp-connection-strip` — `min-width: 0` + `max-width: 100%` so the shared QuickBooks strip shrinks in flex layouts without horizontal page overflow (changelog 441); 440 — `erp-master-redesign.css` `body.erp-maintenance` `@media (max-width: 720px)` `.erp-topbar .title` — `min-width: 0` (parity with dispatch 439 wrapped topbar) (changelog 440); 439 — `erp-master-redesign.css` `body.dispatch-page` `@media (max-width: 720px)` `.erp-topbar .title` — `min-width: 0` with `flex: 1 1 auto` so the title can shrink in the wrapped topbar (changelog 439); 438 — `erp-master-redesign.css` `body.banking-page` + `body.settings-page` `.wrap` — `min-width: 0` (shrink in flex layout; parity with dispatch) (changelog 438); 437 — `erp-master-redesign.css` `body.dispatch-page` `.wrap` + `#dispatchApp` `.wrap.dispatch-board-wrap` — `min-width: 0` so load-board + board shells shrink inside flex layout without page-level horizontal overflow (changelog 437); 436 — `erp-master-redesign.css` `body.fuel-board` `.panel` — `overflow-x: auto` + touch scrolling (parity with banking / settings; wide fleet / map / ledger content on narrow viewports) (changelog 436); 435 — `erp-master-redesign.css` `body.banking-page` `.panel` — `overflow-x: auto` + touch scrolling (parity with settings `.panel`; wide bank-match tables on narrow viewports) (changelog 435); 434 — `app-theme.css` — drop unscoped `.vendor-link-grid > *` (`min-width: 0`) — duplicate of `body.erp-maintenance .vendor-link-grid > *` (changelog 430); catalog grid only used under maintenance shell (changelog 434); 433 — `erp-master-spec-2026.css` / `maint-accounting-ui-2026.css` / `erp-master-redesign.css` — `1fr` → `minmax(0, 1fr)` on single-column grids + `erp-dedicated-form-modal` `grid-template: minmax(0, 1fr) / minmax(0, 1fr)` (+ New ≤720px, reports hub ≤520px / dense tiles, AP/fuel grids ≤720px, WO `.maint-wo-form-split`, fuel `.route-inputs` / `.layout` ≤1450px, dispatch `.grid2` ≤720px, safety `.safety-active-grid` ≤900px) (changelog 433); 432 — `app-theme.css` — remove redundant `min-width: 0` on `.asset-cat-tile` and `.maint-form-grid__row--3 > *` (superseded by 431 `.asset-cat-grid > *` / `.maint-form-grid__row > *`) (changelog 432); 431 — `app-theme.css` `body.erp-maintenance` `.toolbar` / `.search-row` / `.unit-summary` + `.wo-line-grid` / `.asset-cat-grid` / `.maint-form-grid__row > *` — `min-width: 0` (changelog 431); 430 — `app-theme.css` `body.erp-maintenance` `.grid.grid-2 > *` / `.grid.grid-3 > *` / `.grid.grid-4 > *` + `.vendor-link-grid > *`; `maint-accounting-ui-2026.css` `.acct-entry-list--uniform > *` + `.acct-dash-tools-grid > *`; `erp-master-spec-2026.css` `.erp-reports-hub__tiles--dense > *` — `min-width: 0` (changelog 430); 429 — `maint-accounting-ui-2026.css` `#maintWorkOrderFormMount` `.maint-wo-form-split` (≥1100px) notes column `minmax(min(180px,100%), minmax(0, 0.32fr))`; `erp-master-spec-2026.css` `.mr-kpi-strip > *` + `erp-master-redesign.css` `.maint-kpi-cards > *` + `body.hub-page` `.grid > *` — `min-width: 0` so KPI + hub auto-fit grids can shrink; `erp-master-redesign.css` `body.settings-page` `.panel` `overflow-x: auto` for wide admin tables (changelog 429); 428 — `app-theme.css` `@container maint-wo-modal` `.maint-form-grid__row--3` / `.unit-summary` / `.search-row` + `erp-master-spec-2026.css` `.mr-kpi-strip` + `maint-accounting-ui-2026.css` `.acct-entry-board` / `.acct-entry-list--uniform` — auto-fit/auto-fill `1fr` → `minmax(0, 1fr)` so tracks shrink without implicit min-content overflow (changelog 428); 427 — `erp-master-redesign.css` `body.fuel-board` `.layout > *` + `body.banking-page` `.metrics > *` — `min-width: 0` so fuel 3-column shell + banking metric tiles respect grid track shrinking (changelog 427); 426 — `erp-master-redesign.css` `body.erp-maintenance` `.erp-new-menu__tile-grid` + `body.fuel-board` `.fleet-columns` / `.detail-grid` — `repeat(2, minmax(0, 1fr))` + `min-width: 0` on grid children / `.fleet-col` so two-column shells do not force horizontal overflow (changelog 426); 425 — `scripts/system-smoke.mjs` — clearer console hint when `GET /api/__smoke_not_found__` returns HTML (wrong port / stale listener): use this repo’s server + matching `SMOKE_BASE` (changelog 425); 424 — `erp-master-spec-2026.css` `#erpApp` `.erp-new-menu__cols` — `repeat(3, minmax(0, 1fr))` + direct-child `min-width: 0` so + New menu columns shrink without forcing horizontal scroll (changelog 424); 423 — `app-theme.css` `body.erp-maintenance` `.maint-board-layout` nav column `minmax(0, min(200px,100%))` (changelog 423); 422 — `app-theme.css` legacy `body.erp-maintenance` `.app` sidebar + `.split` / `.tire-layout-wrap` / `.track-grid` first tracks `minmax(0, min(Npx,100%))` vs fixed `px` (changelog 422); 421 — `maint-accounting-ui-2026.css` `#maintWorkOrderFormMount` `.maint-wo-form-split` (≥1100px) notes column `minmax(min(180px,100%),0.32fr)` (changelog 421); 420 — `app-theme.css` `body.erp-maintenance` `.maint-wo-workspace-grid` / `.maint-wo-split` aside columns `minmax(min(220px|260px,100%),…)` so WO panes narrower than fixed mins do not force grid overflow (changelog 420); 419 — `erp-master-redesign.css` `body.dispatch-page` load-board `.grid2` / `.stop-card-top` (≤900px) / `.invoice-extra-row` (≤720px) / `.stop-card-bottom` — `repeat(*, minmax(0, 1fr))` + direct-child `min-width: 0` so tracks shrink without horizontal overflow (changelog 419); 418 — `erp-master-spec-2026.css` `body.erp-maintenance` `.mr-kpi-strip` `minmax(min(180px,100%),1fr)` narrow-viewport overflow (changelog 418); 417 — `erp-master-redesign.css` dispatch `.stop-card-top` + `.invoice-extra-row` grid tracks clamp with `min(...,100%)` to reduce horizontal overflow on narrow panes (changelog 417); 416 — `erp-master-redesign.css` `body.banking-page` `.metrics` + `body.fuel-board` `.layout` grids use `min(...,100%)` in `minmax` so tracks cannot force horizontal scroll (changelog 416); 415 — `erp-master-redesign.css` `body.erp-maintenance` `.maint-kpi-cards` `minmax(min(11.25rem,100%),1fr)` narrow-viewport overflow (changelog 415); 414 — `erp-master-redesign.css` `body.erp-maintenance` `.erp-topbar.topbar` top + horizontal padding use `env(safe-area-inset-*)` (parity with dispatch shell) (changelog 414); 413 — `erp-master-redesign.css` `#dispatchApp` `.erp-topbar.topbar` top padding respects `env(safe-area-inset-top)` (loads board header) (changelog 413); 412 — `erp-master-redesign.css` `body.hub-page` `.grid` uses `minmax(min(280px, 100%), 1fr)` so card columns cannot overflow narrow viewports (changelog 412); 411 — `erp-master-redesign.css` `body.dispatch-page` `.topbar` top padding respects `env(safe-area-inset-top)` (changelog 411); 410 — `erp-master-redesign.css` `body.hub-page` header top padding respects `env(safe-area-inset-top)` (changelog 410); 409 — `public/index.html.save` removed (was web-servable via `express.static`); `.gitignore` `public/*.save` (changelog 409); 408 — `ERP_MASTER_REDESIGN_PARALLEL_AGENT.md` §2 — smoke lists STATIC_TEXT shells (`design-tokens`, `erp-master-redesign`, `erp-master-spec-2026`, `board-nav`, `erp-ui`, `board-nav.js`) + `qa:automated` (changelog 408); 407 — `docs/AGENT_COORDINATION.md` — Agent B smoke asset list (`erp-master-redesign` + `erp-master-spec-2026` + …) + `qa:automated`; Agent 1 scope note (satellite inline `<style>` cleared) (changelog 407); 406 — `scripts/system-smoke.mjs` `STATIC_TEXT` — `GET /css/erp-master-spec-2026.css` + spec header needle; `README.md` + `ARCHITECTURE.md` smoke bullets (changelog 406); 405 — `scripts/system-smoke.mjs` `STATIC_TEXT` — `GET /css/erp-master-redesign.css` + needle `IH35 ERP — QuickBooks-style shell (UI only).` (changelog 405); 404 — `app-theme.css` file header — load order + no inline `<style>` on satellite pages; `npm run report:erp` → `docs/reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf` refresh (changelog 404); 403 — `maintenance.html` — remove empty page `<style>` (placeholder comment only; layout lives in `app-theme.css` / `erp-master` sheets) (changelog 403); 402 — `erp-master-redesign.css` `body.hub-page` — `index.html` hub (header / `.grid` / `a.card` / tags / footer / 900px); `index.html` — remove `<style>`, link `erp-master-redesign.css` (changelog 402); 401 — `erp-master-redesign.css` `body.fuel-board` — full `fuel.html` page `<style>` (layout, fleet, map, autocomplete, badges, relay/banners, `html.fuel-embed`); badge vars on `body`; controls use `--color-border-input`; `fuel.html` — remove page `<style>` (changelog 401); 400 — `erp-master-redesign.css` `body.banking-page` — merge `banking.html` `<style>` (shell, `label` / `row` / `.metrics` / `.metric` / `table`, `button:hover`, `.mini` / `.hidden`, 900px `ph`/`pb`); inputs use `--color-border-input`; `banking.html` — remove page `<style>` (changelog 400); 399 — `erp-master-redesign.css` `body.settings-page` — merge `settings.html` page `<style>` (layout, `#boardNavMount`, `label` / `table` / `.mini` / `.err` / `.hidden`, employees filter override, 900px `panel`); `settings.html` — remove duplicate `<style>` (changelog 399); 398 — `erp-master-redesign.css` `body.dispatch-page` dispatch vars + `line-height` + `box-sizing` + `.hidden` + `.maint-banner` + `.st-*`; `html.dispatch-embed` embed bar + chrome hide; `dispatch.html` — remove early `<style>` (load-board inline CSS fully cleared) (changelog 398); 397 — `erp-master-redesign.css` `body.dispatch-page` book-load `.grid2` / `.row` / `.in` (width + padding + border) / `.stops-head` / `.stop-block` / `.stop-card-*` / `.invoice-extra-*` / `.stop-miles-summary` (full + table); `textarea.stop-addr` scoped to `.stop-card-addr` + `--color-border-input`; `dispatch.html` load-board — drop inline form grid + stops + miles (changelog 397); 396 — `erp-master-redesign.css` `body.dispatch-page` `.modal-bg` / `.modal` / `.modal.book-modal-wide` / `.modal h2` / `.modal-body` / `.modal-actions` (load-board book modal shell); `dispatch.html` load-board — drop inline modal shell (changelog 396); 395 — `erp-master-redesign.css` `body.dispatch-page` `.ac-wrap` / `.ac-list` (full) / `.ac-list.on` / `.ac-item` / `.stops-actions`; `dispatch.html` load-board — drop inline autocomplete + stops-actions (changelog 395); 394 — `erp-master-redesign.css` `body.dispatch-page .btn` / `.btn-primary` / `.btn-ghost` (+ `:hover`); `dispatch.html` load-board `<style>` — drop inline `.btn` block (changelog 394); 393 — `erp-master-redesign.css` `body.dispatch-page .stop-miles-summary` — `border-radius: var(--radius-card)`; `dispatch.html` load-board — drop literal `10px` on `.stop-miles-summary`; `fuel.html` page `<style>` — `border-radius` `10px`/`8px` → `var(--radius-card)` on callouts / cards / password input (`design-tokens.css`) (changelog 393); 392 — `erp-master-redesign.css` `body.dispatch-page .ac-list` — `border-radius: var(--radius-input)`; `dispatch.html` load-board `.ac-list` — drop literal `8px`; `fuel.html` page `<style>` `button` / `input` — drop dead `border-radius` (`body.fuel-board button|input` in `erp-master-redesign`) (changelog 392); 391 — `fuel.html` `#fleetSummary` / `#fleetAssigned` / `#fleetUnassigned` + `#selectedDetail` + `#fuelLedger` / `#fuelLedgerMeta` + `#recommendedStops` — static Rule 22 leads (changelog 391); 390 — `banking.html` `#snapMeta` / `#snapMetrics`, `#importOut`, `#suggestOut` + `settings.html` `#usersBody` / `#empBody` — static `mini-note` + `erp-help-tip` leads (changelog 390); 389 — `banking.html` lead + CSV step `erp-help-tip`; `settings.html` Users / Employees `erp-help-tip` + employees `mini-note` — shorter Rule 22 copy (changelog 389); 388 — `maintenance.html` WO `#maintWoBanner` / `#maintWoBannerUnit` — `syncMaintWorkOrderHeader()` (changelog 388); 387 — `maintenance.html` Accounting AP `#apInvoicePdfStatus` — `parseApInvoicePdf()` `textContent` (changelog 387); 386 — `maintenance.html` WO `#maintSaveHint` / `#maintEditRecordHint` — `maintToneSaveHint()` / `updateMaintEditRecordHint()` (changelog 386); 385 — `maintenance.html` Accounting `#maintPostingPreflight` — `runPostingPreflight()` `textContent` (changelog 385); 384 — `maintenance.html` WO `#maintInvoicePdfStatus` — `parseMaintRecordInvoicePdf()` (changelog 384); 383 — `maintenance.html` WO `#maintCostLinesSubtotal` — `recalcMaintCostTotalFromLines()` (changelog 383); 382 — `maintenance.html` WO `#maintInvoiceCostSummary` — stub `updateMaintInvoiceCostCoordination()` (changelog 382); 381 — `maintenance.html` `#section-catalog` (service / QBO / vendors / operational / fleet) + `fuel.html` roadmap + write banner + Selected Unit `erp-help-tip` + diesel ledger + Relay callout — shorter Rule 22 copy (changelog 381); 380 — `maintenance.html` WO `#maintCatalogServiceHint` — static lead + `addCatalogServiceType()` `textContent` (changelog 380); 379 — `maintenance.html` WO `#maintCostTotalMatch` — `updateMaintCostTotalMatchUi()` `textContent` / `innerHTML` (changelog 379); 378 — `maintenance.html` WO `#recordLoadNumberHint` — `applyLoadNumberSuggestion()` / PDF parse `textContent` (changelog 378); 377 — `maintenance.html` WO `#repairLocationChips` — static `mini-note` + `erp-help-tip` above host — `renderRepairLocationChips()` `innerHTML` on `REPAIR_LOCATION_DEFS` (changelog 377); 376 — `maintenance.html` `#section-accounting` — Pay bills (open bills + bill payment log) + Fuel expenses (tab overview, manual fuel, cost lines, Relay import/preview, category vs item) + QuickBooks Live Master (title, status snapshot, create item/account, vendor/item/account columns) + Expense history + QBO rollback — shorter static `erp-help-tip` / `mini-note` (changelog 376); 375 — `maintenance.html` WO form — static `mini-note` + `erp-help-tip` above `#serviceMileageHint` — `updateServiceMileageHint` `innerHTML` / `textContent` (changelog 375); 374 — `maintenance.html` Reports hub — static `mini-note` + `erp-help-tip` above `#repRoadmapBanner` — `repReportsRoadmapMsg` / `openSection('reports')` `textContent` (changelog 374); 373 — `maintenance.html` `#section-fuel` embed planner `erp-help-tip` + Safety → HOS (`#safe-hos`) legend, intro `mini-note`, filter-chip `mr-filter-drop__hint` lines, grid lead + `renderSafetyHos` panel — shorter Rule 22 / DEFERRED §2 copy (changelog 373); 372 — `maintenance.html` Accounting → QuickBooks rollback — static `mini-note` + `erp-help-tip` above `#erpRollbackPostedSection` (bulk revert `innerHTML`); restores Rule 22 stacks above `#erpImportBatchesBody` / `#fuelImportBatchesBody` (changelog 372); 371 — `maintenance.html` Accounting → QuickBooks rollback — static `mini-note` + `erp-help-tip` above `#erpImportBatchesBody` / `#fuelImportBatchesBody` — `loadErpImportBatchesUi()` `innerHTML` (changelog 371); 370 — `maintenance.html` `#section-maintenance` — KPI strip, fleet pills + QBO strip, Units / header / operational sheet, WO form (planned work, cost mode, QBO header, tire + accident tips), Imports, R&amp;M Status, Shop / location, Maintenance Table + Unit history — shorter `mini-note` / `erp-help-tip` / chip hints (changelog 370); 368 — `maintenance.html` Tracking (fleet-mix KPI `erp-help-tip`, `#tr-maint-shop` nav + shop queues + Integrity · fuel · parts + Map + Assets `mini-note` / `erp-help-tip`) + Safety (`#safe-active`, `#safe-assign`, `#safe-fleet`, `#safe-files`) `erp-help-tip` bodies — shorter Rule 22 / DEFERRED §2 copy (changelog 368); 367 — `maintenance.html` Home dashboard + Safety → HOS filter hints + grid `erp-help-tip` + Accounting AP attachments / saved AP / Pay bills composer tips — shorter Rule 22 / DEFERRED §2 copy (changelog 367); 366 — `maintenance.html` Accounting → Live QuickBooks Master — static leads + `erp-help-tip` above `#qboVendorList` / `#qboItemList` / `#qboAccountList` (`renderQboMaster` `innerHTML`, first 50 per column) (changelog 366); 365 — `maintenance.html` Shell chrome — module sidebar + create drawer + auth banner + QBO sync bar + global search + topbar nav `erp-help-tip`; Settlement trip lookup / driver pay / cost index `mini-note` + tips — shorter Rule 22 copy (changelog 365); 364 — `maintenance.html` Accounting → Expense history — static lead + `erp-help-tip` above `#expHistBody` (`renderExpenseHistory` / `buildExpenseHistoryRows` / `filterExpenseHistoryRows` `innerHTML` + `#expHistPagerHost`) (changelog 364); 363 — `maintenance.html` Reports → Maintenance detailed — static lead + `erp-help-tip` above `#repMaintDetailBody` (`renderMaintDetailedReport` `innerHTML` + `#repMaintDetailPagerHost`) (changelog 363); 362 — `maintenance.html` Lists → catalogs `#section-catalog` (QBO / vendors / operational / fleet) `mini-note` + `erp-help-tip`; `renderErpModuleSidebar` Lists strip + Reports hub + Accounting Connections lead; `erpNewPlaceholder` drawer — shorter DEFERRED §2 copy (changelog 362); 361 — `maintenance.html` Accounting → Pay bills — static lead + `erp-help-tip` above `#bpOpenBillsBody` (`loadAccountingOpenBills` / `bpMountOpenBillsInTable` / `bpRenderOpenBillsPage` `innerHTML` + `#bpOpenBillsPagerHost`) (changelog 361); 360 — `maintenance.html` Reports → Team `loadTeamSecurityPanel` + WO `#maintAssetStatusPanel` fleet roster `mini-note` + `updateServiceMileageHint` + Accounting Relay fuel import `erp-help-tip` / checkbox — shorter Rule 14 / DEFERRED §2 copy (changelog 360); 359 — `maintenance.html` Accounting → Recent bill payments — static lead + `erp-help-tip` above `#bpLogBody` (`renderBpBillPaymentLogTable` / `bpRenderBillPaymentLogPage` `innerHTML` + `#bpLogPagerHost`) (changelog 359); 358 — `maintenance.html` WO workspace help + `renderReportsAll` / `renderRepMaintSpendByUnitTable` `mini-note` trims (changelog 358); 357 — `maintenance.html` Reports → Settlement → Driver pay settlements — static lead + `erp-help-tip` above `#driverPaySettlementsBox` (`loadDriverPaySettlements` / `renderDriverPaySettlementsBox` `innerHTML`) (changelog 357); 356 — `maintenance.html` Shop queues + Safety → HOS + WO service mileage — shorter `innerHTML` / empty-state / `erpNotify` copy (`renderShopQueues`, `renderSafetyHos`, `updateServiceMileageHint`, `fillServiceMileageFromSamsara`) (changelog 356); 355 — `maintenance.html` Upload center (`#section-uploads`) — Rule 22 `erp-help-tip` + `mini-note` trims; `uploadCenterFuelPreview` / `upImportLogRender` / `uploadMaintenanceFile` / `uploadApFile` / `uploadCenterRunOtherStub` shorter result `innerHTML` + import-log detail strings (changelog 355); 354 — `maintenance.html` Reports → Settlement / P&amp;L → Loads with recorded costs — static lead + `erp-help-tip` above `#settlementIndexBody` (`loadSettlementIndex` tbody + `#settlementLoadHints` datalist) (changelog 354); 353 — `maintenance.html` Reports hub (`#section-reports`) — Rule 22 `erp-help-tip` on toolbar + tab bodies (Overview, TMS, Settlement incl. driver pay + index, Maint spend/detail, QBO, Sync, Team, IFTA) (changelog 353); 352 — `maintenance.html` WO form → Planned work — static `mini-note` + `erp-help-tip` above `#maintPlannedServicesHost` (`resetMaintPlannedServiceRows` / template clone) (changelog 352); 351 — `maintenance.html` Reports → Spend by unit — static lead + `erp-help-tip` above `#repMaintBody` (`renderRepMaintSpendByUnitTable` `innerHTML`) (changelog 351); 350 — `maintenance.html` Maintenance action strip → QBO indicator — static `mini-note` + `erp-help-tip` above `#maintTopQboIssuesInd` (`renderMaintAsideQbIssues` `innerHTML`) (changelog 350); 349 — `maintenance.html` Accounting module sidebar → Connections — static lead + `erp-help-tip` sibling before `#erpAcctSbConnectionsBody` in `renderErpModuleSidebar('accounting')` template (`paintAcctSidebarConnections` `innerHTML`) (changelog 349); 348 — `maintenance.html` Lists &amp; catalogs (`#section-catalog`) — Rule 22 `erp-help-tip` on panel title + Service types / QuickBooks / Vendors / Operational / Fleet &amp; Samsara sub-panels (incl. `refreshQboMaster`, Samsara POST rename hints) (changelog 348); changelog 347: `maintenance.html` Safety → HOS clocks + Active / in service — Rule 22 `mr-filter-drop__hint` text + `erp-help-tip` (`#safe-hos` duty legend `?`, HOS grid technical `?`, active/in-service split `?`, In service now column `?`) (changelog 347); changelog 346: `maintenance.html` Accounting board → QuickBooks sync — static lead + `erp-help-tip` above `#acctDashQboErrList` (`paintAcctDashQboErrors` `innerHTML`) (changelog 346); 345 — `maintenance.html` Main column → QBO sync alert bar — static lead + `erp-help-tip` above `#qboSyncAlertBar` (`renderQboSyncAlerts` / `dismissQboSyncAlertBar` `innerHTML`) (changelog 345); 344 — `maintenance.html` Maintenance → Shop (under Tracking → Shop & maintenance) — Rule 22 `erp-help-tip` / leads: External / Roadside / Parts queues + OOS `?`; Unit history technical `?`; Maintenance reports + Integrity · fuel · parts title `?` + integrity lead `mini-note` — shorter copy (changelog 344); 343 — `maintenance.html` Topbar → Auth banner — static lead + `erp-help-tip` above `#authBanner` (`j()` 401 `innerHTML`, `loadAll`) (changelog 343); 342 — `maintenance.html` Create drawer — static lead + `erp-help-tip` above `#erpDrawerBody` (`openErpDrawer`, `openAddNewDrawer`) (changelog 342); 341 — `maintenance.html` Tracking → Map / Assets / Drivers / Summary + Shop → Integrity alerts — Rule 22 `erp-help-tip` (`#tr-map` unit-card `?`, `#tr-assets` title + grid `?`, `#tr-drivers` title + table `?`, Summary `#trackingSummaryText` technical `?`; Tracking → Shop integrity band before `#securityAlertsBody`) (changelog 341); 340 — `maintenance.html` Left rail → Module sidebar — static lead + `erp-help-tip` above `#erpModuleSidebarInner` (`renderErpModuleSidebar`, `erpSbGroup`) (changelog 340); 339 — `maintenance.html` Topbar → Global search — static lead + `erp-help-tip` above `#globalSearchMenu` (`globalSearchRender`, `globalSearchCollect`) (changelog 339); 338 — `maintenance.html` Tracking → Shop & maintenance + Yard & idle — Rule 22 `erp-help-tip` (`#tr-maint-shop` nav panel, Shop tabs tip, Internal shop row; `#tr-idle` band / geofence / idle-snapshot `?` panels) (changelog 338); 337 — `maintenance.html` Reports → Maintenance detail — static lead + `erp-help-tip` above `#repMaintDetailBody` (`renderMaintDetailedReport`, `erp.records`, `repMaintDetailPager`) (changelog 337); 336 — `maintenance.html` Safety — Rule 22 Assignments / Fleet snapshot / Driver files `mini-note` + `erp-help-tip` (`safe-assign`, `safe-fleet`, `safe-files`); 335 — `maintenance.html` Reports → Team security — static lead + `erp-help-tip` above `#repTeamBody` (`loadTeamSecurityPanel`, `GET /api/security/posture`) (changelog 335); 334 — `maintenance.html` Reports → Sync compare — static lead + `erp-help-tip` above `#repSyncBody` (`renderReportsAll`, `reportsSummaryCache.posting`) (changelog 334); 333 — `maintenance.html` Reports → QBO activity — static lead + `erp-help-tip` above `#repQboBody` (`renderReportsAll`, `transactionActivity`) (changelog 333); 332 — `maintenance.html` Reports → Overview — static lead + `erp-help-tip` above `#repOverviewBody` (`loadReportsData`, `renderReportsAll`) (changelog 332); 331 — `maintenance.html` Home + Safety → HOS clocks — Rule 22 (`#section-dashboard` Reports hero + System / Sync Summary `erp-help-tip`; HOS intro `mini-note` + lead + Active grid lead); 330 — `maintenance.html` Upload center — Rule 22 `#section-uploads` `erp-help-tip` + short `mini-note` (panel, bank, Comdata, Connections, fuel, AP, maint history, other); 329 — `maintenance.html` Reports → TMS pipeline — static lead + `erp-help-tip` above `#repTmsBody` (`renderReportsAll`, `reportsSummaryCache.tms`) (changelog 329); 328 — `maintenance.html` Reports → Settlement rollup — static lead + `erp-help-tip` above `#settlementSummary` (`runSettlementLookup`, `GET /api/settlement/by-load/`, `#settlementLinesBody`, `#settlementTmsBox`) (changelog 328); 327 — `maintenance.html` Lists & catalogs — Rule 22 `#section-catalog` `mini-note` / `erp-help-tip` trims (QuickBooks, vendors, operational, Fleet & Samsara, service admin); 326 — `maintenance.html` WO form → Tire extras — static lead + `erp-help-tip` above `#tireExtraLinesHost` (`addTireExtraLine`, `clearTireExtraLines`, `#tireExtraLineTpl`, `collectTireLineItems`) (changelog 326); 325 — `maintenance.html` Tracking → Summary — static lead + `erp-help-tip` above `#trackingSummaryText` (`renderTrackingBoard`, `vehicles`, `board` counts) (changelog 325); 324 — `maintenance.html` Tracking → Map detail — static lead + `erp-help-tip` above `#trackingDetailBox` (`renderTrackingBoard`, `selectedTrackingVehicle`) (changelog 324); 323 — `maintenance.html` Maintenance → Fleet pills — static lead + `erp-help-tip` row above `#maintActionFleetPills` (`renderMaintenanceCategoryChips`, `setAssetCategory`) (changelog 323); 320 — `maintenance.html` Reports → Settlement + Tracking → Yard & idle — Rule 22 copy (`rep-settlement` band + `erp-help-tip` bodies, `runSettlementLookup` / `loadSettlementIndex` / `renderDriverPaySettlementsBox` empty + no-match strings, `applySettlementMilesToTms` warning, idle grid lead `mini-note`) (changelog 320); 319 — `maintenance.html` Maintenance → Unit history — static lead + `erp-help-tip` above `#unitHistory` (`renderUnitHistory`, `erp.records`, `selectedUnit`, `innerHTML`) (changelog 319); 318 — `maintenance.html` WO workspace — static lead + `erp-help-tip` above `#maintWoSummaryBar` (`renderMaintAsideWoSummary`, `#recordWorkOrderInput`, `#costInput`, `#repairLocationSearch`, `#maintAsideWoSummary`, hidden without `selectedUnit`) (changelog 318); 317 — `maintenance.html` WO workspace — static lead + `erp-help-tip` above `#maintAssetHeaderCard` (`renderSelectedUnit`, `#maintAssetHeaderCard` `innerHTML`, `#maintAssetStatusPanelBody`, fuel–PM strip) (changelog 317); 316 — `maintenance.html` Maintenance → Shop / location — static lead + `erp-help-tip` above `#maintLocationBoardHost` (`renderMaintLocationBoard`, `shopQueueCache`, `erp.records`) (changelog 316); 315 — `maintenance.html` R&M Status → Integrity alerts — static lead + `erp-help-tip` above `#maintSecurityAlertsBody` (`loadSecurityCatalogUi`, `GET /api/maintenance/security-alerts`, `renderSecurityAlerts`, `maintSecurityAlertRenderOpts`) (changelog 315); 314 — `maintenance.html` Units list — static lead + `erp-help-tip` above `#maintenanceUnitList` (`renderMaintenanceUnitList`, `vehiclesByCategory`, `#maintenanceUnitSearch`, `selectUnit`) (changelog 314); 313 — `maintenance.html` Safety → Fleet snapshot — static lead + `erp-help-tip` above `#safetyFleetSummary` (`renderSafetyFleet`, `vehicles`, `board.vehicles`, `board.hos`, `openSafetyTab('safe-fleet')` refresh) (changelog 313); 312 — `maintenance.html` Maintenance → Fleet table — static lead + `erp-help-tip` above `#maintenanceTableBody` (`renderMaintenanceTable`, `dashboard`, `activeAssetCategory`, `#maintenanceServiceFilter`, `maintDashboardTablePager`) (changelog 312); 311 — `maintenance.html` Shop board → internal / external / roadside / parts — static lead + `erp-help-tip` before `#shopInternalTableBody` / `#shopExternalTableBody` / `#shopRoadsideTableBody` / `#shopPartsTableBody` (`renderShopQueues`, `shopQueueCache`, `GET /api/maintenance/shop-queue`, `shopQueuePager`) (changelog 311); 310 — `maintenance.html` Tracking → Map unit cards — static lead + `erp-help-tip` above `#trackingList` (`renderTrackingBoard`, `vehicles`, `trackingListPager`, `#trackingSearch`, `selectTrackingVehicle`) (changelog 310); 309 — `maintenance.html` Tracking → Yard & idle — static lead + `erp-help-tip` above `#idleSnapshotBody` (`loadIdleSnapshot`, `GET /api/tracking/idle-snapshot`, `renderIdleSnapshotTableBody`, `idleSnapshotPager`, `#idleSnapshotMeta`) (changelog 309); 308 — `maintenance.html` Tracking → Drivers — static lead + `erp-help-tip` above `#trackingDriversBody` (`renderTrackingDrivers`, `board.hos`, `normalizeHosClock`, `applySafetyHosDutyMode`, `trackingDriversPager`) (changelog 308); 307 — `maintenance.html` Tracking → All tracked assets — static lead + `erp-help-tip` above `#trackingAssetsTableBody` (`renderTrackingAssetsTable`, `vehicles`, `trackingAssetsTablePager`, `#trackingAssetFilter`) (changelog 307); 306 — `maintenance.html` Safety → Active / in service — static lead + `erp-help-tip` above `.safety-active-grid` (`renderSafetyActivePanels`, `#safetyActiveBody` / `#safetyInServiceBody` `innerHTML`, `safetyActivePager` / `safetyInServicePager`) (changelog 306); 305 — `maintenance.html` Safety → HOS clocks — static lead + `erp-help-tip` above `#safetyHosBody` (`renderSafetyHos`, `board.hos`, `safetyHosPager`, `#safetyHosMeta`) (changelog 305); 304 — `maintenance.html` Safety → Assignments — static lead + `erp-help-tip` above `#safetyAssignBody` (`renderSafetyAssignments`, `board.assignments`, `safetyAssignPager`) (changelog 304); 303 — `maintenance.html` Safety → Driver files — static lead + `erp-help-tip` above `#driverFilesBody` (`renderDriverFiles` `innerHTML`, `POST /api/safety/driver-profiles`) (changelog 303); 302 — `maintenance.html` Shop → Out of service units — static lead + `erp-help-tip` above `#shopOosBody` (`renderShopQueues` `erp.records` OOS filter) (changelog 302); 301 — `maintenance.html` WO → Operational status sheet (`#maintAssetStatusPanel`) — static lead + `erp-help-tip` above `#maintAssetStatusPanelBody` (`renderSelectedUnit` `innerHTML`, `saveAssetStatus` / `saveAssetFleetActive`) (changelog 301); 300 — `maintenance.html` Lists → Fleet & Samsara — static lead + `erp-help-tip` before rename POST form (`POST /api/integrations/samsara/vehicle|driver/<id>`) (changelog 300); 299 — `maintenance.html` Lists → QuickBooks items & accounts — static lead + `erp-help-tip` before Refresh / Live master (`refreshQboMaster` → `GET /api/qbo/master`, `erp.qboCache`) (changelog 299); 298 — `erp-master-redesign.css` `body.dispatch-page .link` — `color` `var(--color-border-focus)` (match topbar links); `textarea.stop-addr` — `border-color` / `border-radius` (`var(--radius-input)`); `dispatch.html` load-board `<style>` — drop inline `.link` + dead `stop-addr` `border-radius` / `font-family` (changelog 298); 297 — `maintenance.html` Shop → Integrity · fuel · parts alerts — static lead + `erp-help-tip` above `#securityAlertsBody` (`renderSecurityAlerts` `innerHTML`, `integritySearch` / `integritySeverity`) (changelog 297); 296 — `erp-master-redesign.css` `body.dispatch-page label.lbl` — uppercase field labels (`display` / `font-size` / `font-weight` / `color` / `margin` / `text-transform`); `dispatch.html` load-board `<style>` — remove duplicate `label.lbl` block (changelog 296); 295 — `maintenance.html` Lists → Service types (DB) — static lead + `erp-help-tip` above `#serviceCatalogAdminBody` (`renderServiceCatalogAdmin` `innerHTML`); placeholder Not loaded yet. (changelog 295); 294 — `erp-master-redesign.css` `body.dispatch-page .muted` — add `font-size: 12px`; `.modal h2` — add `font-size: 1.05rem` + `letter-spacing: -0.02em`; `dispatch.html` load-board `<style>` — remove redundant `.muted` + `.modal h2` type rules; `body.banking-page th` — `font-weight: 500` + `banking.html` inline `th` rule removed (changelog 294); 293 — `maintenance.html` Lists → Operational status — static lead + `erp-help-tip` above `#operationalCatalogBody` (`loadOperationalCatalogPanel` `innerHTML`) (changelog 293); 292 — `maintenance.html` Lists → Vendors & drivers — static lead + `erp-help-tip` above `#vendorCatalogBody` (`loadVendorCatalogPanel` still replaces `innerHTML`); placeholder Not loaded yet. (changelog 292); 291 — `dispatch.html` load-board `<style>` (before `erp-master-redesign.css`) — `body.dispatch-page` keep `line-height` only — drop dead `margin` / `font-size` (`erp-master-redesign.css` `body.dispatch-page`); `.modal` drop dead `border-radius` / `box-shadow`; `.modal h2` drop dead `font-weight` / gradient `background`; `input.in` / `select.in` drop dead `border-radius` (`body.dispatch-page .modal*` / `input.in|select.in`) (changelog 291); 290 — `banking.html` page `<style>` — `textarea` / `button` drop dead `border-radius` / `background` on `button`; remove dead `button.primary` (+ `:hover`) (`erp-master-redesign.css` `body.banking-page button*`); `settings.html` — `input` / `button` drop dead `border-radius` / `font-family` / `font-weight` (`body.settings-page input|button`) (changelog 290); 289 — `maintenance.html` Lists & catalogs → New QuickBooks vendor — `#qboNewVendorCatalogHint` status `<span>` + `erp-help-tip`; `createQboVendorFromCatalogForm` `textContent` unchanged (changelog 289); 288 — `banking.html` page `<style>` — `.ph` drop dead gradient + `font-weight`; `.metric` drop dead `background` + `border-radius`; `th` drop dead `background` + `color` (`erp-master-redesign.css` `body.banking-page .ph` / `.metric` / `th`); `settings.html` — remove dead `button.primary` (+ `:hover`); `a` keep `text-decoration` only (`body.settings-page a` / `button.primary*` in `erp-master-redesign`) (changelog 288); 287 — `maintenance.html` Live Master — `#newItemHint` / `#newAccountHint` status `<span>` + `erp-help-tip` (QBO create item / account); `createQboItem` / `createQboAccount` `textContent` unchanged (changelog 287); 286 — `banking.html` + `settings.html` page `<style>` — drop dead `body` / `.wrap` / `h1` / `p.lead`|`p` / `.panel` / `banking` `.pb` ink + chrome superseded by `erp-master-redesign.css` `body.banking-page*` / `body.settings-page*` — keep `min-height`, `banking` `.wrap` centering, `p.lead` / `p` rhythm, `settings` `.panel` padding + margins (changelog 286); 285 — `maintenance.html` Accounting → Pay bills — `#bpSelectionTotal` status `<span>` + `erp-help-tip` (draft total vs row caps); `bpRefreshSelectionTotal` `textContent` unchanged (changelog 285); 284 — `fuel.html` page `<style>` — remove dead `.fleet-item:hover` / `.fleet-item.active` (`erp-master-redesign.css` `body.fuel-board .fleet-item:hover|active` already supplies hover + selected fills) (changelog 284); 283 — `fuel.html` page `<style>` — `body` `margin:0` only — drop dead `font-family` / `background` / `color` (`app-theme.css` + `erp-master-redesign.css` `body.fuel-board`); `.topbar` drop dead `background` / `border-bottom` (`erp-master-redesign.css` `body.fuel-board .topbar`); `.fleet-summary` / `.fleet-section-title` drop dead `background` / `color` (`erp-master-redesign.css` `body.fuel-board .fleet-*`) — keep layout / borders / sticky (changelog 283); 282 — `maintenance.html` Accounting → QuickBooks Live Master — `#qboMasterStatus` status `<span>` + `erp-help-tip` (cache summary vs column cap); `renderQboMaster()` `textContent` unchanged (changelog 282); 281 — `fuel.html` page `<style>` — `.panel` keep `overflow:hidden` only — drop dead `background` / `border` / `border-radius` (`app-theme.css` + `erp-master-redesign.css` `body.fuel-board .panel`) (changelog 281); 280 — `maintenance.html` Accounting → Pay bills — `#bpOpenBillsStatus` status `<span>` + `erp-help-tip` (QBO + sidebar Refresh); `loadAccountingOpenBills` / `bpApplyOpenBillsFilter` / `bpLoadOpenBillByQuickId` unchanged `textContent`) (changelog 280); 279 — `fuel.html` page `<style>` — `.panel h2` remove dead `border-bottom` + `background` (`erp-master-redesign.css` `body.fuel-board .panel h2` supplies header bar + divider + `font-weight: 500`) — keep margin / padding / `font-size` (changelog 279); 278 — `fuel.html` page `<style>` — `.topbar h1` remove dead `font-size:20px` (`erp-master-redesign.css` `body.fuel-board .topbar h1` supplies `18px` / `500`) — keep margin only (changelog 278); 277 — `maintenance.html` Lists → Service types (DB) — `#catalogSectionServiceHint` status-only `<span>` ( `addCatalogServiceFromCatalogsSection` `textContent` ); API / `DATABASE_URL` / admin note → `erp-help-tip`) (changelog 277); 276 — `banking.html` + `settings.html` page `<style>` — remove redundant `h1` `font-weight: 700` (`erp-master-redesign.css` `body.banking-page|settings-page h1` already 500); `banking.html` `.metric .v` 700 → 500 (changelog 276); 275 — `maintenance.html` Maintenance → R&M Status — `erp-help-tip` for Refresh alerts / lookback; `#maintSecurityAlertsBody` initial Loading… (renderSecurityAlerts still owns `innerHTML`) (changelog 275); 274 — `board-nav.css` `.board-nav-strip-toggle`; `fuel.html` `.section-title` + topbar Home + roadmap Accounting → Fuel link; `dispatch.html` load-board `.modal h2` / `label.lbl` / trip-miles header; `banking.html` suggest-card JS header — `font-weight` 700 → 500 (8 — Rule 1) (changelog 274); 273 — `maintenance.html` Accounting → Expense history — live totals on `<span id="expHistSummary">`; Refresh data / stale-count guidance → `erp-help-tip`) (changelog 273); 272 — `index.html` hub `<style>` — `.card-tag` `font-weight` 800 → 500; `.arrow` 700 → 500 — Rule 1 parity with satellite ERP / `board-nav`) (changelog 272); 271 — `maint-accounting-ui-2026.css` Rule 1 — ≤900px Upload center `.mr-upload-tabbar` horizontal scroll + `nowrap` tabs; tighter `.mr-upload-panel-card` / `.mr-upload-drop` / `.mr-upload-conn-*` padding + grid gap) (changelog 271); 270 — `banking.html`, `settings.html`, `dispatch.html` load-board `<style>`, `fuel.html` topbar nav — `font-weight` 600 → 500 (10) — Rule 1 parity with `app-theme` 269) (changelog 270); 269 — `app-theme.css` `font-weight` 600 → 500 (23 — banking `button.primary`, catalog `.catalog-search-hit__name`, maintenance + dispatch chrome: topbar / tabs / hero / `.panel-title` / settlement `th` / `.maint-kpi-card__label` / `.maint-aside-kv span` / `.chip` / `.list-row` / `.primary` / uppercase `.mini`, `.qb-picker-item__label`, `.asset-cat-tile`, `.qb-split__main`, `.acct-fuel-import-table thead th`) — Rule 1 parity) (changelog 269); 268 — `erp-master-spec-2026.css` Rule 1 — ≤900px Reports hub: `#section-reports` `.erp-reports-columns` / `.erp-reports-sidebar` / `.erp-reports-hub*` tighter gutters + `#repMainSubtabs` horizontal scroll + safe-area insets) (changelog 268); 267 — `maintenance.html` Rule 22 — Accounting → QBO rollback duplicate intro `mini-note`s merged into `#erpRollbackPostedSection` lead; Reports → Executive overview muted line points to title `?`) (changelog 267); 266 — `maint-accounting-ui-2026.css` `.acct-dash-kpi__label`, `.mr-upload-recent-table th`, `.mr-upload-pill`, `.maint-qb-cost-details__badge`, `.maint-qb-lines-table thead th`, `.maint-qb-cost-totals-bar__piece--grand .maint-qb-cost-totals-bar__val` + `board-nav.css` `.board-nav-trigger` — `font-weight` 600 → 500 — Rule 1) (changelog 266); 265 — `maintenance.html` Rule 22 — Accounting board dash cards (Maintenance expenses, Fuel & DEF, QuickBooks sync, Tools & data) title-row `erp-help-tip` + shorter muted sublines) (changelog 265); 264 — `erp-master-spec-2026.css` `.bp-driver-pay-chip` `font-weight` 600 → 500 — Rule 1 parity with other ERP chrome) (changelog 264); 263 — `maintenance.html` Rule 22 — Upload Fuel / DEF export `<details>`; Maintenance AP / history template `<details>`; Connections Samsara write scopes — dense copy → short `mini-note` + `erp-help-tip`) (changelog 263); 262 — `erp-master-spec-2026.css` `#erpDedicatedFormModal` bar `.erp-dedicated-form-modal__title` / `__total-lbl` / `__total-amt` `font-weight` 600 → 500 — Rule 1 parity) (changelog 262); 261 — `maintenance.html` Rule 22 — Settlement → Driver pay duplicate `mini-note` removed (`DRIVER_SETTLEMENT_PAY_PCT` already in title ?); Upload Bank CSV + Comdata `<details>` → short `mini-note` + `erp-help-tip`) (changelog 261); 260 — `erp-master-spec-2026.css` `.erp-reports-hub__label` + `.erp-qb-cost-global-head__title` `font-weight` 600 → 500 (align Rule 1 with `.erp-new-menu__col-title`) (changelog 260); 259 — `+ New` `.erp-new-menu__sep-label` + `.erp-sb-sep__label` 600 → 500 (changelog 259); 258 — `erp-master-redesign.css` dispatch `@media print` `body.dispatch-page` / `#dispatchApp` `table.data` / `.badge` / `select.sm` — raw `#hex` → `var(--color-text-primary|bg-card|bg-header|border|border-input)`) (changelog 258); 257 — headline range + 256 clause de-dupe (changelog 257); 256 — `erp-master-spec-2026.css` solid-fill `erp-btn--busy` list adds `mr-upload-import-btn` (hyphen class in `maint-accounting-ui-2026.css`) + `maint-action-strip__btn--primary`; plus changelog 255: `erp-master-spec-2026.css` solid-fill `erp-btn--busy` list adds `acct-dash-btn--primary` + `mr-upload-importbtn` (green fills in `maint-accounting-ui-2026.css`) — proactive if `erpWithBusy` is wired later (changelog 255); plus changelog 254: `erp-master-spec-2026.css` one selector list for all solid-fill `erp-btn--busy::after` rings (`button.primary`, banking/settings, `btn--primary`, QB split primary, dispatch `btn-primary|erp-btn-primary`, maintenance `qb-btn-primary|erp-btn-primary`) — drops 253’s split + prior three duplicate blocks (changelog 254); plus changelog 253: `erp-master-spec-2026.css` merges 251 + 252 into one selector list (dispatch `#dispatchApp` `btn-primary|erp-btn-primary` + maintenance `qb-btn-primary|erp-btn-primary`) — same `border-color` / `border-top-color`, fewer duplicate lines (changelog 253); plus changelog 252: `erp-master-spec-2026.css` `body.erp-maintenance` `button.qb-btn-primary|erp-btn-primary.erp-btn--busy::after` → `var(--erp-btn-busy-on-solid-track)` + `var(--color-bg-card)` (same `button.primary` gap as dispatch 251 — AP `erpWithBusy`, shop `qb-btn-primary`, `+ New`) (changelog 252); plus changelog 251: `erp-master-spec-2026.css` dispatch `#dispatchApp` `button.btn-primary|erp-btn-primary.erp-btn--busy::after` → `var(--erp-btn-busy-on-solid-track)` + `var(--color-bg-card)` (accent primary buttons were not `button.primary` — busy ring matched body text spinner) (changelog 251); plus changelog 250: `design-tokens.css` adds `--erp-btn-busy-track|leading|on-solid-track` (`color-mix` on `--color-text-primary|bg-card`); `erp-master-spec-2026.css` `.erp-btn--busy::after` uses those vars instead of inline `color-mix` (changelog 250); plus changelog 249: `erp-master-spec-2026.css` `.erp-btn--busy::after` (default + primary / QB split / settings) drops duplicate `border` / `border-color` / `border-top-color` lines — dead `rgba(15,23,42,*)` / `rgba(255,255,255,*)` superseded by existing `color-mix` + `var(--color-text-primary|bg-card)` (~8 lines removed, behavior unchanged) (changelog 249); plus changelog 248: `design-tokens.css` adds `--font-sans` (same stack as `app-theme.css`) so `var(--font-spec-sans, var(--font-sans, …))` resolves on first paint when only `design-tokens.css` precedes an early `<style>` (e.g. `index.html` hub `body`) (changelog 248); plus changelog 247: `fuel.html` page `<style>` `:root` `--red|yellow|green` → `var(--pill-red-text|pill-yellow-text|pill-green-text)`; `fuelMapInk()` + `renderMap` Leaflet unit / stop / route / recommended colors read `design-tokens.css` via `getComputedStyle` (drop hardcoded `#dc2626` / `#d97706` / `#16a34a` / `#2563eb` / `#7c3aed` / `#0ea5e9`) (changelog 247); plus changelog 246: `erp-master-redesign.css` `:root` `--qb-amber` literal `#f9ab00` → `var(--color-semantic-warn-accent)` (legacy `--qb-amber` bridge matches `maint-accounting-ui-2026.css` 245 + `design-tokens.css`) (changelog 246); plus changelog 245: `maint-accounting-ui-2026.css` drops `var(--qb-green|qb-red|qb-amber)` → `var(--btn-primary-save-bg)` / `var(--color-semantic-error)` / `var(--color-semantic-warn-accent)` (19 — upload/accounting/WO chrome save / error / “soon” PD tint) (changelog 245); plus changelog 244: `erp-master-spec-2026.css` rule bodies drop last `var(--qb-line|qb-panel|qb-blue)` → `var(--color-border|color-bg-card|color-border-focus)` (3 — dashboard `.hero-card--hint:hover`, banking/settings control `focus`) (changelog 244); `erp-master-redesign.css` `font-family` long literal stack (5) → `var(--font-spec-sans, var(--font-sans, system-ui, sans-serif))` (match `app-theme.css` / hub 243) (changelog 244); plus changelog 243: `index.html` hub `<style>` `body` `font-family` → `var(--font-spec-sans, var(--font-sans, system-ui, sans-serif))` (was explicit `"Segoe UI"` middle fallback — aligns company hub with fuel / settings / `app-theme.css`) (changelog 243); plus changelog 242: `app-theme.css` `font-family: var(--font-sans)` → `var(--font-spec-sans, var(--font-sans, …))` on `body`, `.banking-page .wrap`, `body.dispatch-page` / `body.fuel-board` (3) — global default prefers Inter stack when `design-tokens.css` linked (changelog 242); plus changelog 241: `settings.html` page `<style>` `input` / `button` `font-family` → `var(--font-spec-sans, var(--font-sans, …))` (match `fuel.html` 240 + `erp-master-redesign`) (changelog 241); plus changelog 240: `fuel.html` page `<style>` `body` `font-family` → `var(--font-spec-sans, var(--font-sans, …))` (align fuel with `erp-master-redesign` / `board-nav` / `design-tokens`) (changelog 240); plus changelog 239: `design-tokens.css` adds `--focus-ring-color`; `--focus-ring` → `0 0 0 2px var(--focus-ring-color)` (single rgba source); `erp-master-redesign.css` `:root` `--qb-focus-ring` → `var(--focus-ring-color)` (drop duplicate literal) (changelog 239); plus changelog 238: `design-tokens.css` `--focus-ring` alpha 0.1 → 0.12 (match ERP shell); `erp-master-redesign.css` + `erp-master-spec-2026.css` `box-shadow` `0 0 0 2px var(--qb-focus-ring)` (or `--focus-ring` comma tail) → `var(--focus-ring)` (12); `:root` keeps `--qb-focus-ring` for legacy reference (changelog 238); plus changelog 237: `erp-master-redesign.css` `:root` `--qb-radius-card|control` → `var(--radius-card|input)`; maintenance / dispatch / fuel / banking / settings body `font-family` → `var(--font-spec-sans, …)` (5) — matches `design-tokens.css` + `board-nav` (changelog 237); plus changelog 236: `erp-master-redesign.css` rule bodies read `design-tokens` roles directly — `var(--qb-line|line-input|panel|page|blue|green|icon-nav-bg|sidebar-active-bg|radius-*)` → `var(--color-border|color-border-input|color-bg-card|color-bg-page|color-border-focus|btn-primary-save-bg|color-nav-bg|pill-blue-bg|radius-card|radius-input)` (~200 call sites); `#qboSyncAlertBar .qbo-sync-head` `var(--pill-red-text)` only; `:root` `--qb-*` bridges kept for compatibility (changelog 236); plus changelog 235: `erp-master-redesign.css` `color` / `border-color` stacks drop `var(--qb-text|text-strong|muted|text|icon-nav-muted)` hops on `--color-text-*` + `--color-nav-inactive` (63); `board-nav.css` light `.board-nav-dd` `border` / row dividers → `var(--color-border)` only (2) (changelog 235); plus changelog 234: `erp-master-spec-2026.css` `color` stacks `var(--color-text-label|primary|body, var(--qb-muted|--text|--muted))` → bare `var(--color-text-*)` only (9) — legacy `--text` / `--muted` / `--qb-*` hops redundant when `design-tokens.css` loads first (changelog 234); plus changelog 233: `erp-master-spec-2026.css` `var(--color-border, var(--line|qb-line))` → `var(--color-border)` only (9 — `design-tokens.css` always linked first on maintenance / dispatch / fuel / hub spec consumers) (changelog 233); plus changelog 232: `erp-master-redesign.css` maintenance `.maint-modal-bg` (standard modals) + `.maint-workorder-fullmodal.maint-modal-bg` (WO full-sheet) `background` → `var(--color-modal-backdrop)` (drop `rgba(0,0,0,0.45)` — matches `design-tokens.css`) (changelog 232); plus changelog 231: `erp-master-spec-2026.css` `+ New` `.erp-new-menu` drops duplicate raw `box-shadow` line → `var(--shadow-menu-panel)` only; `.erp-toast` → `var(--shadow-toast)` only; `app-theme.css` `.erp-drawer` → `var(--shadow-drawer)` only (matches `design-tokens.css` literals) (changelog 231); plus changelog 230: `app-theme.css` `box-shadow` on hub grid card hover, `.qb-picker-menu`, `body.erp-maintenance .nav-dropdown-menu`, `.maint-save-toolbar--qb .qb-split__menu` → `var(--shadow-dropdown)` only (4); `board-nav.css` `body.hub-page .board-nav-dd` (1); `dispatch.html` `.ac-list` autocomplete (1) (changelog 230); plus changelog 229: `index.html` hub `<style>` — `.card-tag` `.tag-*` use bare `--pill-*` (and `.tag-banking` → `--pill-blue-bg|text`); `a.card:hover` `box-shadow` → `var(--shadow-dropdown)` only (changelog 229); plus changelog 228: `fuel.html` + `dispatch.html` page `<style>` — `--pill-*` / `--btn-danger-*` / `--btn-info-*`, `--color-warning-border-soft`, `--shadow-dropdown`, `--color-modal-backdrop` token-only (drop `#hex` / `rgba` comma tails on badges, banners, roadmap/relay callouts, autocomplete flyout, hub topbar links, delivered chip vars, `.modal-bg`) (changelog 228); plus changelog 227: `board-nav.css` `--nav-bg|nav-text|nav-line|nav-hover|nav-dd-bg|nav-dd-text` + `--sidebar-muted` — drop `#hex` comma tails (`app-theme.css` already defines `--nav-*`); strip-toggle / hub fallback stacks drop inner `#*` tails; undefined `--nav-text-muted` call sites → `var(--sidebar-muted)`; light `.board-nav-dd` `box-shadow` → `var(--shadow-dropdown)` (changelog 227); plus changelog 226: `erp-master-spec-2026.css` `--qb-focus-ring` only on maintenance `.btn` / `button` / field `focus` (3) + banking/settings field `focus` (1); `.mr-kpi-card--bad` `var(--pill-red-bg)`; `.erp-reports-roadmap` `border` → `var(--color-warning-border-soft)` + `background` `var(--pill-yellow-bg)` (was `--color-semantic-warning` comma `#fde68a` — semantic warning is text in `design-tokens`); `#authBanner.erp-auth-banner` `var(--pill-yellow-bg)` + `var(--color-warning-border-soft)` (changelog 226); plus changelog 225: `erp-master-spec-2026.css` `box-shadow` on help-tip flyout, fuel chrome, `.erp-dedicated-form-modal__shell` → `var(--shadow-dropdown)` only (3 — fallbacks never beat `design-tokens` when linked) (changelog 225); plus changelog 224: `erp-master-spec-2026.css` `.erp-dedicated-form-modal__backdrop` `background` → `var(--color-modal-backdrop)` (drop duplicate `rgba(15, 23, 42, 0.45)` — matches `design-tokens.css` literally) (changelog 224); plus changelog 223: `erp-master-spec-2026.css` pager / modal — `var(--text, #0f172a)` → `var(--color-text-primary, var(--text))`; `var(--muted, #64748b)` → `var(--color-text-label, var(--muted))`; `--color-text-body` stack drops `#334155`; `--color-modal-backdrop` drops `rgba(0,0,0,0.45)` duplicate (~9 sites) (changelog 223); plus changelog 222: `erp-master-spec-2026.css` trims `--color-border-input`, `--color-text-label` (`#80868b` / `#64748b`), `var(--color-border, var(--line, …))` inner slates, `--color-bg-header|page`, `--color-border` / `#e2e8f0`, `--color-text-primary` / `#0f172a`; `board-nav.css` light dropdown `--line` stacks (~25 + 2) (changelog 222); plus changelog 221: `erp-master-redesign.css` `:root` finishes `--qb-page|green|icon-nav-bg|icon-nav-muted` token-only + `--color-bg-active` drops `#d2e3fc` (~6 sites) (changelog 221); plus changelog 220: `erp-master-redesign.css` `:root` `--qb-line|line-input|text|text-strong` → bare `--color-border|border-input|text-body|text-primary`; `--color-bg-header, #fafbfc` → `var(--color-bg-header)` (~15) + `:root` `--qb-line|line-input|text|text-strong` (4) (changelog 220); plus changelog 219: `erp-master-spec-2026.css` strips redundant `#e8eaed` / `#dadce0` / `#fafbfc` / `#3c4043` / `#202124` comma tails on `--color-border`, `--color-border-input`, `--color-bg-header`, `--color-text-body|primary` (~43 sites — Material-style fallbacks; `design-tokens` already define `--color-*`); plus changelog 218: `erp-master-spec-2026.css` `--qb-line` / `--qb-panel` + `--color-border, var(--qb-line)` + banking/settings primary-field `border-color` drop redundant `#e8eaed` / `#fff` / `#1a73e8` tails on `--qb-*` bridges (changelog 218); plus changelog 217: `erp-master-redesign.css` `--qb-muted` / `--qb-placeholder` → bare `--color-text-label` / `--color-text-placeholder`; `erp-master-spec-2026.css` drops Material-style `#5f6368` / `#9aa0a6` tails on those roles + `var(--qb-muted)` stacks (~18 sites); plus changelog 216: `erp-master-spec-2026.css` `--btn-info-border` token-only (drop `#c6dafc` near-duplicate tail on `.bp-driver-pay-chip`); `erp-master-redesign.css` `--qb-red` → `var(--color-semantic-error)` (Material `#d93025` tail removed — `design-tokens` `#b91c1c`); plus changelog 215: `erp-master-spec-2026.css` `--pill-blue-text` loses `#1967d2` tail (`+ New` / help / fuel chrome); `.mr-pill--yellow` + `.mr-violation-legend__dot--yellow` drop `#b06000` (`--pill-yellow-text` only); `.erp-reports-roadmap` `color` → `var(--pill-yellow-text)` (dead `#92400e` fallback — `--pill-yellow-text` already supplied `design-tokens.css`); `erp-master-redesign.css` `--btn-info-border` token-only (`#aec9f9` tail); `board-nav.css` hub accent row inner `--pill-blue-text` (`#1557a0` tail) (changelog 215); plus changelog 214: `erp-master-spec-2026.css` + `erp-master-redesign.css` + `board-nav.css` — `var(--pill-*|btn-*, #hex)` tails removed where literals match `design-tokens.css` (~35 sites); plus changelog 213: `erp-master-spec-2026.css` `.erp-help-tip__btn` `background` → `var(--color-bg-hover)` — `#f8f9fa` matches `design-tokens` `--color-bg-hover`, not `--color-bg-header`; plus changelog 212: `erp-master-spec-2026.css` dispatch `#dispatchApp` main column `background` drops `var(--bg, #f5f6f8)` hop → `var(--color-bg-page)`; `erp-master-redesign.css` `--qb-panel` → `var(--color-bg-card)` (`#ffffff` tail removed); plus changelog 211: `board-nav.css` hub dropdown + strip `--color-hub-*`, `--color-border-focus`, and `--color-bg-card` (~15) lose redundant `#hex` tails where literals match `design-tokens.css`; plus changelog 210: `erp-master-redesign.css` `--qb-blue` + `--color-bg-card|header|hover` and `erp-master-spec-2026.css` `--color-text-placeholder` (~29 token-exact sites) drop hex tails matching `design-tokens.css`; plus changelog 209: `erp-master-spec-2026.css` second `replace_all` pass drops `--color-hub-*`, catalog `--color-text-body|primary`, `.erp-connection-strip` ok/warn `--color-success|warning-border-soft`, and roadmap `--color-semantic-warning` hex tails that match `design-tokens.css` (~15 sites); plus changelog 208: `erp-master-spec-2026.css` `replace_all` strips redundant `var(--color-*, #hex)` tails where literals match `design-tokens.css` (`border-focus`, `bg-header`, `bg-card`, `bg-hover`, `text-label`, `border` on hub / reports / pager / AP-style shells — ~53 sites); plus changelog 207: `dispatch.html` load-board `<style>` removes the last `var(--color-*, var(--text-secondary|line-strong))` legacy stacks (`.st-default` ink, `.btn-primary:hover` `border-color`) → bare `var(--color-text-body)` / `var(--color-border)` with `design-tokens.css` linked first; plus changelog 206: `fuel` / `dispatch` / `banking` / `settings` / `index` / `maintenance` strip `var(--color-*, #hex)` comma tails where `design-tokens.css` already defines the same values — `maintenance.html` avoids `RULE0` forbidden `var(--color-bg-header|bg-hover|nav-bg, #` substrings; plus changelog 205: `fuel.html`, `dispatch.html`, `banking.html`, `settings.html` page `<style>` (and inline `select` rows) drop `var(--line)` / `var(--panel)` / `var(--muted)` / `var(--text)` / `var(--bg)` comma stacks on `--color-border|bg-card|text-label|text-body|bg-page` and `--color-app-frame-border` — token-first with `design-tokens.css` linked first; plus changelog 204: `maintenance.html` removes `var(--color-border-focus, var(--accent))` (7 inline / JS sites) → `var(--color-border-focus)` so `scripts/system-smoke.mjs` `RULE0_FORBIDDEN_SUBSTRINGS` passes — maintenance HTML is intentionally barred from that comma-stack spelling (Agent B merge guard); plus changelog 203: ordered `replace_all` sweep on `public/css/app-theme.css` + `public/css/maint-accounting-ui-2026.css` restores 198–201 comma stacks on bare `--color-text-*`, `--color-border`, `--color-bg-*`, `--color-semantic-*`, `--color-border-focus`, `--color-modal-backdrop`, `--color-app-frame-border`, `--color-warning|success-border-soft`, and `--color-hub-*` call sites, then `app-theme` `nav-bg` `#16213e` tail re-applied; plus changelog 202: `app-theme.css` `replace_all` `var(--color-nav-bg)` → `var(--color-nav-bg, #16213e)` (~~7 CSS + header) so sidebar gradients, shop/repair active fills, SVG accents, and fuel-import sticky `thead` stay navy when tokens are absent; plus changelog 201: `app-theme.css` + `maint-accounting-ui-2026.css` add `--bg` / hex` tails on bare `--color-bg-page|header|hover` so page canvas, sticky headers, and hover washes match 197 `maintenance.html` hex policy); 200 card + 199 border + 198 typography + 197 `maintenance.html` remain; headline ~~66.3% unchanged within rounding.\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6634 (66.34%; headline ~66.3%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 385. Accounting board posting preflight (`public/maintenance.html`, Rules 13 + 14 + 22 + DEFERRED §2): static `mini-note` + `erp-help-tip` above `#maintPostingPreflight` (inside `#maintIntegrationStrip`) — documents `runPostingPreflight()` `textContent` / `display` on WO + AP gaps; called from `loadAll()`.\par 386. WO save toolbar status hosts (`public/maintenance.html`, Rules 12 + 14 + 22 + DEFERRED §2): static `mini-note` + `erp-help-tip` above `#maintSaveHint` / `#maintEditRecordHint` (full-width row inside `maint-form-actions__meta`) — documents `maintToneSaveHint()` + `saveRecordWithAction()` / `maintSaveFromSplitMenu()` / print-draft paths for `#maintSaveHint`; `updateMaintEditRecordHint()` / `hydrateMaintenanceRecordForm()` / `clearMaintenanceEditMode()` for `#maintEditRecordHint`.\par 387. Accounting AP PDF parse status (`public/maintenance.html`, Rules 13 + 14 + 22 + DEFERRED §2): static `mini-note` + `erp-help-tip` above `#apInvoicePdfStatus` — documents `parseApInvoicePdf()` `textContent` and `applyParsedExpenseInvoicePdf(..., 'ap')`; keeps separate product `erp-help-tip` beside the status line.\par 388. WO print banner (`public/maintenance.html`, Rules 12 + 14 + 22 + DEFERRED §2): static `mini-note` + `erp-help-tip` above `#maintWoBanner` — documents `syncMaintWorkOrderHeader()` → `#maintWoBannerUnit` `textContent` + vendor / WO field wraps by `#repairLocationTypeInput`; `maintPrintWorkOrderView()` scrolls to banner before print.\par 389. Banking + settings satellites Rule 22 copy (`public/banking.html` + `public/settings.html`, Rules 14 + 22 + DEFERRED §2): `banking.html` — page lead + CSV import step `erp-help-tip`; `settings.html` — Users / Employees `erp-help-tip` + employees `mini-note` — shorter static copy only (behavior unchanged; complements changelog 57 banking tips).\par 390. Banking + settings dynamic hosts (`public/banking.html` + `public/settings.html`, Rules 14 + 22 + DEFERRED §2): `banking.html` — static `mini-note` + `erp-help-tip` above `#snapMeta` / `#snapMetrics`, `#importOut`, `#suggestOut` (snapshot / CSV import / match suggestions `textContent` / `innerHTML`). `settings.html` — static leads above `#usersBody` / `#empBody` (`GET /api/users` / `GET /api/erp/employees` table markup).\par 391. Fuel board dynamic hosts (`public/fuel.html`, Rules 14 + 22 + DEFERRED §2): static `mini-note` + `erp-help-tip` above `#fleetSummary` / `#fleetAssigned` / `#fleetUnassigned`, `#selectedDetail`, `#fuelLedger` / `#fuelLedgerMeta`, `#recommendedStops` — documents `renderFleet()`, `renderSelectedDetail()`, `renderFuelLedger()`, `recommendForSelected()` vs `GET /api/board` / `GET /api/fuel/purchases` (behavior unchanged).\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}