{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T18:52:32.395Z\par \par --- Summary ---\par Overall completion: ~65.1% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6512 → 65.12%; rounded headline ~65.1%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6512 (65.12%; headline ~65.1%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 86. Topbar workspace tip + modal border + smoke (`maintenance.html` + `erp-master-spec-2026.css` + `erp-master-redesign.css` + `system-smoke.mjs`, Rules 2 + 0 + 22): `.topbar-hint-wrap` — short lead + `erp-help-tip` for rail / search / + New; ≤720px hides `.topbar-hint--lead` only; `.modal` border token; smoke `/maintenance.html` also requires `topbar-hint-wrap`.\par 87. Modal backdrop token + narrow sidebar padding (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 1): `--color-modal-backdrop` on `:root`; maintenance `.modal-bg` uses `var(--color-modal-backdrop, …)`; at ≤900px, `.erp-module-sidebar__head` / `__inner` / `__footer` horizontal padding tightens slightly.\par 88. Dispatch modal parity + satellite tablet padding + cost header token (`erp-master-spec-2026.css`, Rules 0 + 1): Dispatch `.modal-bg` / `.modal` match maintenance token pattern; `.mr-kpi-card--ok` border + `.maint-cost-table thead th` background use `var(--color-*)`; ≤900px tighter horizontal padding / margins on dispatch board wrap + strip, banking `.wrap`, settings `body`, fuel connection strip.\par 89. Connection strip tier borders + dedicated modal backdrop + hub tablet strip (`design-tokens.css` + `erp-master-spec-2026.css`, Rules 0 + 1 + 24): `--color-success-border-soft` / `--color-warning-border-soft`; `.erp-connection-strip--ok` / `--warn` border colors; `.erp-dedicated-form-modal__backdrop` uses `--color-modal-backdrop`; ≤900px company hub `.erp-connection-strip` horizontal padding 24px → 16px.\par 90. Hourly Word-openable report + dispatch depriority + maintenance KPI/toast tokens (`scripts/generate-erp-progress-report.mjs` + `docs/reports/*` + `package.json` + `ERP_MASTER_REDESIGN_STATUS.md` + `erp-master-spec-2026.css`): `npm run report:erp` → `docs/reports/ERP_MASTER_REDESIGN_PROGRESS_latest.rtf` (open in Word; cron per `docs/reports/README.md`); guardrails: dispatch lowest priority; Workspace snapshot `.mr-kpi-card*` typography + bad tier + `.erp-toast__x` use design-token fallbacks (no dispatch-only changes in this item).\par 91. QBO inline banner + +New column titles (`erp-master-spec-2026.css`, Rules 0 + 15 + 4): `.mr-qbo-banner*` icon/title/meta/list/severity/buttons + degraded tier + `body.erp-maintenance .erp-new-menu__col-title` use `var(--pill-red-*|color-text-*|color-border|color-bg-card|color-border-focus)` fallbacks.\par 92. Maintenance pills + sidebars + map/AP chrome (`erp-master-spec-2026.css`, Rules 0 + 4 + 12): `.mr-pill--*` use `var(--pill-*-*)`; + New `erp-new-menu__item` uses `var(--pill-blue-text)`; `.erp-module-sidebar` / `.erp-reports-sidebar` group chrome + WO parts map + `#erpTrackingMap` + Accounting AP `qb-doc-shell--ap-spec` topbar/backlink/hover use `var(--color-*|pill-blue-*)` fallbacks.\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}