{\rtf1\ansi\deff0{\fonttbl{\f0\fnil Calibri;}{\f1\fnil Courier New;}}\f0\fs22 IH35 ERP — Master redesign progress report\par Generated (UTC): 2026-04-17T19:54:56.596Z\par \par --- Summary ---\par Overall completion: ~65.8% toward the documented intent of Rules 0–24 in this repo (not pixel-perfect spec parity). Figures are for planning and continuity; they are judgment-based, not a contract metric. (Average of the per-rule fractions in the table below ÷ 25 ≈ 0.6580 → 65.80%; rounded headline ~65.8%.)\par Rolling average (above table): sum of fractions ÷ 25 ≈ 0.6580 (65.80%; headline ~65.8%). When reporting updates, revise fractions (not the formula) as work lands, then re-average.\par \par --- Policy ---\par Autonomous redesign policy (this repo): Loads & dispatch board (dispatch.html) is LOWEST PRIORITY until other checklist work is further along; skip dispatch-only UI passes unless explicitly doing end-of-list cleanup. Progress % and changelog are sourced from ERP_MASTER_REDESIGN_STATUS.md.\par \par --- Recent changelog (last up to 12 numbered entries) ---\par 103. Maintenance legacy surfaces + drawer borders (`app-theme.css`, Rules 0 + 12 + 13 + 16): `.erp-drawer*` borders use `var(--color-border|line)`; Safety `.safety-table-wrap`, shop `.shop-col-add`, tracking map / cards / iframe / tire / parts-map host surfaces; fuel import sticky `thead` use `--color-nav-bg|sidebar-*` + `--color-bg-header|card|hover` fallbacks; WO full modal header gradient + title/body + `.maint-modal` surface tokenized.\par 104. Maintenance QB doc + shop + nav dropdown + WO chrome (`app-theme.css`, Rules 0 + 2 + 11 + 12 + 13): `.qb-doc-*` / `.qb-lines-*` / `.qb-attach-box` / `.qb-btn-del` surfaces + borders + titles; `.shop-col-head`; `.nav-dropdown-menu` border/shadow + `.nav-dd-item*` / `.nav-dd-sep`; `.maint-modal-bg` `--color-modal-backdrop`; WO KPI / asset header / status pill / cost table header + modal flow title.\par 105. WO form + save split flyout (`app-theme.css`, Rules 0 + 12 + 13 + 20): `.maint-field--readonly`; repair chips + cost line header/acct/extras rails; `.maint-wo-banner*` gradient + eyebrow/unit text (banner hairline `box-shadow` unchanged); `.maint-subcard*` / `.maint-save-toolbar*` gradients + `var(--shadow-sm)` where applicable; `.qb-split__menu*` surfaces + `var(--shadow-dropdown)` + focus/hover tokens; neutral split caret; `#maintSaveHint` caution/error → `--color-semantic-*`; `.maint-panel-head-center` + `.maint-cost-coordination` surfaces.\par 106. Security alerts + catalog + print + fuel scroll (`app-theme.css`, Rules 0 + 12 + 13 + 16): `.sec-alert-high|warn|info` borders ordered so `border-left` stripe wins + `pill-*` / `btn-danger-border` / `color-warning-border-soft` / `color-semantic-warning`; `.catalog-admin-table` `th`/`td` borders + `th` `--color-bg-header`; `@media print` `body.erp-maintenance` page background `var(--color-bg-card)`; `.acct-fuel-import-scroll` border/surface tokens.\par 107. Save split + QB inputs (`app-theme.css`, Rules 0 + 13 + 20): `.qb-split--primary|neutral` shells (`--btn-primary-save-bg` border + `--sidebar-muted` + `--color-bg-card`); neutral `.qb-split__main` gradient/text/hover + primary label/caret `--color-bg-card`; `:focus-visible` rings `--color-warning-border-soft`; `.qb-split__caret` divider `color-mix` on `--color-bg-card` (with rgba fallback); `.qb-in` border `--color-border|line`.\par 108. Shop + board nav + KPI strip (`app-theme.css`, Rules 0 + 12): `.maint-exp-pill` / `.shop-queue-card*` surfaces + borders + overdue `--color-semantic-warn-accent` border; `.nav-badge--danger` label `--color-bg-card`; `.maint-board-nav__btn:hover` `color-mix` on `--accent` (rgba fallback); `.maint-board-nav__btn.active` `--app-frame-border` / `--sidebar-text` / `--color-nav-bg|sidebar-bg`; `.maint-kpi-card` border/surface tokens.\par 109. Catalog + picker + dashboard cards (`app-theme.css`, Rules 0 + 12): `.catalog-add-card*` / `.catalog-search-results` borders + card surface; `.qb-picker-menu` border + prefers `var(--shadow-dropdown)` over `--shadow-md` (literal fallback); maintenance `.hero-card` / `.panel` / `.metric` / `.record-card` `--color-bg-card|border`; `.qb-doc-actions` top rule `--color-border|line`.\par 1. Priorities: Which rules are mandatory for the next release vs nice-to-have (spec asks for everything at once; engineering may still sequence).\par 2. Rule 10 / React path: Shared component + build step vs one canonical HTML/JS cost-line implementation.\par 3. Rule 4 — + New menu: Which rows are deep-links vs new modals (avoid new save APIs unless explicitly scoped).\par 4. Rule 18: Thin `GET /api/qbo/accounts`-style aliases vs documenting existing catalog routes only.\par 5. Regression: Run server + manual click-through + `node scripts/system-smoke.mjs` when convenient before release.\par \par --- Files ---\par Source: docs/ERP_MASTER_REDESIGN_STATUS.md\par Consolidated report: docs/ERP_MASTER_REDESIGN_FINAL_REPORT.md\par \par Open this .rtf in Microsoft Word. To keep .docx, use File → Save As → Word Document (.docx).\par}